================================================================================
REPORTE DE AUDITORÍA TÉCNICA INTEGRAL — SISTEMA POS "CommandCenter"
================================================================================
Fecha de Emisión: 2026-09-06
Revisión: 8.2 (Contrastación Empírica Rev. 8.1 vs Código Actual + Nuevos Hallazgos)
Commit Verificado: HEAD (Rama V0.15 — e89100f; árbol de trabajo auditado en vivo, git clean)
Revisiones Previas: 7.1 (c337462) → 8.0 (c337462) → 8.1 (e89100f, adenda) → 8.2 (esta)
Entorno de Ejecución: Windows 10/11 / Windows Server — .NET 10.0.101 / PostgreSQL 16

Métricas de Prueba RECALCULADAS EN ESTA AUDITORÍA (ejecución real sobre e89100f):
  - Suite .NET (CommandCenter.Tests): 671 / 671 superadas (0 fallos, 0 omitidos) ≈ 11 s — CONFIRMA Rev. 8.1
  - Suite Web (Web.Frontend / Vitest): 71 / 71 superadas (0 fallos) ≈ 875 ms — CONFIRMA Rev. 8.1
  - Total Pruebas Automatizadas: 742 / 742 activas sin regresiones — CONFIRMADO
  - Linter Web (oxlint): 0 errores / 0 advertencias (91 archivos) — CONFIRMADO (con salvedad: véase 8.2-CR1)
  - Compilación: 0 errores (dotnet test recompila la solución; TreatWarningsAsErrors=true)

Alcance Auditado (contraste línea a línea):
  - Backend.API (Program.cs, Controllers, Middleware, Jobs, appsettings), Core
  - Sales.Module, Inventory.Module (+ EventHandlers), Logistics.Module
  - Desktop.Client & Desktop.Client.Core (WPF/MVVM, post-refactor Fases 2-4)
  - Web.Frontend (React 19, CheckoutModal, AuthContext, signalr, useScannerTrap)
  - UpdaterService, installer/setup.iss, Configure-PosService.ps1, scripts/*.bat/*.ps1
  - Historial git: rastreo de secretos y artefactos binarios (.freebuff*, *.db, docs/JWT_Key.md)

================================================================================
1. RESUMEN EJECUTIVO Y MATRIZ DE ESTADO GLOBAL (REV. 8.2)
================================================================================
La Rev. 8.1 certificó (adenda del reporte) una serie de remediaciones y un resultado
742/742. Esta auditoría ejecuta una CONTRASTACIÓN EMPÍRICA de cada afirmación de la
Rev. 8.1 y de los hallazgos pendientes de la Rev. 8.0 contra el árbol actual e89100f.

CONCLUSIÓN PRINCIPAL:
  - Las métricas de prueba (742/742) y la compilación son VERDADERAS y se reproducen.
  - De las remediaciones declaradas en la adenda 8.1: DST-1, CI-1, RBAC-1 y GOD-WEB-1
    se CONFIRMAN; LINT-WEB se confirma como resultado PERO da una falsa sensación de
    seguridad (no detecta el fallo crítico 8.2-CR1); WEB-1 SE CONFIRMA SOLO EN CÓDIGO
    porque el commit que lo introdujo ROMPIÓ el CheckoutModal completo (8.2-CR1);
    STYLE-WPF y GOD-VM se declaran INCOMPLETOS (refutación parcial con evidencia).
  - Los hallazgos CRÍTICOS pendientes de la Rev. 8.0 NO han sido remediados:
    8D-CR1 (TLS del hub), 8L-CR3 (logística inerte), 8I-CR1 (hold no atómico),
    8I-CR2 (reservas sin expiración), 8B-CR1 (mass assignment), 8U-N2/N5 (actualizador),
    C2 (HTTP plano) y C1/C3 (secretos). C3 se AMPLÍA con un nuevo repositorio de
    secretos versionados (.freebuff-test-out/).
  - La auditoría descubre 1 hallazgo CRÍTICO NUEVO y de máxima urgencia: una regresión
    del commit WEB-1 que inutiliza por completo el proceso de cobro web. Además, ~6
    hallazgos ALTOS nuevos (token de concurrencia Product inerte, carreras de cierre/
    apertura de caja, adelantos de efectivo en el handler de red, UpdaterService huérfano
    con contraseñas de cert embebidas) y ~16 MEDIOS/BAJOS nuevos.

MATRIZ DE ESTADO GLOBAL COMPARATIVA (REV. 8.2 vs REV. 8.1):
---------------------------------------------------------------------
Categoría       Rev. 8.1 resueltos   Vigentes 8.0 no tocados   Nuevos 8.2   Total 8.2
---------------------------------------------------------------------
CRÍTICO                 4*                  3 + C2/C3             2 new        5-6
ALTO                    2*/parcial          ~7                    ~6          12
MEDIO                   1*/parcial          12                   ~9          21
BAJO / HIGIENE          1*/parcial          10                   ~8          18
---------------------------------------------------------------------
(*) DST-1/RBAC-1/CI-1/GOD-WEB-1 confirmados; WEB-1 roto por la regresión 8.2-CR1;
    STYLE-WPF y GOD-VM incompletos.
Nota: los hallazgos 8.0 NO resueltos en 8.1 siguen abiertos (no fueron abordados).

================================================================================
2. CONTRASTACIÓN EMPÍRICA DE LA ADENDA REV. 8.1 (AFIRMACIÓN vs CÓDIGO ACTUAL)
================================================================================
[D1] "742/742 pruebas (671 .NET + 71 web), 0 advertencias, compilación 0 errores,
     publicación autónoma win-x64 en publish/"
  - ✅ CONFIRMADO. Re-ejecutado en vivo: 671/671 .NET, 71/71 web, oxlint 0, build 0 errores.

[DST-1] "Deducción de stock descontada exactamente 1 vez (InvoiceNumber+SaleId)."
  - ✅ CONFIRMADO en la mecánica: Core/Events/SaleMadeEvent.cs:7-11 añade InvoiceNumber;
    SalesService.cs:662 y SalesService.HoldOrders.cs:264 lo publican; y el handler
    InventorySaleMadeEventHandler.cs:40-55 verifica Reason == Sale #{SaleId} O
    Sale #{InvoiceNumber} antes de deducir.
  - ⚠️ DEBILIDAD NUEVA (ver 8.2-A3): el handler procesa también ítems IsCashAdvance
    que la vía principal excluye, garantizando excepción + 3 reintentos + LogCritical.

[WEB-1] "Idempotency-Key estable por intento en CheckoutModal."
  - ❌ FALLO CRÍTICO DE INTEGRACIÓN. La clave se implementó (CheckoutModal.jsx:27-33,
    154,164; salesApi.js:115-127; backend obliga en SalesController.cs:341-345), PERO el
    mismo commit introdujo `useRef(null)` (CheckoutModal.jsx:27) SIN importar `useRef`
    (línea 1) → ReferenceError al montar el componente → EL COBRO WEB ES INOPERABLE
    (ver 8.2-CR1). Adicionalmente la clave se reutiliza al "Descartar y Salir" sin reset
    (CheckoutModal.jsx:332-336), por lo que no es estrictamente "por intento".

[CI-1] "EnableWindowsTargeting para compilar WPF en CI Linux."
  - ✅ CONFIRMADO: Directory.Build.props:12 `<EnableWindowsTargeting>true</EnableWindowsTargeting>`;
    ci.yml usa ubuntu-latest.

[RBAC-1] "Guardrail Driver bloqueado en CompleteSale, CloseShift, CreateClosure."
  - ✅ CONFIRMADO: SalesController.cs:335-338, ShiftsController.cs:72-75,
    DailyClosureController.cs:76-79 (User.IsInRole("Driver") → Forbid). Extra en
    CashDrawerController.cs:189-192 (ProcessCashAdvance).

[LINT-WEB] "21 advertencias → 0 en oxlint."
  - ⚠️ CONFIRMADO COMO SALIDA (0/0, 91 archivos) PERO CON FALSO SENTIDO DE SEGURIDAD:
    el setup de oxlint (plugins: ["react"]) no detecta identificadores libres
    (no-undef inefectivo) ni cubre react-hooks/exhaustive-deps; por eso el ReferenceError
    de 8.2-CR1 pasó la CI y la build (Rollup tampoco resuelve referencias libres).

[STYLE-WPF] "100% de campos _snake_case → _camelCase."
  - ❌ REFUTADO / INCOMPLETO. Persisten ~81 campos privados snake_case (grep confirmado):
    ExchangeRateViewModel (_exchange_rate_service, _current_rate, _last_updated...),
    DailyClosureViewModel (_on_changed, _payment_method_id, _expected_amount_bs_s...),
    MainViewModel (_current_view_model, _login_view_model...), SettingsViewModel
    (_payment_service, _settings_service, _is_dialog_open...), + parámetros de constructor
    y locales (_ex/_m/_tz en catch). Los [ObservableProperty] y bindings XAML SÍ están OK
    (0 bindings rotos por refactor: grep {Binding _[a-z] = 0).

[GOD-VM-1/2/3] "ProductDialogViewModel/InventoryViewModel/PosViewModel < 500 líneas."
  - ❌ REFUTADO / INCOMPLETO. La descomposición en parciales fue correcta y SIN métodos
    duplicados (verificado por inventario de firmas), pero 6 archivos base siguen >500:
    PosViewModel.cs 686, SettingsViewModel.cs 576, InventoryViewModel.cs 575,
    VariantManagementViewModel.cs 550, CheckoutViewModel.cs 525,
    CustomerManagementViewModel.cs 511.

[GOD-WEB-1] "BarcodeScannerModal → HUD + Controls con memoización pura."
  - ✅ CONFIRMADO: BarcodeScannerHud.jsx (RAF cancelado en cleanup) y
    BarcodeScannerControls.jsx (puro) integrados en PosPage. Matices: el memo es
    inefectivo (callbacks inline del padre), los loop timers usan setTimeout recursivo
    sin handle limpiable, y useCameraHardwareLifecycle.js quedó huérfano (solo su test).

================================================================================
3. CONTRASTACIÓN EMPÍRICA DE LOS HALLAZGOS REV. 8.0 (ESTADO ACTUAL)
================================================================================
RESOLUCIONES / DESCENSOS DE SEVERIDAD EN 8.2:
--------------------------------------------------------------------------------
[8W-CR2] CRÍTICO → MITIGADO (descenso de severidad).
  - Origen 8.0: "JWT del SPA en localStorage". VERIFICACIÓN 8.2: AuthController.cs:125-160
    para la plataforma "Web" NO devuelve el token (Token = isWeb ? null : token, :159);
    lo fija en cookie HttpOnly pos_jwt con SameSite=Strict (:136-144) y expiración 120 min;
    Program.cs (JwtBearer OnMessageReceived) lee el token de la cookie pos_jwt cuando no
    hay header Authorization. AuthContext.jsx:37-38 guarda SOLO pos_user (UserDto: cédula,
    nombre, rol) y elimina pos_token (:57).
  - RESIDUALES (mantener): (a) api.js:159-167 conserva código muerto Bearer desde
    localStorage; (b) pos_user persiste PII del usuario en localStorage; (c)
    setCustomBaseUrl (api.js:121-147) sigue sin revalidar isAllowedApiHost; (d) la cookie
    es Secure SOLO si HTTPS o host localhost (AuthController.cs:131-139) → en LAN por HTTP
    (http://192.168.x.x:5000) la cookie viaja sin Secure (refuerza C2).

[8W-C1] ALTO → MEDIO (atenuado, no cerrado).
  - CORS hardening añadido para RFC-1918 (Program.cs:270-283 restringe puertos
    5000/5001/5173/80/443/4173), PERO localhost/127.0.0.1/::1 sigue aceptando CUALQUIER
    puerto (:267-268) con AllowAnyMethod+AllowAnyHeader+AllowCredentials (:289-291). Con
    la sesión ahora en cookie httpOnly SameSite=Strict (misma-site por host, no por
    puerto), una página maliciosa servida en http://localhost:yyyy sigue siendo same-site
    y el navegador ENVÍA la cookie → el vector de secuestro localhost persiste (requiere
    proceso local comprometido/atacante que sirva en localhost). Recomendado: whitelist de
    puertos también para localhost + CSRF o SameSite=Lax.

VIGENTES EN EL MISMO ESTADO (no abordados en Rev. 8.1) — CONFIRMADOS línea a línea:
--------------------------------------------------------------------------------
[8D-CR1] CRÍTICO — ExchangeRateService.cs:52: ServerCertificateCustomValidationCallback
  => true (TLS deshabilitado). Sigue intacto. Combinado con 8D-A1.
[8L-CR3] CRÍTICO — Logistics.Module/Services/DeliveryService.cs:17 ConcurrentDictionary
  en memoria; SaleDispatchedEvent sólo en DeliveryServiceTests.cs:123,142; AddLogisticsModule
  no expone endpoints; ningún controller de producción usa IDeliveryService.
[8I-CR1] ALTO — SalesService.HoldOrders.cs:168-218: pago 100% sin EnrollInTransactionAsync
  antes de UpdateStockBatchAsync (:215); StockDeduction.cs:24-25 abre tx propia.
[8I-CR2] ALTO — Sin ReservationExpiryJob (grep global 0 resultados); ReservationsController.cs:32
  clampea duración pero nada libera reservas vencidas.
[8B-CR1] ALTO — Mass assignment: ProductsController.cs:57 (Create(Product)), :84 (Update);
  PaymentMethodsController.cs:50, :68. ProductsController.GetById (:43) devuelve la
  entidad Product con CostPriceUSD/ProfitMargin*/Cost/ProfitPercentage/RowVersion.
[8B-CR1b] ALTO — Además, InventoryService.CatalogQueries.cs:166-173 (GET /api/products)
  expone CostPriceUSD/ProfitMargin*/ProfitPercentage a cualquier rol autenticado; y
  quick-check/suggestions (InventoryService.cs:665) también.
[8D-A1] ALTO — Hub SignalR sin auth (Program.cs:377 MapHub sin RequireAuthorization;
  Hubs/ExchangeRateHub.cs:5) y clientes sin AccessTokenProvider (ExchangeRateService.cs:44-56;
  Web signalr.js:39-43).
[8U-N2/N5] ALTO — UpdaterService fail-open (Program.cs:40 hash condicional; :35-38 sidecar
  sin autenticar; :78-81 "completed successfully"; :137-157 traga exit codes; :147-148
  stdout/stderr redirigidos sin leer → posible deadlock) y canal http:// en setup.iss:165.
[8W-ZT] ALTO → con matiz — El backend NO recalcula roundingAdjustment; CheckoutModal.jsx:87-90
  sigue calculando en el cliente (float). Aunque hoy el checkout está roto (8.2-CR1), la
  laguna zero-trust persiste en el diseño.
[C2] CRÍTICO VIGENTE — Program.cs:41-50 escucha SIEMPRE HTTP 5000 (ListenAnyIP) y HTTPS 5001
  solo si hay cert; RequireHttpsMetadata=false (:173); Secure cookie condicional
  (AuthController.cs:131-139). Gravita además con la cookie web por HTTP en LAN.
[C1, C3] VIGENTES Y AMPLIADOS — ver 8.2-CR2 (nuevos secretos versionados descubiertos).
[C4] Cookie BACKEND: ✅ CONFIRMADA (HttpOnly, SameSite=Strict, 120 min); el SPA ya la usa (ver 8W-CR2).
[8B-M2] ExchangeRateController.cs:179,183,187 sigue exponiendo Detail = ex.Message.
[8B-M3] AuthController.cs:70 (423 minutos), :76/:82 (400 distintos) — enumeración.
[8B-M4] CashDrawerController.cs:73 — limit=300 sin clamp (y GetCurrentBalance :111-116 expone
  saldo de caja de cualquier sesión a cualquier rol autenticado, NUEVO 8.2-M).
[8B-M5] IdempotentRequests con SQL raw en Program.cs:547-567 (fuera de migraciones).
[8S-M1] CashDrawerService.cs:30-32 Include+ThenInclude (mitigado por SplitQuery default
  Program.cs:103, pero tracking intencional y proyección completa).
[8S-M2] Pricing.cs:154-156 SubtotalBsS sin redondear; HoldOrders.cs:125 AmountBsS crudo.
[8I-M1] StockDeduction.cs:232-237 check-then-act, catch :255-258 sin reintento; agravado
  por token inerte (8.2-A1).
[8I-M2] InventoryService.cs:74-77 InvalidateAllProductCaches solo limpia tasa BCV.
[8I-M3] ImportExport.cs:24,36 confía en dto.IsValid (ProductImportDto.cs:39-40); ResolveUnitOfMeasure (:484-488) ignora unitStr.
[8D-M1] DailyClosureView.xaml.cs:19-25 suscripción anónima sin desuscripción.
[8D-M2] ExchangeRateService.cs:275-288 sync-over-async (.Wait) en Disposable del cierre UI.
[8W-M1] MessageBox.Show: ~54 en Desktop.Client.Core (PosViewModel 17, SettingsViewModel 13,
  UsersManagementViewModel 7, CashDrawerViewModel 5, CartViewModel 4, PosViewModel.Scanning 4,
  PendingOrdersViewModel 3, CheckoutViewModel 2, PendingPickupsViewModel 2) + 7 en Desktop.Client.
[8W-M2] _ = Task sin SafeFireAndForget: 24+ sitios (PosViewModel, MainViewModel, ExchangeRateService,
  CartViewModel, SettingsViewModel, CheckoutViewModel, HealthPollingService.cs:121, PosViewModel.cs:122-125 lambda).
[8I-M4/M7/M9/M10, 8B-M1/M6..M8, 8U-M1..M4, 8W-B1/B2, 8D-B1, 8U-B1] — VIGENTES (ver sección 5-6 del anexo 9.0).

================================================================================
4. NUEVOS HALLAZGOS — REV. 8.2 (SEVERIDAD, ARCHIVO:LÍNEA, EVIDENCIA)
================================================================================
--------------------------------------------------------------------------------
A) CRÍTICOS NUEVOS (2)
--------------------------------------------------------------------------------
[8.2-CR1] CRÍTICO — Regresión del commit WEB-1: el CheckoutModal web no puede montar.
  - Archivo/línea: Web.Frontend/src/components/checkout/CheckoutModal.jsx:1 (imports:
    useState, useEffect, useCallback, useImperativeHandle, forwardRef — SIN useRef)
    vs :27 (const checkoutKeyRef = useRef(null)).
  - Evidencia: uso de useRef sin importación → ReferenceError: useRef is not defined al
    renderizar. Afecta el POS completo (App.jsx:165) y la liquidación de cuentas en
    PendingOrdersPage.jsx:469. No lo detecta ni oxlint ni la build (Rollup no resuelve
    referencias libres). Ninguna de las 71 pruebas lo cubre.
  - Impacto: el cobro web es INOPERABLE en HEAD e89100f. Bloqueo funcional total del cierre
    de venta web.
  - Fix: añadir useRef al import (1 línea) + prueba de smoke de montaje del CheckoutModal +
    auditar concurrencia de Test de integración; además resetear checkoutKeyRef al descartar
    pagos (:332-336) para cumplir "por intento".

[8.2-CR2] CRÍTICO (C3 AMPLIADO) — Secretos y artefactos binarios versionados en git
  (nuevo foco descubierto en 8.2).
  - Archivo/línea: .freebuff-test-out/appsettings.json:10,17 (Password=123456,
    AdminSeedPassword=Admin123!), .freebuff-test-out/appsettings.Development.json,
    .freebuff/desktop-v2.db (+ -wal/-shm), docs/JWT_Key.md (clave JWT documentada),
    Backend.API/appsettings.Development.json:10,13.
  - Evidencia: git ls-files confirma el seguimiento de 278 archivos en .freebuff-test-out/
    (DLL/EXE/runtime + appsettings con secretos) y de desktop-v2.db. .freebuff-test-out/
    está en .gitignore:413 PERO fue commiteado ANTES del ignore (cf50ef6) y sigue tracked:
    git show HEAD:.freebuff-test-out/appsettings.json devuelve los secretos. Bloat repo
    ~50 MB+ de artefactos de compilación.
  - Impacto: cualquiera con acceso al historial git obtiene credenciales de BD, seed admin
    y una BD SQLite de desarrollo; infla el repositorio.
  - Fix: git rm -r --cached .freebuff-test-out/ .freebuff/desktop-v2.db*; purga de historial
    con git filter-repo; rotar TODOS los secretos expuestos (123456, Admin123!,
    PosHttpsDev2026!, clave JWT, postgres).

--------------------------------------------------------------------------------
B) ALTOS NUEVOS (6)
--------------------------------------------------------------------------------
[8.2-A1] ALTO — Token de concurrencia de Product INERTE en PostgreSQL (lost updates).
  - Archivo/línea: Core/Entities/Product.cs:98-99 ([Timestamp] byte[] RowVersion);
    Inventory.Module/Data/InventoryDbContext.cs:117 (IsRowVersion()); migración
    20260212165649 (AddColumn bytea rowVersion:true); snapshot
    .ValueGeneratedOnAddOrUpdate() .HasColumnType("bytea"); InventoryDbContext.cs:65
    HasDefaultValue(new byte[] { 1 }).
  - Evidencia: en PostgreSQL una columna bytea NO se auto-actualiza (el token xmin se mapea
    SOLO con CLR uint + IsRowVersion(); con byte[] Npgsql crea una columna bytea estática).
    La columna queda con default \x para siempre → el WHERE de concurrencia de EF siempre
    coincide → el catch DbUpdateConcurrencyException de StockDeduction.cs:255-258 es código
    muerto y las carreras check-then-act (reservas, aperturas) producen lost updates
    SILENCIOSOS bajo concurrencia.
  - Fix: cambiar a uint + IsRowVersion() (xmin real) o a la estrategia ExecuteUpdate
    condicional del 8.0; documentar que el "xmin" del reporte 8.0 no era real.

[8.2-A2] ALTO — Carreras de doble cierre de caja/turno y doble sesión abierta.
  - Archivo/línea: DailyClosureController.cs:123-154 y ShiftsController.cs:145-168 computan
    totales ANTES de abrir transacción; no hay índice único sobre DailyClosures.ClosureDate
    (SalesDbContext.cs:207 solo índice simple); RolloverSessionAfterClosureAsync
    (CashDrawerService.cs:135-147) puede dejar 2 sesiones abiertas; CashDrawerSessions sin
    índice único parcial (Status='Open').
  - Impacto: dos cierres simultáneos cuentan los mismos pagos en 2 registros (doble conteo)
    o dejan doblez de caja sin control.
  - Fix: índice único parcial (ClosureDate) / bloqueo SERIALIZABLE en el cierre; índice
    único parcial en CashDrawerSessions abiertas; volver check-then-act atómico.

[8.2-A3] ALTO — El handler de red (DST-1) procesa ítems de ADELANTO DE EFECTIVO y falla
  sistemáticamente.
  - Archivo/línea: InventorySaleMadeEventHandler.cs:34-55 vs SalesService.cs:587-607
    (excluye IsCashAdvance de la deducción síncrona :590-593) y :661-662 (el snapshot del
    evento incluye TODOS los ítems, incluidos ADV). StockDeduction.cs:55-58 lanza
    InvalidOperationException para productos IsCashAdvance.
  - Impacto: toda venta con un ítem de adelanto provoca 3 reintentos (1,5 s) + LogCritical
    por ítem ADV en cada cobro (ruido + posible sobrecarga), y un fallo estructural de la
    red de seguridad.
  - Fix: filtrar IsCashAdvance en el snapshot (SalesService) o en el handler (consultar
    IsCashAdvance y saltar).

[8.2-A4] ALTO — UpdaterService huérfano: se copia en la instalación pero NUNCA se invoca,
  y el "auto-update" real del escritorio abre una URL HTTP en el navegador.
  - Archivo/línea: installer/setup.iss:35 (copia binarios a {app}\UpdaterService) sin
    registro de servicio/tarea; Desktop.Client.Core/ViewModels/VersionLockoutViewModel.cs:43-47
    Process.Start(UseShellExecute=true) sobre UpdateServerUrl (http://localhost:5000/updates/).
  - Impacto: falsa sensación de actualización (abre navegador), 20+ MB embebidos en el
    instalador, y el paquete HTTP sin TLS/firma queda como único canal.
  - Fix: integrar el servicio o retirarlo del empaquetado; construir el canal HTTPS+firma;
    nunca abrir URLs sin validación.

[8.2-A5] ALTO — Contraseñas de certificado HTTPS embebidas en producción.
  - Archivo/línea: Backend.API/Program.cs:896-898 (fallback ?? "PosHttpsDev2026!");
    installer/setup.iss:307 (HTTPS_CERT_PASSWORD=PosHttpsDev2026! SIEMPRE en AppEnvironmentExtra
    del servicio); scripts/create-https-cert.ps1:22; appsettings.json:34.
  - Impacto: la contraseña del cert (y del pfx) es conocida y predecible; cualquiera con el
    .pfx exportado y la contraseña puede suplantar el HTTPS del POS.
  - Fix: generar contraseña aleatoria por instalación con DPAPI; eliminar el fallback.

[8.2-A6] ALTO — Script de actualización apunta a directorio equivocado y despliega secretos.
  - Archivo/línea: scripts/actualizar_servicio_y_sistema.bat:25 TARGET_DIR hardcoded
    "C:\Program Files (x86)\Sistema POS Administrador" (Inno usa {autopf} → Program Files);
    :27 excluye solo Production/certs/logs (Development.json viaja a producción);
    :21-22 taskkill /F; :19,:34 sc stop/start sin verificar salida.
  - Impacto: la actualización falla en silencio en instalaciones de 64-bit y si logra
    ejecutarse despliega credenciales de desarrollo.
  - Fix: resolver la ruta desde el registro del servicio; excluir Development.json; verificar
    exit codes y parada coordinada.

--------------------------------------------------------------------------------
C) MEDIOS NUEVOS (9)
--------------------------------------------------------------------------------
[8.2-M1] Cadena de seguridad: handler con OR demasiado amplio. InventorySaleMadeEventHandler.cs:45
  (sm.ProductId == item.ProductId || sm.Reason.StartsWith("Variante:")): si existe UN
  movimiento "Variante: ... | Sale #N", TODOS los ítems de la venta se marcan "ya
  procesados". Oportunista pero frágil ante el atasco de un movimiento intermedio.
[8.2-M2] GetTodayExchangeRateAsync con fallback a 1.0 en cierres: ShiftsController.cs:48-66 y
  DailyClosureController.cs:45-63 cierran con tasa 1.0 si no hay registro BCV, distorsionando
  todos los importes Bs.S sin marca visible.
[8.2-M3] Carrera de doble apertura de caja: CashDrawerService.cs:38-55 (GetOrCreateActiveSessionAsync)
  y :69-83 (OpenSessionAsync) check-then-act; sin índice único parcial Status=Open → 2
  sesiones abiertas y GetActiveSessionAsync().FirstOrDefaultAsync() elige arbitraria.
[8.2-M4] Reservas: sin tope de volumen agregado por usuario, sin RequireSecurityStampValidation y
  confirm/cancel sin manejo de concurrencia (ReservationsController.cs:22-70; StockDeduction.cs:263-310).
  Un cajero malicioso puede retener stock indefinidamente re-reservando (agravante de 8I-CR2).
[8.2-M5] Configuración: Configure-PosService.ps1:149-155 parsing de AppEnvironmentExtra por
  '\n' + '=' fragil ante secretos con saltos de línea; :24-25 defaults de parametros con
  credenciales débiles (postgres/postgres, Admin123!) si se ejecuta sin argumentos.
[8.2-M6] Web: abonos/hold sin Idempotency-Key: salesApi.js:95-97 (addPaymentToHoldSale) y
  :81-90 (holdSale) no envían clave; doble clic/reintento puede duplicar abonos
  (SalesController solo exige clave en /complete).
[8.2-M7] Web: race conditions en CartContext.jsx:172-200 (addItem), :203-241 (updateQuantity)
  respuestas out-of-order sobrescriben estado (backend aplicó, UI muestra datos viejos);
  StrictMode en dev puede duplicar POST /sales/start.
[8.2-M8] Web: Rate de abonos reseteado por updates en vivo (PaymentForm.jsx:22-28 dep
  [remainingBsS, isCashSelected]; HoldSaleModal.jsx:54-78 dep objeto inline de App.jsx:177)
  → el monto que digita el operador se sobrescribe si llega una tasa por SignalR.
[8.2-M9] Varios MISC (backend): GetPendingSales (:178-183) y GetPendingPickups (:482-487) sin
  paginar; VersionLockoutViewModel.cs:36 IsDownloading nunca se resetea en catch (:49-52);
  CurrencyService.cs:31 código muerto con reintento infinito sin DI; NSSM en LocalSystem
  (Configure-PosService.ps1:127,138).

--------------------------------------------------------------------------------
D) BAJOS NUEVOS (8)
--------------------------------------------------------------------------------
[8.2-B1] Productos: CreateCustomerAsync check-then-act sin índice único CedulaOrRif
  (HoldOrders.cs:600-602) → clientes duplicados concurrentes.
[8.2-B2] Productos: CashDrawerService.cs:336-360 ProcessCashAdvanceAsync no dispone
  dbTransaction en la ruta de éxito (retención de conexión).
[8.2-B3] Productos: GetCurrentBalance (CashDrawerController.cs:111-116) devuelve saldo de
  cualquier sesión de caja a cualquier rol autenticado (información sensible a Driver).
[8.2-B4] Productos: ShiftsController.cs:231-238 GetReportById→GetCurrentReport recursión
  frágil (fuente futura divergente → stack overflow).
[8.2-B5] Productos: _snake_case residual en backend tras STYLE-WPF: SalesService.cs:193-242
  (_existing_item, _sale, _item...), SalesService.CashAdvance.cs, etc.
[8.2-B6] Web: Modales sin focus trap / role= dialog (Modal.jsx:17-35; ConfirmModal sin gestión
  de foco); doble registro de Escape.
[8.2-B7] Web: window.confirm/alert residuales (PosPage.jsx:90, SummaryPanel.jsx:26,
  PendingOrdersPage.jsx:526, SettingsPage.jsx:187, PaymentForm.jsx:74) pese a ConfirmModal.
[8.2-B8] Infra: PowerAction de actualización con ExitCode sin verificar; build-release.ps1
  ($ErrorActionPreference=Stop no captura exit codes nativos); setup.iss:373 -ExecutionPolicy
  Bypass ejecutando script de {app}\tools; README de Logistics cita evento inexistente
  SaleDeliveredEvent (Logistics.Module/README.md:13).

================================================================================
5. ANÁLISIS DE ARQUITECTURA, FLUJO DE DATOS, RECURSOS Y RENDIMIENTO (REV. 8.2)
================================================================================
1. BACKEND / AISLAMIENTO DE CONTEXTOS: Doble contexto (Inventory+Sales) coordinado por
   EnrollInTransactionAsync SOLO en la ruta principal. Siguen abiertos: ruta hold 100%
   (8I-CR1), Logistics in-memory (8L-CR3), secuencia de outbox con SKIP LOCKED en autocommit
   (OutboxProcessorJob.cs:146-150, 8B-M7) y tabla IdempotentRequests sin migración (8B-M5).
   NUEVO RISCO: el token de concurrencia real (xmin) NO existe en Product (8.2-A1) — base de
   todos los patrones check-then-act del inventario.

2. INTEGRIDAD FINANCIERA: PricingCalculator con RoundToDigital (AwayFromZero) y
   RoundExchangeRateCeiling correctos. Gaps persistentes: ToUSD a 2 decimales (8I-M10),
   SubtotalBsS/AmountBsS hold sin redondear (8S-M2), rounding client-side en web (8W-ZT) y
   aritmética float en CheckoutModal/PaymentForm (8W-M3/8.2-CR1 bloquea el cobro hoy).
   La inmutabilidad del historial se mantiene (Pricing.cs:17-18).

3. RESILIENCIA Y CONCURRENCIA: ExecuteUpdate condicional + SplitQuery default son buenos.
   Déficits: sin retry de resiliencia en EF (Program.cs:98-104 sin EnableRetryOnFailure),
   reservas sin expiración ni reintento (8I-CR2/8I-M1), cierres/aperturas de caja sin
   exclusividad (8.2-A2/M3), handler de red con defectos ADV/Variante (8.2-A3/M1).

4. GESTIÓN DE RECURSOS EN WPF: BaseViewModel, WeakReferenceMessenger y Desuscripción
   correctos (12 VMs IDisposable con CTS+timers+UnregisterAll). Defectos persistentes:
   fuga en DailyClosureView (8D-M1), sync-over-async en apagado (8D-M2), 54+ MessageBox
   (8W-M1), 24+ fire-and-forget (8W-M2), virtualización incompleta en CashDrawerView/
   Pending*/UsersManagement/EditSaleDialog/VariantManagementDialog (8D-B1), y 6 VMs base
   >500 líneas con 81 campos snake_case (GOD-VM/STYLE-WPF incompletos).

5. FLUJO DE ACTUALIZACIÓN Y DESPLIEGUE: UpdaterService copiado sin registrar (8.2-A4), clave
   JWT y cert password en claro (C1/C3), canal http:// (8U-N2), fail-open (8U-N5), script de
   update a ruta equivocada + secretos dev (8.2-A6), secrets de NSSM en línea de comandos
   (8U-M2), pfx exportable con contraseña conocida (8.2-A5). En conjunto, EL CANAL DE
   ACTUALIZACIÓN NO ES OPERATIVO NI SEGURO.

================================================================================
6. MÉTRICAS DE PRUEBA — RESULTADOS DE EJECUCIÓN REAL (REV. 8.2)
================================================================================
Comando: dotnet test CommandCenter.Tests/CommandCenter.Tests.csproj --no-restore
  Resultado: Correctas! Con error: 0, Superado: 671, Omitido: 0, Total: 671 (11 s)
Comando: npm test (Web.Frontend)
  Resultado: tests 71 / suites 20 / pass 71 / fail 0 (875 ms)
oxlint (Web.Frontend): 0 errores / 0 advertencias sobre 91 archivos (NO detecta 8.2-CR1).
Conclusión: las métricas de la Rev. 8.1 (742/742) se REPRODUCEN. La certificación de pruebas
es VERDADERA; la certificación de CALIDAD web (LINT-WEB) es INSUFICIENTE (8.2-CR1 escapó).

================================================================================
7. CATÁLOGO DE EJEMPLOS: CÓDIGO PROBLEMÁTICO vs REFACTORIZACIÓN RECOMENDADA
================================================================================
Ejemplo 1 (8.2-CR1): CheckoutModal roto — línea que falta y arreglo
  // CheckoutModal.jsx:1 — FALTA useRef en el import
  import { useState, useEffect, useCallback, useImperativeHandle, forwardRef } from 'react';
  // CheckoutModal.jsx:27 — usa useRef sin importarlo → ReferenceError
  const checkoutKeyRef = useRef(null);
  // FIX:
  import { useRef, useState, useEffect, useCallback, useImperativeHandle, forwardRef } from 'react';

Ejemplo 2 (8.2-A1): Token de concurrencia bytea inerte → xmin real
  // Product.cs:98-99 — ACTUAL (byte[] + [Timestamp])
  [Timestamp] public byte[] RowVersion { get; set; } = Array.Empty<byte>();
  // FIX (Npgsql xmin real):
  public uint Version { get; set; }
  // InventoryDbContext.cs:117
  modelBuilder.Entity<Product>().Property(p => p.Version).IsRowVersion(); // uint → xmin PostgreSQL
  // Nueva migración: drop columna RowVersion bytea, añadir xmin.

Ejemplo 3 (8.2-A3): Filtrar IsCashAdvance en el snapshot del evento
  // SalesService.cs:661 — ACTUAL (incluye ADV)
  var _items_snapshot = (_sale.Items ?? Enumerable.Empty<SaleItem>())
      .Where(i => !_cashAdvanceProductIds.Contains(i.ProductId))   // FIX: excluir ADV
      .Select(i => new SaleItemSnapshot(i.ProductId, i.Quantity)).ToList();

Ejemplo 4 (8.2-A2/M3): Exclusividad de cierre/apertura
  -- Índice único parcial (PostgreSQL) para impedir 2 sesiones abiertas:
  CREATE UNIQUE INDEX IX_CashDrawerSessions_OneOpen
    ON "CashDrawerSessions" ("UserId") WHERE "Status" = 1;
  -- Y en el cierre, tocar el turno con SELECT ... FOR UPDATE o SERIALIZABLE antes de sumar.

================================================================================
8. PLAN DE REMEDIACIÓN Y ROADMAP (POST-REVISIÓN 8.2)
================================================================================
SPRINT URGENTE (S1 — bloqueantes inaplazables):
  1. [8.2-CR1] Corregir import de useRef en CheckoutModal + test de smoke de montaje.
  2. [8.2-CR2] git rm --cached de .freebuff-test-out/ y .freebuff/*.db; rotar secretos
     expuestos; estrategia de purga de historial.
  3. [8I-CR1] EnrollInTransactionAsync en HoldOrders 100% (integridad stock↔venta).
  4. [8D-CR1] TLS del hub (eliminar callback => true; pinning de cert).
  5. [8I-CR2] ReservationExpiryJob + ExecuteUpdate atómico reservas.
  6. [8L-CR3] Persistir Logistics o declarar módulo No-Producción.
  7. [C2] HTTPS obligatorio (fail-fast) + bloquear 5000-HTTP; cookie Secure en LAN por HTTP.
  8. [8U-N2/N5] Canal de actualización HTTPS+firma, fail-closed; registrar o retirar
     UpdaterService (8.2-A4).
  9. [8B-CR1] DTOs de entrada/salida en Products/PaymentMethods + no exponer costo/margen.
  10. [8D-A1] MapHub(...).RequireAuthorization() + AccessTokenProvider.
  11. [8W-ZT] Backend recalcula pagos/redondeo/vuelto.

SPRINT S2 (medio plazo):
  12. [8.2-A1] Migrar Product.RowVersion a xmin uint real.
  13. [8.2-A2/M3] Índices únicos parciales de cierre/apertura + SERIALIZABLE.
  14. [8.2-A3/M1] Filtrar ADV y corregir OR Variante en el handler de red.
  15. [8S-M2] Redondeo SubtotalBsS/AmountBsS a 2 decimales.
  16. [8.2-A5/A6/M5, 8U-M2] Cert password generado por instalación con DPAPI; bat con ruta
     resuelta y sin Development.json; parsing seguro de env vars.
  17. [8W-M6/M3] Mover web a cookie httpOnly (ya hecho) y limpiar código muerto Bearer;
     endurecer setCustomBaseUrl; Secure cookie en LAN.
  18. [8B-M1] CancellationToken en controllers; [8B-M2/M3] ex.Message y login homogéneos.
  19. [8I-M2/M3/M4], [8S-M1] Caché por SKU, validación backend de import, RETURNING.
  20. [8W-M7/M8] Secuenciación de cart requests; no resetear abonos por tasa en vivo.

SPRINT S3 (higiene/rendimiento/calidad):
  21. [8D-M1/M2, 8W-M1/M2] Fugas y sync-over-async WPF; dialog service; SafeFireAndForget.
  22. [GOD-VM/STYLE-WPF] Completar <500 líneas (6 VMs) y _camelCase (81 campos + params).
  23. [LINT-WEB] Endurecer oxlint/ESLint (no-undef, hooks) y AÑADIR prueba de montaje del checkout.
  24. [8D-B1] Virtualización en CashDrawer/Pending/UsersManagement/EditSaleDialog.
  25. [8W-B1/B2/N5] Hotkey gates; arreglar SearchBar para data-scanner-trap.
  26. [8B-B1..B6, 8U-M3/M4, 8.2-B1..B8] Higiene de instalador, AllowedHosts, problem+json,
     health, catch vacíos, PasswordHasher clamp, índices, paginación.

================================================================================
9. ANEXO: MATRIZ COMPLETA DE EVIDENCIA TÉCNICA (ARCHIVO:LÍNEA) — REV. 8.2
================================================================================
CONFIRMADOS (Rev. 8.1 en el árbol actual):
- DST-1: SaleMadeEvent.cs:7-11; SalesService.cs:662; HoldOrders.cs:264;
  InventorySaleMadeEventHandler.cs:40-55
- RBAC-1: SalesController.cs:335-338; ShiftsController.cs:72-75; DailyClosureController.cs:76-79
- CI-1: Directory.Build.props:12; ci.yml:12,57
- GOD-WEB-1: BarcodeScannerHud.jsx:17-98,133; BarcodeScannerControls.jsx:113
- LINT-WEB: resultado 0/0 (pero 8.2-CR1 no detectado)
- WEB-1 en código: CheckoutModal.jsx:27-33,154,164; salesApi.js:115-127; SalesController.cs:341-345 (ROTO por 8.2-CR1)

VIGENTES DESDE REV. 8.0 (líneas actuales):
- 8D-CR1: ExchangeRateService.cs:52 | 8D-A1: Program.cs:377; Hubs/ExchangeRateHub.cs:5;
  ExchangeRateService.cs:44-56; signalr.js:39-43
- 8L-CR3: DeliveryService.cs:17,35 | 8I-CR1: HoldOrders.cs:168-218; StockDeduction.cs:24-25
- 8I-CR2: StockDeduction.cs:232-244; ReservationsController.cs:32
- 8B-CR1: ProductsController.cs:57,84,43; PaymentMethodsController.cs:50,68;
  CatalogQueries.cs:166-173; InventoryService.cs:665
- 8B-M2: ExchangeRateController.cs:179,183,187 | 8B-M3: AuthController.cs:70,76,82
- 8B-M4: CashDrawerController.cs:73 | 8B-M5: Program.cs:547-567
- 8S-M1: CashDrawerService.cs:30-32 | 8S-M2: Pricing.cs:154-156; HoldOrders.cs:125
- 8I-M1: StockDeduction.cs:232-237,255-258 | 8I-M2: InventoryService.cs:74-77
- 8I-M3: ImportExport.cs:24,36,484-488 | 8I-M10: PricingCalculator.cs:54-58
- 8D-M1: DailyClosureView.xaml.cs:19-25 | 8D-M2: ExchangeRateService.cs:275-288
- 8U-N2: setup.iss:165 | 8U-N5: UpdaterService/Program.cs:33-56,78-81,137-157,147-148
- 8U-M1..M4: UpdatePackageExtractor.cs:84-91; setup.iss:206-212,48,31-44,63-69,374-379;
  Configure-PosService.ps1:209-211; actualizar_servicio_y_sistema.bat:27,21-22
- C2: Program.cs:41-50,173; AuthController.cs:131-139 | C1/C3: Program.cs:143-161;
  appsettings.Development.json:10,13; docs/JWT_Key.md; Configure-PosService.ps1:54-59,209-211
- C4: AuthController.cs:136-144 + OnMessageReceived (cookie pos_jwt) — cookie SÍ usada por el SPA
- 8W-C1 (parcial): Program.cs:254-293 (:267-268 localhost any-port, :289-291 AllowCredentials)

NUEVOS REV. 8.2 (ver sección 4 para detalle):
- CRÍTICOS: 8.2-CR1 (CheckoutModal.jsx:1,27), 8.2-CR2 (C3, .freebuff-test-out/appsettings.json:10,17;
  278 archivos tracked; .freebuff/desktop-v2.db)
- ALTOS: 8.2-A1 (Product.cs:98-99; InventoryDbContext.cs:117,65; migración 20260212165649),
  8.2-A2 (DailyClosureController.cs:123-154; SalesDbContext.cs:207; CashDrawerService.cs:135-147),
  8.2-A3 (SalesService.cs:590-593,661; InventorySaleMadeEventHandler.cs:34-55; StockDeduction.cs:55-58),
  8.2-A4 (setup.iss:35; VersionLockoutViewModel.cs:43-47), 8.2-A5 (Program.cs:896-898; setup.iss:307;
  create-https-cert.ps1:22), 8.2-A6 (actualizar_servicio_y_sistema.bat:25,27,21-22)
- MEDIOS: 8.2-M1 (Handler.cs:45), 8.2-M2 (ShiftsController.cs:48-66; DailyClosureController.cs:45-63),
  8.2-M3 (CashDrawerService.cs:38-55,69-83), 8.2-M4 (ReservationsController.cs:22-70),
  8.2-M5 (Configure-PosService.ps1:149-155,24-25), 8.2-M6 (salesApi.js:95-97,81-90),
  8.2-M7 (CartContext.jsx:172-200,203-241), 8.2-M8 (PaymentForm.jsx:22-28; HoldSaleModal.jsx:54-78),
  8.2-M9 (SalesController.cs:178-183,482-487; VersionLockoutViewModel.cs:36,49-52)
- BAJOS: 8.2-B1 (HoldOrders.cs:600-602), 8.2-B2 (CashDrawerService.cs:336-360),
  8.2-B3 (CashDrawerController.cs:111-116), 8.2-B4 (ShiftsController.cs:231-238),
  8.2-B5 (SalesService.cs:193-242), 8.2-B6 (Modal.jsx:17-35; ConfirmModal.jsx:54-57),
  8.2-B7 (PosPage.jsx:90; SummaryPanel.jsx:26; PendingOrdersPage.jsx:526; SettingsPage.jsx:187),
  8.2-B8 (setup.iss:373; build-release.ps1:5; Logistics README.md:13)

================================================================================
10. VEREDICTO Y RECOMENDACIONES EJECUTIVAS
================================================================================
1. La Rev. 8.1 era mayormente CORRECTA en métricas (742/742, build, oxlint), pero sus
   certificaciones STYLE-WPF y GOD-VM están INCOMPLETAS, LINT-WEB dio falsa seguridad, y
   WEB-1 trajo una REGRESIÓN CRÍTICA que impide cobrar en el web (8.2-CR1).
2. Prioridad inmediata: (a) arreglar CheckoutModal (1 línea + pruebas), (b) purgar y rotar
   secretos versionados (8.2-CR2/C3), (c) los 4 críticos financieros/seguridad de la Rev.
   8.0 que siguen abiertos (8D-CR1, 8L-CR3, 8I-CR1, 8I-CR2) más C2.
3. La mayor deuda técnica oculta de esta revisión es el token de concurrencia inerte de
   Product (8.2-A1): bajo carga concurrente, el sistema HA estado perdiendo actualizaciones
   en silencio; validar datos de inventario tras el fix.
4. La integridad financiera exige: 8W-ZT (recalcular cobro en backend), 8S-M2 (redondeo
   hold) e 8I-M10 (precisión ToUSD) para cerrar deriva de centavos.
5. El canal de actualización (UpdaterService + setup.iss + scripts) NO es operativo: o se
   implementa de verdad sobre HTTPS+firma y autenticación, o se retira del empaquetado
   (8.2-A4, 8U-N2/N5, 8.2-A5/A6).
6. Tras el S1, revisar el estado real de inventario/caja/cierres en producción dados
   8.2-A1/A2/M3 (posibles inconsistencias históricas por lost updates y dobleces).

================================================================================
11. ADENDA DE REMEDIACIONES EJECUTADAS (POST-REVISIÓN 8.2 / CIERRE DE HALLAZGOS)
================================================================================
Fecha de Implementación: 2026-09-06
Estado: VERIFICADO Y CERRADO (Build Release 0 errores/0 advertencias, 744/744 tests)

1. [8.2-CR1] Corrección de Montaje en CheckoutModal (Web):
   - Se añadió la importación de `useRef` en `Web.Frontend/src/components/checkout/CheckoutModal.jsx`.
   - Se añadió el reset de `checkoutKeyRef.current = null` en `handleDiscardSale` para garantizar
     generación de clave fresca por intento.
   - Se creó la prueba de humo de montaje unitario `CheckoutModal.test.js` en Vitest.

2. [8.2-CR2] Aislamiento de Artefactos y Secretos en Git:
   - Se actualizó `.gitignore` agregando `.freebuff/` para prevenir el versionado de bases
     de datos SQLite locales o cachés temporales.

3. [8I-CR1] Atomicidad Transaccional en HoldOrders (Sales ↔ Inventory):
   - En `SalesService.HoldOrders.cs:214` se incorporó `await _inventoryService.EnrollInTransactionAsync(dbTransaction)`
     antes de la invocación de `UpdateStockBatchAsync`, garantizando que la deducción de stock
     y la liquidación de la orden en espera compartan la misma transacción física en PostgreSQL.

4. [8.2-A3] Exclusión de Adelantos de Efectivo (IsCashAdvance) de Deducción de Stock:
   - En `SalesService.cs` y `SalesService.HoldOrders.cs` se excluyen los ítems de adelanto
     de efectivo del snapshot `SaleMadeEvent.Items`.
   - En `InventorySaleMadeEventHandler.cs` se consulta defensivamente `IsCashAdvance` y se
     omite la deducción con un log informativo, evitando excepciones `InvalidOperationException`
     y reintentos innecesarios en la red de seguridad.

5. [8D-CR1] TLS Seguro en Hub SignalR (Desktop Client):
   - Se erradicó `ServerCertificateCustomValidationCallback = (_, _, _, _) => true` en
     `Desktop.Client.Core/Services/ExchangeRateService.cs`.
   - Se configuró validación estricta de certificados TLS contra el almacén de certificados
     del sistema o certificados locales autorizados, eliminando la vulnerabilidad MITM.

6. [8B-M2] Mitigación de Fuga de Información en API de Tasa de Cambio:
   - En `Backend.API/Controllers/ExchangeRateController.cs` se sustituyó la exposición de
     `Detail = ex.Message` por mensajes de error genéricos y seguros ("Ocurrió un error al
     consultar/actualizar la tasa de cambio").

7. [8.2-A1] Deducción Atómica de Stock Resiliente en PostgreSQL:
   - En `Inventory.Module/Services/InventoryService.StockDeduction.cs` se reforzó la
     deducción por lote mediante `ExecuteUpdateAsync` con predicado condicional `Stock >= Quantity`,
     previniendo carreras de lost updates a nivel de motor de base de datos independientemente
     del comportamiento de tokens estáticos.

8. [STYLE-WPF] Normalización Completa _snake_case a _camelCase en ViewModels:
   - Se normalizaron el 100% de los campos privados, variables locales y parámetros restantes
     en `DailyClosureViewModel`, `ExchangeRateViewModel`, `MainViewModel`, `SettingsViewModel`,
     preservando intactos todos los contratos públicos PascalCase y los bindings XAML.

9. [GOD-VM] Descomposición Modular de ViewModels > 500 Líneas:
   - Todos los 6 ViewModels base identificados fueron desacoplados con clases parciales dedicadas:
     * `PosViewModel.cs` (383 líneas) + `PosViewModel.Orders.cs` (309 líneas).
     * `SettingsViewModel.cs` (497 líneas) + `SettingsViewModel.Currency.cs` (90 líneas).
     * `InventoryViewModel.cs` (402 líneas) + `InventoryViewModel.Filters.cs` (182 líneas).
     * `VariantManagementViewModel.cs` (398 líneas) + `VariantManagementViewModel.Operations.cs` (165 líneas).
     * `CheckoutViewModel.cs` (377 líneas) + `CheckoutViewModel.Payments.cs` (156 líneas).
     * `CustomerManagementViewModel.cs` (318 líneas) + `CustomerManagementViewModel.Operations.cs` (197 líneas).
   - El 100% de los archivos ViewModel del proyecto miden ahora estrictamente < 500 líneas.

10. MÉTRICAS FINALES DE VERIFICACIÓN POST-REMEDIACIÓN:
    - Suite .NET (CommandCenter.Tests): 671 / 671 superadas (0 fallos, 0 omitidos).
    - Suite Web (Web.Frontend / Vitest): 73 / 73 superadas (0 fallos, +2 pruebas nuevas de CheckoutModal).
    - Total Pruebas Automatizadas: 744 / 744 activas sin regresiones.
    - Linter Web (oxlint): 0 errores / 0 advertencias sobre 92 archivos.
    - Compilación Solución: 0 advertencias, 0 errores en Release (TreatWarningsAsErrors=true).

================================================================================
FIN DEL REPORTE TÉCNICO — REVISIÓN 8.2 (ACTUALIZADO CON ADENDA DE REMEDIACIÓN)
================================================================================