================================================================================
REPORTE DE AUDITORÍA TÉCNICA INTEGRAL — SISTEMA POS "CommandCenter"
================================================================================
Fecha de Emisión: 2026-09-06
Revisión: 8.6 (Re-Auditoría Exhaustiva del Sistema + Contrastación Empírica Rev. 8.5)
Entorno de Ejecución: Windows 10/11 / Windows Server — .NET 10.0.101 / PostgreSQL 16

Métricas de Prueba RECALCULADAS EN ESTA AUDITORÍA (ejecución real):
  - Suite .NET (CommandCenter.Tests): 685 / 685 superadas (0 fallos, 0 omitidos) ≈ 9-10 s
  - Suite Web (Web.Frontend / node:test): 75 / 75 superadas (0 fallos)
  - Total Pruebas Automatizadas: 760 / 760 activas sin regresiones
  - Linter Web (oxlint): 0 errores / 0 advertencias (93 archivos)
  - Compilación Release: 0 advertencias / 0 errores (TreatWarningsAsErrors=true)

================================================================================
0. NOTA METODOLÓGICA DE ESTA REVISIÓN (8.6)
================================================================================
La Rev. 8.5 detectó 21 hallazgos nuevos (6 altos, 6 medios, 5 Web, 4 WPF) y dejó 13
afirmaciones vigentes de la Rev. 8.4. Esta revisión:
  (a) CONTRASTA línea a línea cada afirmación de la Rev. 8.5 contra el árbol actual,
      clasificándola como RESUELTA, PARCIAL o VIGENTE (con la línea actual en cada caso);
  (b) incorpora la corrección inmediata del hallazgo 8.5-A6 (DI anti-patrón del
      CashDrawerService) aplicada al inicio de esta auditoría;
  (c) añade una nueva re-lectura profunda de BACKEND, DESKTOP (WPF) y WEB (React) con la
      infraestructura (tests, instalador, scripts, git) para hallazgos NUEVOS.

Resultado neto: 10 afirmaciones de la Rev. 8.5 quedan RESUELTAS, 9 permanecen PARCIALES,
12 permanecen VIGENTES/CONFIRMADAS, y se descubren 24 hallazgos NUEVOS (1 CRÍTICO,
7 ALTOS, 12 MEDIOS, 4 BAJOS) distribuidos en backend, frontend e infraestructura. La
declaración "100% remediado" sigue sin sostenerse.

================================================================================
1. HALLAZGOS DE LA REV. 8.5 RESUELTOS (contrastación línea a línea)
================================================================================
  [8.5-A1] ALTO → RESUELTO. TOCTOU de caja (double-spend): CashDrawerService.cs:255-270
    ahora envuelve check de saldo (L.265-269) + INSERT del egreso (L.287) en la MISMA
    transacción con advisory lock (L.262, lock), commit L.290-293. Verificado contra el
    test real PostgreSQL ConcurrentPhysicalExpenses_CannotOverdrawBalance.
  [8.5-A2] ALTO → RESUELTO. Cierre no atómico: CashDrawerService.cs:156-184 serializa el
    cierre con lock + re-lectura (Status==Open L.169) antes de flip (L.171-174) e INSERT
    del egreso Closing (L.176-184). Doble cierre ya no genera filas duplicadas.
  [8.5-A4] ALTO → RESUELTO. Forja de Source=Closing/Opening en egreso manual:
    CashDrawerController.cs:127-130 rechaza los orígenes reservados en el endpoint manual.
  [8.5-A6] ALTO → RESUELTO. DI anti-patrón (new SalesService(_context, null!, this, null!)):
    CashDrawerService.cs:16,21,27-30 resuelve ISalesService perezosamente desde
    IServiceProvider. Se eliminó el parámetro inyectado del constructor y el fallback null!;
    Program.cs:119-120 registra el servicio. (Corregido al inicio de esta auditoría; ver §4.)
  [8.5-M2] MEDIO → RESUELTO. StartSaleAsync re-leía vía GetSaleAsync (round-trip con side
    effects): SalesService.cs:91-105 retorna el DTO construido localmente sin re-consultar.
  [8.5-M5] MEDIO → RESUELTO. Jobs frágiles: OutboxProcessorJob.cs:58,72 try/catch por tick
    y externo; BcvExchangeRateJob.cs:110 catch-all en SyncRateAsync (loop solo responde a
    OperationCanceledException, L.46); ReservationExpiryJob e IdempotencyCleanupJob con
    try/catch logueado por ciclo. InventorySaleMadeEventHandler.cs:78-91 REINTENTA 3x500ms
    (ya no abandona tras el primer fallo).
  [8.4-W-2/8.5-W2] ALTO → RESUELTO. LoginViewModel lambda sin desuscribir:
    LoginViewModel.cs:96-100 guarda el handler en campo; Dispose() L.106-114 hace
    -= (-= L.111); LoginView.xaml.cs:24 invoca oldVm.Dispose() en DataContextChanged.
  [8.5-W3] MEDIO → RESUELTO. Doble fuente de verdad de tasa: la clase CurrencyService con
    URL hardcoded ya NO EXISTE en el árbol (grep = 0). ExchangeRateService.cs:27 es la
    fuente única; DailyClosureViewModel.cs:164 Y :200 usan DateTime.UtcNow (consistente).
  [8.5-W4] MEDIO → RESUELTO. SettingsViewModel disparaba 3 SafeFireAndForget que re-hacían
    EnsureLoadedAsync: constructor limpio (L.87-89 comentario), EnsureLoadedAsync idempotente
    con guard _hasLoaded (L.91/95, L.93-103); única llamada desde MainViewModel.cs:238.
  [8.5-WEB1] MEDIO → RESUELTO. CheckoutModal conecta pagos sin validar preview: preview
    exclusivo del backend (L.27-28,106-152), roundingAdjustment SOLO del preview (L.157-159),
    canFinalize exige !previewFailed (L.169) y guard en handleFinalizeSale (L.207).
  [8.5-WEB2] MEDIO → RESUELTO. Ráfaga de syncs de tasa + sessionStorage en cada cambio:
    CartContext.jsx:48-61 debounce 400ms a sessionStorage y :139-153 sync al backend con
    debounce 1500ms trailing (1 escritura por ráfaga).
  [8.5-WEB3] MEDIO → RESUELTO. Logout catch vacío y payment-methods sin retry:
    AuthContext.jsx:70-91 logout con AbortSignal.timeout(5000) + revocación en servidor y
    limpieza local como fallback visible; CheckoutModal.jsx:78-95 reintenta 1 vez (800ms) la
    carga de métodos de pago y muestra error visible si persiste.
  [8.4-N5/8.5-WEB4] MEDIO → PARCIAL. Perfil con PII en localStorage: whithelist estricta en
    AuthContext.jsx:9-17 (sin tokens ni banderas internas), api.js:210-225 limpia perfil ante
    401. RESIDUAL: cedula/nombre siguen persistidos en localStorage['pos_user_profile']
    (PII con permanencia indefinida) — ver hallazgo NUEVO [8.6-W2].

================================================================================
2. HALLAZGOS DE LA REV. 8.5 PARCIALES (contrastados en el árbol actual)
================================================================================
  [8B-M3] 🔴 PARCIAL — AuthController.cs:218-226 sigue devolviendo Status423Locked y el
    string del Message expone "remainingMinutes" (anti-enumeración de cuenta NO resuelta;
    login sí es 401 genérico). VERIFICADO.
  [8.5-A3] 🔴 PARCIAL — Autorización a nivel de objeto. CompleteSale (:513-516),
    ConfirmPickup (~709) y AddPayment SÍ llaman IsAuthorizedForSaleAsync (A2). PERO
    PERMANECEN SIN CHECK: GetSale (~81-93), GetCheckoutPreview (:435-497 — expone totales/
    desglose de venta ajena sin ownership), UpdateSaleCustomer (~343-363), UpdatePriceList
    (~770-790 sin rol concreto ni scoping). CashDrawerController.GetActiveSession/GetHistory
    sin roles (Driver lee datos financieros). ShiftsController.GetReportById (:248-290)
    permite ver cierre ajeno por ID. ReservationsController confirm/cancel sin ownership
    (:81-111). VERIFICADO.
  [8.5-A5] 🔴 PARCIAL — Tasa de cambio: la ruta de cobro principal (CompleteSale)
    SalesService.cs:423-449 ahora RECHAZA desvíos ≥ ±100% (L.438-442) y loguea warning en
    ±10% (L.444-447); se audita la rate del pago. PERO: Hold (HoldOrders.cs:99-103,394-397),
    CashAdvance y la transacción manual siguen aceptando cualquier tasa >0 sin anclaje al
    ExchangeRateHistory del día — el "shortage enmascarado" persiste en esos flujos.
    ADEMÁS el catch en L.432 puede tragarse el chequeo (catch-swallow).
  [8.5-A5 / Hold rate] 🔴 ALTO PARCIAL — HoldOrders.cs:99-103 acepta request.ExchangeRate >0
    del cliente sin anclar a la tasa BCV del día ni validar desvío → un abono en espera
    puede registrar Bs.S desviados. VERIFICADO (incluido en §3 como [8.6-B3]).
  [8.5-M1] 🟡 PARCIAL MEJORADO — GetCurrentBalanceLocalAsync pasó de 3 a 2 queries
    (CashDrawerService.cs:316-336: sesión L.316-320 + SUM agregado L.327-334), pero aún no
    es 1 sola query agregada; GetActiveSessionAsync con Include(Transactions).ThenInclude(Sale).
  [8.5-M3] 🟡 PARCIAL — DeleteProductAsync (InventoryService.cs:579-599) ya NO captura
    Exception genérico: solo DbUpdateException (L.587) → soft-delete con log de degradación
    (L.592-598); caveat: sigue enmascarando errores Npgsql no-DbUpdateException. Persisten
    catch vacíos: Backend.API 5 (Program.cs:81,903 deliberados; SecurityStampValidator
    :60/:82/:94 micro), Core 7 (logging/versión), Sales.Module ~6, Inventory.Module ~2.
  [8.5-M4] 🟡 PARCIAL MEJORADO — Inconsistencia de formato de error: el payload canónico del
    GlobalExceptionHandlerMiddleware.cs (255-287) ya NO duplica Message/TraceId capitalizados
    (serializa SOLO camelCase {type,title,status,error,message,detail,instance,traceId});
    RESIDUAL: en casi todas las ramas message == detail (mismo string, L.189-190,204-205,
    221-222,235-236) y persisten respuestas no uniformes en controllers (NotFound() vacío vs
    NotFound(new{message}) vs UpdatePriceList con Message capitalizado).
  [8.5-W1] 🟡 PARCIAL — PosViewModel.Orders.cs:40-57: AddSelectedSuggestionAsync YA respeta
    _scannerLock (L.48 WaitAsync, comentario fix L.46-47). RESIDUAL: OnSelectedSuggestionChanged
    (void) sigue SafeFireAndForget (L.40) y dos selecciones rápidas de la MISMA sugerencia
    encolan dos Tasks que pasan el semáforo secuencialmente → doble-add; AddSelectedSuggestion
    CoreAsync (L.59+) no tiene guard de cooldown/IsProcessing.
  [8.5-WEB5] 🟡 PARCIAL — PaymentList key estable (PaymentList.jsx:17-19 key=p.uid) y App.jsx
    valida el retorno resetCart en hold (App.jsx:70-88). RESIDUAL: CheckoutModal.jsx:247-249
    tras completeSale hace await resetCart() e ignora el booleano → si createNewSale falla,
    la UI anuncia éxito con currentSale obsoleto. VERIFICADO (nuevo en §3 como [8.6-W1]).
  [8.5-M6 → RESUELTO con residuo] 🔴 MITIGADO — VersionLockoutViewModel.cs:41-54 valida el
    esquema http/https de la URL (Uri.TryCreate) antes de Process.Start (L.58, UseShellExecute
    L.61); origen VersionCheckService.cs:60-63. RESIDUAL: abrir navegador con URL del servidor
    automáticamente sigue siendo vector si el servidor de updates es comprometido; host no
    validado (acepta IPs privadas maliciosas).
  [8.2-A1/xmin] 🟡 PARCIAL — token xmin uint de Product en InventoryDbContext.cs:117-128,
    pero el [Timestamp] bytea RowVersion ORIGINAL COEXISTE (Product.cs:98-99): dos tokens de
    concurrencia sobre la misma entidad (inofensivo pero confuso). Resto del inventario sin xmin.

================================================================================
3. NUEVOS HALLAZGOS — REV. 8.6 (backend, frontend, infraestructura)
================================================================================
A) CRÍTICOS NUEVOS (1)
--------------------------------------------------------------------------------
[8.6-C1] 🔴 CRÍTICO — Vuelto (change) insertado como egreso SIN control de saldo.
  - SalesService.cs:549-578: al liquidar una venta con sobrepago, _change_tx (CashTransaction
    Expense, Source=SalePayment, IsPhysicalCash=true) se añade directo a _context (L.575) con
    SOLO un límite de magnitud (L.553: changeUsd>100m Y >sale.TotalUSD). NO consulta el saldo
    de la caja, NO usa advisory lock ni transacción de caja (a diferencia de 8.5-A1). Un pago
    en efectivo excesivo puede llevar la caja a saldo NEGATIVO sin bloqueo.
  - Además L.560 toma la tasa CLIENTE (changeBsS = changeUsd * exchange_rate) sin anclaje;
    y L.558 cashMethodId es null si el pago cash se borró de la colección (FK nullable).
  - Fix: centralizar el registro de vuelto en CashDrawerService con lock+check de saldo
    (misma transacción que 8.5-A1) y anclar la tasa del vuelto al día.

B) ALTOS NUEVOS (7)
--------------------------------------------------------------------------------
[8.6-B1] ALTO — GetCheckoutPreview sin autorización a nivel de objeto.
  - SalesController.cs:435-497: expone totales, desglose e incluso la venta de OTRO cajero
    por ID, pese a que el resto del flujo de cobro sí aplica IsAuthorizedForSaleAsync.
  - Fix: aplicar el mismo chequeo de ownership de A2/8.5-A3 a GetCheckoutPreview y
    UpdateSaleCustomer/UpdatePriceList.

[8.6-B2] ALTO — AddItemAsync acepta exchange_rate <= 0 sin validar al añadir ítem.
  - SalesService.cs:206: _sale.AppliedRate = exchange_rate sin validación > 0 en AddItem;
    solo existe compensación defensiva al COMPLETAR (L.418-421). Un cliente puede fijar tasa 0
    o negativa y persistir montos Bs.S en 0 durante la edición del carrito.
  - Fix: validar exchange_rate > 0 al inicio de AddItemAsync (como ya se hace en el cobro).

[8.6-B3] ALTO — Tasa del HOLD aceptada del cliente sin anclaje BCV.
  - HoldOrders.cs:99-103 (HoldSale) y :394-397 (UpdateHoldSale): request.ExchangeRate>0 se
    asigna a _sale.AppliedRate sin validar desvío ni consultar ExchangeRateHistory del día.
    Completa la evasión de 8.5-A5 para el flujo de esperas (abono parcial desviado en Bs.S).
  - Fix: anclar a la tasa BCV del día con tolerancia configurable y audit log.

[8.6-B4] ALTO — Test de autorización a nivel de objeto (ownership) no existe.
  - RbacPermissionTests verifica SOLO roles (Admin vs Cashier en catálogo); 8.5-A3/8.6-B1
    (venta/reserva/reporte de otro cajero) carecen de cobertura automatizada. CheckoutModal
    sigue sin montaje REAL (CheckoutModal.test.js lee el fuente con fs.readFileSync — 8.4-N5).
  - Fix: tests de ownership por recurso y montaje jsdom real del modal.

[8.6-B5] ALTO — Sync-over-async bloqueante en apagado WPF + Dispose de ExchangeRateService.
  - App.xaml.cs:379 stopTask.Wait(TimeSpan.FromSeconds(3)) en OnExit y
    ExchangeRateService.cs:291 DisposeAsync().AsTask().Wait(cts.Token) (timeout 1s, L.290).
    Ambos bloquean el hilo UI en shutdown; el segundo traga excepciones (L.293-294).
  - Fix: método async de stop ("ExitAsync") + DisposeAsync en el host; documentar tolerancia.

[8.6-B6] ALTO — Sin WebApplicationFactory/TestServer en toda la suite .NET.
  - grep en CommandCenter.Tests → 0 coincidencias de WebApplicationFactory/TestServer. Los
    "integration tests" instancian controllers/servicios directos (DefaultHttpContext en
    Phase7ClosureWithoutRateTests.cs:49-55) → no se cubre el pipeline HTTP real (auth, model
    binding, filtros, idempotencia, CORS). Los 3 tests con PostgreSQL real (PostgresReal*)
    están condicionados a env var y hacen `if(connStr==null) return;` (silent-pass, L.20-24/
    31-34) y comparten la MISMA BD en colecciones paralelas (riesgo de flakiness).
  - Fix: WebApplicationFactory por escenario + Assert.Skip (o retorno visible) en postgres.

[8.6-B7] ALTO — Fallback silencioso a localhost en el cliente WPF.
  - ExchangeRateService.cs:43: httpClient.BaseAddress ?? new Uri("http://localhost:5000/") —
    si DI no configura BaseAddress, conecta a localhost en silencio (logs ruidosos, y
    WpfDialogService.cs:462 Y :490 crean new ExchangeRateService(new HttpClient()) fuera de
    DI sin Dispose → conexión SignalR huérfana). App.xaml.cs:127-129 lo enmascara en la
    práctica, pero el fallback queda sin diagnóstico.
  - Fix: InvalidOperationException si BaseAddress es null (fail-fast) y eliminar los fallbacks
    de WpfDialogService.

C) MEDIOS NUEVOS (12)
--------------------------------------------------------------------------------
[8.6-M1] F5 (syncBcvRate) ROTO: PosPage.jsx:29,81 desestructura syncBcvRate de
  useExchangeRate(), pero ExchangeRateContext.jsx:62 expone solo {exchangeRate,setExchangeRate,
  lastUpdated,setLastUpdated,isRateOutdated,loading} → TypeError capturado en silencio; el
  atajo de sincronización de tasa NUNCA funciona. Fix: exportar syncBcvRate o eliminar F5.
[8.6-M2] Doble dispatch de eventos SignalR: signalr.js:51-63 despacha el callback Y
  window.dispatchEvent; ExchangeRateContext.jsx:41-50 re-despacha los mismos window events →
  cada evento del servidor genera 2 notificaciones y dobles fetches (PendingOrdersPage/
  SettingsPage). Fix: un solo canal (contexto o evento global).
[8.6-M3] PendingOrdersPage re-fetch por cambio de tasa: PendingOrdersPage.jsx:52 incluye
  exchangeRate en deps del useEffect → getPendingSales() en cada actualización (ráfagas
  SignalR) aunque el listado no dependa de la tasa. Fix: quitar exchangeRate de deps.
[8.6-M4] Re-registro incompleto de handlers SignalR: signalr.js:13-35 la rama "ya conectado"
  hace off/on de OnHoldSalesUpdated/OnPaymentMethodsUpdated/OnCurrencyFormatUpdated pero NO de
  ReceiveRateUpdate (L.45-49) → callbacks viejos acumulan stale closures en re-conexiones.
[8.6-M5] Sin code splitting en el bundle web: main.jsx monta todo estático; vite.config.js sin
  manualChunks; chunk único ~996 kB (React+signalr+zxing+lucide). Fix: React.lazy por página +
  vendor chunks. (Warning >500 kB ya reportado en build.)
[8.6-M6] WpfDialogService crea ExchangeRateService fuera de DI: L.462 y L.490
  new ExchangeRateService(new HttpClient()) sin Dispose → servicio SignalR huérfano
  (ver 8.6-B7). Fix: inyección obligatoria.
[8.6-M7] Inconsistencia horaria en DailyClosureViewModel: :164 y :200 usan UTC (confirmación),
  pero :313 y :368 usan DateTime.Now (hora LOCAL) en comprobante/FileName exportado → el mismo
  cierre puede mostrar horas distintas. ExchangeRateViewModel.cs:16 copia stale de _currentRate
  (no suscrito a ExchangeRateChangedMessage).
[8.6-M8] Estructura de datos en listas: EditSaleModal.jsx:~219 y PendingPickupsPage.jsx:273-280,
  393-401 usan key={idx} en ítems editables/expandibles → estado desincronizado si reordenan
  tras recálculo. (EditableSaleModal ya usa key estable.)
[8.6-M9] Junk trackeado en git: "Nueva carpeta/" (INSTALLATION.md + README.md duplicados,
  trackeados) y "Proyecto_Pos_Estable/" como gitlink de submodule huérfano (modo 160000, sin
  .gitmodules, árbol vacío). skills/test.txt vacío (trackeado). Fix: git rm y limpiar.
[8.6-M10] Reglas de código desalineadas: skills/rules.md:8 exige snake_case para variables,
  Contradice .editorconfig (dotnet_naming_rule private_fields _camelCase) y al.codebase actual;
  rutas src/Infrastructure (rules.md:14-15) no existen en el repo; regla "No Code-Behind"
  incumplida (LoginView.xaml.cs modificado). rules.md duplicado en la raíz con hash distinto.
  Fix: reconciliar rules.md con .editorconfig y borrar duplicado.
[8.6-M11] Apagado de UIs WPF via host: los VMs transient de MainViewModel viven toda la app
  (inyectados 1 sola vez) → lifetime registrado no refleja el real (de facto singleton);
  riesgo futuro de fuga al navegar vistas. Fix: registrar como singleton con intención o
  liberar holdings al cambiar de vista.
[8.6-M12] CI no cubre la rama de trabajo: .github/workflows/ci.yml:3-7 dispara solo en
  main/develop; la rama V0.15 (de trabajo real) NO activa CI por push; develop no existe.
  Fix: añadir V0.15 a triggers (o merge a main como puerta).

D) BAJOS NUEVOS (4)
--------------------------------------------------------------------------------
[8.6-L1] useCameraHardwareLifecycle.js es código muerto (solo lo usa su propio test; la cámara
  real se gestiona en BarcodeScannerModal.jsx:434-444). Eliminar o integrar.
[8.6-L2] api.js:10-28 incluye 169.254.0.0/16 (link-local) en la whitelist de hosts LAN — amplía
  superficie sin necesidad para POS. Retirar si no se requiere.
[8.6-L3] package.json usa rangos ^ sin pin exacto (react ^19.2.7, vite ^8.1.1, @microsoft/
  signalr ^10.0.5, @zxing ^0.23.0) → build no reproducible entre entornos. Fijar exactos.
[8.6-L4] Scripts residuales: taskkill /F en start.bat:8-25, actualizar_servicio_y_sistema.bat:27
  y deshabilitar_servicio_windows.bat:24-25; paths absolutos frágiles en el fallback del
  servicio (L.34-35); binario único commiteado installer/nssm.exe (331 KB).

E) VERIFICACIONES NUEVAS POSITIVAS (lo que SÍ está bien)
--------------------------------------------------------------------------------
  - JWT: Program.cs:149-163 rechaza secret <32 chars y la clave filtrada/desarrollo en
    Producción (lanza InvalidOperationException); RequireHttpsMetadata=false solo en el
    bearer; cookie pos_jwt + access_token permitida ÚNICAMENTE para /hubs (L.192-196);
    ClockSkew.  Rate limiting FixedWindow activo (AuthRateLimit L.235-244, GeneralApiRateLimit
    L.246-253) aplicado global (L.383) + [EnableRateLimiting] en AuthController.
  - CORS (Program.cs:259-300): única policy con SetIsOriginAllowed que restringe a orígenes
    configurados + localhost/loopback + RFC-1918 con puertos whitelist {5000,5001,5173,80,443,
    4173}; AllowAnyMethod/Header + AllowCredentials acotado a LAN.
  - Ingreso de datos: MISMA transacción física para venta+pagos+vuelto(inventario) vía
    EnrollInTransactionAsync (SalesService.cs:384-392 y HoldOrders.cs:169-177); RecalculateTotal
    defensivo (L.580-601) rechaza total<=0 y AppliedRate<=0 al persistir.
  - Jobs: los 6 jobs tienen try/catch logueado por ciclo; handler de inventario reintenta 3x.
  - Idempotencia: header Idempotency-Key validado en /hold, /payments Y /complete.
  - Secretos: .gitignore:405-409 excluye .env/appsettings.Development.json/JWT_Key.md y
    git ls-files confirma que NO están trackeados en el índice (ver 8.6-B6-infra para el
    histórico). .env (raíz) contiene SOLO DB_PASSWORD=123456 (debía rotarse).
  - Redondeo: RoundToDigital a 2 decimales con AwayFromZero aplicado en SubtotalBsS/AmountBsS
    (Pricing.cs:155-157, HoldOrders.cs:125-126); vuelto/change con PricingCalculator.

================================================================================
4. ANÁLISIS DE ARQUITECTURA, FLUJO DE DATOS, RECURSOS Y RENDIMIENTO (REV. 8.6)
================================================================================
1. BACKEND / AISLAMIENTO: El doble DbContext (Program.cs:98-104) se mantiene por diseño con
   coordinación transaccional en las rutas de venta. LA RUTA DE CAJA quedó endurecida (8.5-A1/
   A2/A4) y el DI hack eliminado (8.5-A6 vía IServiceProvider perezoso). Persisten: ownership
   incompleto (8.5-A3 + 8.6-B1), anclaje de tasa parcial (8.5-A5 + 8.6-B3), vuelto sin control
   de saldo (8.6-C1), falta de paginación en GetPendingSales/GetPendingPickups
   (HoldOrders.cs:485-518, History.cs:38-72) mientras GetSalesHistory sí pagina (:115-116),
   y la proyección de DTOs duplicada (ProductDto 5+, QuickInfoDto 3).

2. INTEGRIDAD FINANCIERA: pricing/redondeo correctos en la ruta principal; gaps reales:
   vuelto sin saldo (8.6-C1), tasa del hold no anclada (8.6-B3), PaymentMethodId sin validar
   pre-persistencia (SalesService.cs:506), RoundingAdjustment sin [Range] (SalesController.cs:
   577/811 y SalesService.cs:454,610), AddItemAsync con tasa<=0 (8.6-B2).

3. RESILIENCIA/CONCURRENCIA: advisory lock + transacción en caja (bueno); Sin
   EnableRetryOnFailure en la cadena de conexión EF (Program.cs:98-104), lo que deja la
   resiliencia a conexión intermitente PostgreSQL SIN strategy (recomendado: retry 6x con jitter,
   mismo connectionString en ambos contextos). Jobs con try/catch (bueno). Outbox con transacción
   explícita (resuelto Rev 8.5).

4. GESTIÓN DE RECURSOS WPF: desuscripciones correctas (Login, Main, Settings, Pos, ProductDialog,
   BaseViewModel UnregisterAll) y WeakReferenceMessenger con UnregisterAll (PosViewModel.cs:382).
   PERSISTEN: sync-over-async en shutdown (8.6-B5), 52 MessageBox (8W-M1 §2), acoplamiento
   Desktop→Sales.Module (Desktop.Client.Core.csproj:12), fallback a localhost en ExchangeRate
   Service (8.6-B7), WpfDialogService fuera de DI (8.6-M6), 34 campos snake_case (STYLE-WPF).

5. DESPLIEGUE/INFRAESTRUCTURA: setup.iss:185 sigue con default http://localhost:5000/updates/
   (8U-N2) y hereda la URL a appsettings.Production.json (setup.iss:271). UpdaterService no se
   empaqueta (ARCHITECTURE.md:14). Secretos en el HISTÓRICO git (JWT key ddf95c83... y
   appsettings.Development.json trackeados en commits previos a la purga) → RECOMENDACIÓN:
   rotar las credenciales (DB 123456, Admin123!, JWT) aunque el índice esté limpio. CI sin
   cobertura de V0.15 (8.6-M12). Junk trackeado en git (8.6-M9).

================================================================================
5. PLAN DE REMEDIACIÓN Y ROADMAP (POST-REVISIÓN 8.6)
================================================================================
SPRINT URGENTE (S1 — bloqueantes):
  1. [8.6-C1] Vuelto con control de saldo: mover el registro del vuelto a una operación de
     CashDrawerService con advisory lock + check de saldo y tasa anclada al día.
  2. [8.5-A3 + 8.6-B1] Completar autorización a nivel de objeto: GetCheckoutPreview,
     UpdateSaleCustomer, UpdatePriceList, reportes por ID y reservas (ownership de cajero/
     reservante); tests de ownership (8.6-B4).
  3. [8.5-A5 + 8.6-B3] Anclar tasa BCV del día en COMPLETE y HOLD (tolerancia configurable +
     audit), retirar el catch-swallow de L.432, y validar PaymentMethodId pre-persistencia.
  4. [8.6-B2] Validar exchange_rate > 0 en AddItemAsync.
  5. [8B-M3] Unificar change-password en 401 genérico (retirar el 423).
SPRINT S2 (medio plazo):
  6. [8U-N2→SS1] Canal de actualización HTTPS/pinning o retiro del flujo.
  7. [8.6-B7/M6] Fail-fast en BaseAddress + eliminar fallbacks de WpfDialogService;
     angularidad de apagado WPF (8.6-B5) con ExitAsync/DisposeAsync.
  8. [8.6-M1..M5, W1] syncBcvRate real, canal único SignalR, dep fix PendingOrders,
     off/on de ReceiveRateUpdate, code splitting.
  9. [8.6-B6] WebApplicationFactory + ownership; quitar silent-pass y paralelismo en
     PostgresReal* tests.
  10. [8.6-B4-extra] Montaje jsdom real de CheckoutModal.
  11. [8.6-B6-infra] Rotar credenciales (JWT/DB/Admin) por el histórico git; borrar .env del
      working tree o mover a secrets.
SPRINT S3 (higiene/rendimiento/calidad):
  12. [§2 STYLE-WPF] Completar _camelCase (~34 campos) y [8W-M1] MessageBox restante (0).
  13. [8.5-M1] Query agregada en GetCurrentBalanceLocalAsync (2→1) + AsNoTracking en
      GetActiveSessionAsync.
  14. [Program.cs 922] Descomponer la composición raíz en extensiones por área.
  15. [8.6-M7..M12, L1..L4] Unificar UTC/local, key estable en listas, limpiar junk trackeado,
      reconciliar rules.md, corregir lifetime de VMs, CI en V0.15, código muerto y `^` fijos.
  16. EnableRetryOnFailure en ambos DbContexts (misma config) — resiliencia de conexión.

================================================================================
6. ANEXO: MATRIZ DE EVIDENCIA TÉCNICA (ARCHIVO:LÍNEA) — REV. 8.6
================================================================================
RESUELTAS 8.5 (verificadas en el árbol): 8.5-A1 (CashDrawerService.cs:255-293), 8.5-A2
  (CashDrawerService.cs:156-184), 8.5-A4 (CashDrawerController.cs:127-130), 8.5-A6
  (CashDrawerService.cs:16,21,27-30; Program.cs:119-120), 8.5-M2 (SalesService.cs:91-105),
  8.5-M5 (OutboxProcessorJob.cs:58,72; BcvExchangeRateJob.cs:46,110; ReservationExpiryJob;
  IdempotencyCleanupJob; InventorySaleMadeEventHandler.cs:78-91), 8.5-W2 (LoginViewModel.cs:
  96-100,111; LoginView.xaml.cs:24), 8.5-W3 (CurrencyService ELIMINADO; ExchangeRateService.cs:27;
  DailyClosureViewModel.cs:164,200), 8.5-W4 (SettingsViewModel.cs:87-95), 8.5-WEB1 (CheckoutModal
  .jsx:27-28,106-152,157-169,207), 8.5-WEB2 (CartContext.jsx:48-61,139-153), 8.5-WEB3
  (AuthContext.jsx:70-91; CheckoutModal.jsx:78-95), 8.5-WEB4-parcial (AuthContext.jsx:9-17;
  api.js:210-225).

PARCIALES 8.5 (verificadas): 8B-M3 (AuthController.cs:218-226), 8.5-A3 (GetSale ~81-93;
  GetCheckoutPreview 435-497; UpdateSaleCustomer 343-363; UpdatePriceList 770-790;
  CashDrawerController 35-65; ShiftsController 248-290; ReservationsController 81-111),
  8.5-A5-parcial (SalesService.cs:423-449; catch L.432; HoldOrders.cs:99-103,394-397),
  8.5-M1 (CashDrawerService.cs:316-336), 8.5-M3 (InventoryService.cs:579-599; catch vacíos),
  8.5-M4 (GlobalExceptionHandlerMiddleware.cs:255-287; message==detail 189-236),
  8.5-W1 (PosViewModel.Orders.cs:36-57; falta guard en CoreAsync L.59+), 8.5-WEB5-parcial
  (CheckoutModal.jsx:247-249 vs App.jsx:70-88), 8.5-M6-mitigado (VersionLockoutViewModel.cs:41-
  61; VersionCheckService.cs:60-63), xmin/RowVersion (InventoryDbContext.cs:117-128;
  Product.cs:98-99), GetSaleAsync write-on-read (SalesService.cs:108-132,119-121),
  GetPendingSalesAsync write-on-read (HoldOrders.cs:485-518,494), Startup verificado.

NUEVOS 8.6:
  CRÍTICO: 8.6-C1 (SalesService.cs:549-578).
  ALTOS: 8.6-B1 (SalesController.cs:435-497), 8.6-B2 (SalesService.cs:206), 8.6-B3
    (HoldOrders.cs:99-103,394-397), 8.6-B4 (RbacPermissionTests; CheckoutModal.test.js),
    8.6-B5 (App.xaml.cs:379; ExchangeRateService.cs:291), 8.6-B6 (PostgresReal*Tests.cs:20-24/
    31-34; 0 WebApplicationFactory), 8.6-B7 (ExchangeRateService.cs:43; WpfDialogService.cs:
    462,490).
  MEDIOS: 8.6-M1 (PosPage.jsx:29,81; ExchangeRateContext.jsx:62), 8.6-M2 (signalr.js:51-63;
    ExchangeRateContext.jsx:41-50), 8.6-M3 (PendingOrdersPage.jsx:52), 8.6-M4 (signalr.js:45-49),
    8.6-M5 (main.jsx; vite.config.js), 8.6-M6 (WpfDialogService.cs:462,490), 8.6-M7
    (DailyClosureViewModel.cs:313,368 vs :200; ExchangeRateViewModel.cs:16), 8.6-M8
    (EditSaleModal.jsx:~219; PendingPickupsPage.jsx:273-280,393-401), 8.6-M9 ("Nueva carpeta/";
    "Proyecto_Pos_Estable/"; skills/test.txt), 8.6-M10 (skills/rules.md:8,14-15; rules.md raíz;
    .editorconfig), 8.6-M11 (App.xaml.cs:199,228-229; MainViewModel.cs:123-125), 8.6-M12
    (ci.yml:3-7).
  BAJOS: 8.6-L1 (useCameraHardwareLifecycle.js), 8.6-L2 (api.js:10-28), 8.6-L3 (package.json
    ^), 8.6-L4 (start.bat:8-25; actualizar_servicio_y_sistema.bat:27,34-35;
    deshabilitar_servicio_windows.bat:24-25; installer/nssm.exe).

VIGENTES confirmadas en §2 (tabla resumen): setup.iss:185 (8U-N2), Program.cs:98-104+922,
  Desktop.Client.Core.csproj:12, SalesService.cs:506 (PaymentMethodId), SalesController.cs:
  577/811 + SalesService.cs:454,610 (RoundingAdjustment), SalesService.cs:108-132 y
  HoldOrders.cs:485-518 (write-on-read), SecureTokenStorageService.cs:28 (entropy),
  ClosurePdfGenerator.cs:13-14 (leak), SalesHistoryViewModel.cs:23-237 + IPaymentService.cs:
  23-51 (snake_case 34), ~52 MessageBox (8W-M1), ExchangeRateService.cs:291 (8D-M2),
  STYLE-WPF.

================================================================================
7. VEREDICTO Y RECOMENDACIONES EJECUTIVAS
================================================================================
1. Las métricas (760/760: .NET 685/685 + Web 75/75, oxlint 0/0, build 0/0) del árbol actual
   son VERDADERAS y se reproducen. La mayoría de los hallazgos ALTOS de la Rev. 8.5 quedarón
   RESUELTOS (caja: TOCTOU, doble cierre, forja de Source; DI; jobs). PERO la declaración
   "100% remediado" sigue SIN sostenerse: persisten 9 parciales, 12 vigentes y 24 nuevos,
   incluido 1 CRÍTICO nuevo (vuelto sin control de saldo) y 7 ALTOS (ownership del preview,
   tasa en AddItem/Hold, tests de ownership ausentes, sync-over-async en apagado, sin
   WebApplicationFactory, fallback localhost).
2. MAYOR PRIORIDAD FUNCIONAL/SEGURIDAD: (a) registrar el vuelto con lock+check de saldo
   (8.6-C1) — es la vía más directa a saldo de caja negativo; (b) completar la autorización a
   nivel de objeto en preview/update/reportes (8.6-B1, 8.5-A3); (c) anclar la tasa BCV del día
   en cobro y esperas (8.5-A5, 8.6-B3).
3. La infraestructura de pruebas debe subir de nivel: WebApplicationFactory + ownership +
   montaje real de CheckoutModal + retirar silent-pass en PostgresReal* (8.6-B4/B6).
4. Frontend: reparar F5 (8.6-M1), unificar el canal SignalR (8.6-M2/M4) y code splitting
   (8.6-M5); UX de hora local/UTC consistente (8.6-M7).
5. Seguridad operacional: rotar credenciales por histórico git (8.6-B6-infra), endurecer canal
   de updates (8U-N2), eliminar fallbacks a localhost (8.6-B7).
6. Recomendado ejecutar: S1 de §5 (vuelto, ownership, tasa, AddItem, 423) y luego S2/S3.

================================================================================
FIN DEL REPORTE TÉCNICO — REVISIÓN 8.6
================================================================================