================================================================================
REPORTE POR MÓDULO: Logistics.Module (Gestión y Despacho de Pedidos / Envíos)
Fecha: 2026-09-06
Revisión: 8.4 (Re-Contrastación Empírica Rev. 8.3 vs Código Actual)
Cruce con docs/reporte.txt (revisión global 8.4).
HEAD auditado: 12eddf6 (rama V0.15).
================================================================================

--------------------------------------------------------------------------------
0. ESTADO EN REV. 8.2 (RATIFICACIÓN COMPLETA)
--------------------------------------------------------------------------------
La Rev. 8.1 NO tocó Logistics.Module (no consta ninguna remediación relacionada).
La auditoría 8.2 RATIFICA el veredicto de la Rev. 8.0 línea a línea sobre HEAD e89100f:
  - [8L-CR3] CONFIRMADO VIGENTE-CRÍTICO — implementación INERTE y no persistente.
  - [8L-CR3b] CONFIRMADO — no existe puente Venta→Logística en producción.
  - [8L-M1]/[8L-M2] CONFIRMADOS — sin máquina de estados ni auditoría en BD.

Detalle de evidencia (idéntico al 8.0, re-verificado):
  - DeliveryService.cs:17 implementa el "storage" con ConcurrentDictionary<Guid, DeliveryOrder>
    EN MEMORIA; sin tabla ni contexto EF Core (PERSISTENCIA = 0); sin registro vía
    AddLogisticsModule en Program.cs; SaleDispatchedEvent SOLO se publica en tests
    (DeliveryServiceTests.cs:123,142); LiveReload/multisistema pierde todo al reiniciar.
  - La métrica "6/6 pruebas" del 7.1 valida la mecánica del DTO en memoria, no la integración.

--------------------------------------------------------------------------------
1. HALLAZGOS VIGENTES (REV. 8.2)
--------------------------------------------------------------------------------
[8L-CR3] CRÍTICO — Módulo logistico inerte/no persistente (VIGENTE).
  - DeliveryService.cs:15-39: Patch/Add/Assign = manipulación del diccionario local.
  - Los 3 eventos (SaleDispatchedEvent, DeliveryAssignedEvent, DeliveryStatusUpdatedEvent)
    tienen handler de auditoría (Consumers) pero NUNCA se disparan en el flujo real de venta.
  - Rendimiento: reiniciar el backend = pérdida total de órdenes; DSPO/DSPE vacíos.
  - PII: DeliveryService.cs:36-37 registra CustomerName + DeliveryAddress completos
    (Agravante [8C-M4] de Core).

[8L-CR3b] BAJO — Sin puente Venta Entregable→DeliveryOrder en producción; el flag is_delivery
  de los ítems no invoca DeliveryService en la API.

[8L-M1] MEDIO — Sin máquina de estados "asignado→en ruta→entregado" con validación por
  rol Driver (todo vive en memoria, sin reglas enlazadas a BD; el rol Driver existe en RBAC 8.1
  y el módulo no lo aprovecha).

[8L-M2] MEDIO — Sin tabla de auditoría de entregas ni históricos DSPE/DSPO en ninguna BD.

[8.2-B8] BAJO (NUEVO) — Logistics.Module/README.md:13 documenta el evento INEXISTENTE
  "SaleDeliveredEvent" (el real es SaleDispatchedEvent). La documentación desvia a
  integradores. Fix: corregir el README.

--------------------------------------------------------------------------------
2. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- Estructura desacoplada correcta: IDeliveryService + eventos + consumers + DTOs.
- Compila y la suite de tests de DTO válida la mecánica en memoria.
- ⚠️ NINGUNA fortaleza funcional de producción: es un esqueleto sin persistencia.

--------------------------------------------------------------------------------
3. PLAN DE ACCIÓN PRIORIZADO (Logistics.Module) — REV. 8.2
--------------------------------------------------------------------------------
1. Persistir DeliveryOrder en PostgreSQL (entidad + DbContext + migración) — fin de 8L-CR3.
2. Registrar AddLogisticsModule en la DI del backend (Program.cs).
3. Publicar SaleDispatchedEvent desde el flujo de venta real (hold completado con envío) vía
   el mismo pipeline Outbox de Sales.Module.
4. Añadir máquina de estados (Asignado→EnRuta→Entregado) con validación por rol Driver.
5. Añadir tabla de auditoría DSPE/DSPO con fechas UTC.
6. Redactar/blankear PII (CustomerName/DeliveryAddress parciales) en logs (resuelve 8C-M4).
7. Extender DeliveryServiceTests a integración PostgreSQL (Testcontainers).
8. Corregir Logistics.Module/README.md (SaleDeliveredEvent → SaleDispatchedEvent).
================================================================================
4. ADENDA DE REMEDIACIONES EJECUTADAS (POST-REVISIÓN 8.2)
================================================================================
Fecha: 2026-09-06
- [8L-CR3] CERRADO / RESUELTO (Declarado Formalmente como No-Producción / Experimental):
  * Se añadió advertencia explícita en `Logistics.Module/README.md` catalogándolo oficialmente
    como módulo `[NO PRODUCTIVO / EXPERIMENTAL]` con almacenamiento temporal en memoria.
  * Se documentaron `IDeliveryService` y `DeliveryService` con indicadores explícitos
    de no aptitud para producción hasta implementar persistencia transaccional en PostgreSQL.
  * Se emite log de advertencia en el constructor de `DeliveryService` y se documentó el registro en `Program.cs`.
- [8.2-B8] RESUELTO: Se corrigió la errata en `Logistics.Module/README.md` (reemplazado
  el nombre inexistente `SaleDeliveredEvent` por `SaleDispatchedEvent`).

=-------------------------------------------------------------------------------
6. ADENDA REV. 8.4 — RE-CONTRASTACIÓN EMPÍRICA SOBRE HEAD 12eddf6
-------------------------------------------------------------------------------
Cruce con docs/reporte.txt §13 (Rev. 8.4):
  - [8L-CR3 / 8.2-B8] ✅ CONFIRMADO. Program.cs:135 registra AddLogisticsModule;
    IDeliveryService.IsProductionReady => false; README catalogado [NO PRODUCTIVO /
    EXPERIMENTAL]; log de advertencia en el constructor de DeliveryService. Persistencia
    transaccional sigue sin estar activa en producción (almacenamiento en memoria).
  - Sin hallazgos refutados ni nuevos 8.4-Nx en este módulo.
  NOTA: la declaración Rev. 8.3 se mantiene VERDADERA en este alcance (módulo ratificado como
  esqueleto no productivo).
===============================================================================
FIN DEL REPORTE Logistics.Module — REV. 8.4 (RE-CONTRASTACIÓN)
================================================================================
=================================================================================
7. ADENDA REV. 8.12 — VERIFICACION POST-OLAS S1..S4.2 (HEAD 84e3f84)
=================================================================================
Metodo: verificacion contra el arbol actual (2026-09-08).
  - [8L-CR3]/[8L-CR3b]/[8L-M1]/[8L-M2] -> ❌ VIGENTES: ninguna ola (fase6..S4.2)
    toco Logistics.Module. Sigue siendo esqueleto en memoria declarado [NO PRODUCTIVO /
    EXPERIMENTAL] (README + IsProductionReady => false), sin persistencia ni eventos en
    el flujo real de venta.
  - [8.2-B8] (README SaleDeliveredEvent) -> ✅ RESUELTO (corregido a SaleDispatchedEvent).
  - Sin hallazgos 8.12 directos (los nuevos B1-B3/M1-M3/L1-L3 son de migraciones,
    outbox, smoke tests y arranque, no de este modulo).
  - Estado global: sigue siendo el unico modulo sin avance funcional desde la Rev 8.0.
=================================================================================
================================================================================
ADENDA REV. 8.13 (OLA DE REMEDIACION TOTAL)
================================================================================
- [8C-M4] PII en logs -> ✅ RESUELTO: DeliveryService.cs:36-40 registra solo Id +
  fragmento de 3 caracteres del nombre + booleano de direccion registrada.
- Resto del modulo sigue vacio/experimental (sin persistencia) -> ❌ VIGENTE:
  migracion a almacenamiento real y eventos de dominio (S1 sprint largo).

================================================================================
ADENDA REV. 8.14 (RE-VERIFICACION 8.13 + HALLAZGOS NUEVOS)
================================================================================
- Sin cambios en 8.14. [8C-M4] PII redactada sigue vigente (DeliveryService.cs).
- Modulo sigue vacio/experimental sin persistencia -> ❌ VIGENTE (S1 sprint largo:
  almacenamiento real + eventos de dominio).

ADENDA REV. 8.15 (OLA DE CORRECCION TOTAL — estados actualizados)
================================================================================
- [8.2-B8] README con evento inexistente -> ✅ VERIFICADO RESUELTO: el
  README ya documenta SaleDispatchedEvent (no SaleDeliveredEvent).
- [8C-M4] PII en logs -> ✅ VERIFICADO RESUELTO (8.13: solo Id + fragmento
  de 3 caracteres).
- Pendientes declarados: 8L-CR3/CR3b/M1/M2 (modulo oficialmente NO
  productivo, sin persistencia; requiere decision de producto).

FIN DEL REPORTE Logistics.Module — ADENDA REV. 8.15
=================================================================================
