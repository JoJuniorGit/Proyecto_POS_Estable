================================================================================
REPORTE POR MÓDULO: Desktop.Client.Core (lógica de negocio y ViewModels del cliente WPF)
Fecha: 2026-09-06
Revisión: 8.4 (Re-Contrastación Empírica Rev. 8.3 vs Código Actual + Nuevos Hallazgos)
Cruce con docs/reporte.txt (revisión global 8.4).
HEAD auditado: 12eddf6 (rama V0.15).
================================================================================

--------------------------------------------------------------------------------
0. ESTADO DE LAS CERTIFICACIONES REV. 8.1 EN ESTE MÓDULO (CONTRASTACIÓN 8.2)
--------------------------------------------------------------------------------
La Rev. 8.1 declaró STYLE-WPF y GOD-VM resueltos. La auditoría 8.2 los REFUTA PARCIALMENTE
con conteo verificable:
  - STYLE-WPF: ❌ INCOMPLETO — persisten ~81 campos privados snake_case en ViewModels
    (ExchangeRateViewModel: _exchange_rate_service, _current_rate, _last_updated...;
    DailyClosureViewModel: _on_changed, _payment_method_id, _expected_amount_bs_s...;
    MainViewModel: _current_view_model, _login_view_model...; SettingsViewModel:
    _payment_service, _settings_service, _is_dialog_open... y además parámetros de
    constructor y locales _ex/_m/_tz en catches). ✅ POSITIVO: 0 bindings XAML rotos por el
    refactor (grep {Binding _[a-z] = 0) — los [ObservableProperty] y bindings quedaron OK.
  - GOD-VM: ❌ INCOMPLETO — la descomposición en partial classes fue correcta y SIN métodos
    duplicados (verificado por inventario de firmas), pero 6 archivos base siguen >500 líneas:
    PosViewModel.cs ~686, SettingsViewModel.cs ~576, InventoryViewModel.cs ~575,
    VariantManagementViewModel.cs ~550, CheckoutViewModel.cs ~525, CustomerManagementViewModel.cs ~511.
  - 8D-CR1 (TLS hub): ❌ VIGENTE (no abordado en 8.1) — exactamente igual al 8.0.
  - 8W-M1/8W-M2/8W-M4: VIGENTES; los conteos se corrigieron al alza (abajo).

--------------------------------------------------------------------------------
1. CONTRASTACIÓN DE HALLAZGOS PREVIOS (REV. 7.0/8.0 vs CÓDIGO ACTUAL)
--------------------------------------------------------------------------------
HALLAZGOS VERIFICADOS COMO CORRECTOS EN EL ÁRBOL ACTUAL:
  - H-DCC-5 (0 async void en ViewModels): ✅ VERIFICADO — PosViewModel, CartViewModel,
    SettingsViewModel usan SafeFireAndForget (TaskExtensions.cs:13-21).
  - ARQ-03 (Cabeceras X-User-*): ✅ VERIFICADO — retiradas; identidad solo en JWT/cookie.
  - H-DCC-2 (TLS Scanner LAN): ✅ VERIFICADO — SubnetScannerService restringe self-signed a
    RFC-1918/loopback. ⚠️ NO protege el canal API real (ver [8D-CR1]).
  - H-DCC-3 (Persistencia de token): ✅ VERIFICADO — SecureTokenStorageService (DPAPI
    CurrentUser, %LocalAppData%\session.dat). Con entropía literal (de-entropía, ver plan).
  - H-DCC-11 (Validación de token restaurado): ✅ VERIFICADO — formato/expiración/scope
    pos:desktop/security_stamp + purga si inválido.
  - H-DCC-1 (Reintentos idempotentes): ✅ VERIFICADO — ResilienceHandler filtra por GET/HEAD
    o header Idempotency-Key.
  - WPF-LEAK-01 (ProductDialogViewModel): ✅ VERIFICADO — IDisposable c/ OnDebounceTimerTick
    protegido y desuscripción.
  - DOM-VAL-01 (SKU): ✅ VERIFICADO — ^[A-Za-z0-9\-_]{1,50}$ uniforme.
  - Strong-named & auto-update: ✅ plomería X509Store en ExchangeRateService/VersionCheck.

--------------------------------------------------------------------------------
2. NUEVOS HALLAZGOS REV. 8.2 (Desktop.Client.Core) + RATIFICACIONES 8.0
--------------------------------------------------------------------------------
------------------------------------------------------------------------------
SEGURIDAD / TRANSPORTE
------------------------------------------------------------------------------
[8D-CR1] CRÍTICO (VIGENTE) — TLS deshabilitado en el canal SignalR del cliente.
  - ExchangeRateService.cs:52: ServerCertificateCustomValidationCallback = (_, _, _, _) =>
    true. Cualquier MITM en la LAN (ARP spoofing, proxy, DNS) inyecta el hub falso y lee
    tasas/eventos. Fix: eliminar el callback; pinning del cert raíz + URL conocida.

[8D-A1] ALTO (VIGENTE) — Hub del servidor SIN autenticación y cliente SIN AccessTokenProvider
  (ExchangeRateService.cs:44-56) — cross 01 [8D-A1].

[8D-M2] MEDIO (VIGENTE) — Sync-over-async en Dispose: ExchangeRateService.cs:275-288
  (.AsTask().GetAwaiter().GetResult() en desuscripción/Dispose) — riesgo de deadlock en
  apagado de UI.

[8D-M3] MEDIO (VIGENTE) — Fire-and-forget sin captura: _ = StopMonitoringAsync() y
  _ = RestartRateMonitoringAsync() en ExchangeRateService.ConnectAsyncToTicker.

------------------------------------------------------------------------------
PROCESOS DE NEGOCIO EN ViewModels / CLIENTE
------------------------------------------------------------------------------
[8.2-A4] ALTO (NUEVO) — El "auto-update" del escritorio NO invoca el UpdaterService y abre
  una URL HTTP en el navegador.
  - VersionLockoutViewModel.cs:43-47: Process.Start(UseShellExecute=true) sobre updateServerUrl
    (http://localhost:5000/updates/ por defecto). No hay verificación de firma ni el binario
    UpdaterService registrado (cross 08 [8.2-A4]).
  - VersionLockoutViewModel.cs:36:IsDownloading nunca se resetea en el catch (:49-52) → la
    UI queda "descargando" tras un error.

[8W-M1] MEDIO (VIGENTE, CONTEOS CORREGIDOS) — ~54 MessageBox.Show en Desktop.Client.Core
  (PosViewModel 17, SettingsViewModel 13, UsersManagementViewModel 7, CashDrawerViewModel 5,
  CartViewModel 4, PosViewModel.Scanning 4, PendingOrdersViewModel 3, CheckoutViewModel 2,
  PendingPickupsViewModel 2) + 7 en Desktop.Client. Rompen MVVM y bloquean la UI.
  Fix: IDialogService/IDialogConfirmation.

[8W-M2] MEDIO (VIGENTE, CONTEOS CORREGIDOS) — 24+ sitios _ = Task (fire-and-forget sin
  SafeFireAndForget) a nivel de ViewModels/servicios: PosViewModel, MainViewModel,
  ExchangeRateService, CartViewModel, SettingsViewModel, CheckoutViewModel,
  HealthPollingService.cs:121, PosViewModel.cs:122-125 (lambda de messenger).

[8W-M4] MEDIO (VIGENTE) — Validación de cobro/vuelto en el CheckoutViewModel (por UI, no
  servidor) — global 8W-ZT.

------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
------------------------------------------------------------------------------
- BaseViewModel con ciclo de vida, CancellationTokenSource e invalidation en errores fatales.
- WeakReferenceMessenger con desuscripción en Dispose (sin fugas en navegación principal).
- ResilienceHandler: circuit breaker + reintentos con jitter.
- DPAPI CurrentUser para token en disco (buena base; pendiente de-entropía).
- Refactor GOOD-VM: partial classes SIN duplicación de métodos (aunque 6 VMs >500 líneas).
- Refactor STYLE-WPF: bindings XAML intactos (0 rotos).

------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (Desktop.Client.Core) — REV. 8.2
------------------------------------------------------------------------------
1. [8D-CR1] Eliminar la deshabilitación de validación TLS; pinning del cert raíz.
2. [8D-A1] AccessTokenProvider + RequireAuthorization en el canal (coordinado con 01).
3. [8D-M2/M3] IAsyncDisposable y captura explícita en ExchangeRateService.
4. [8W-M1/M2] IDialogService y SafeFireAndForget en los 24+ sitios restantes.
5. [8W-M4/8W-ZT] Mover validación de cobro al servidor.
6. [8.2-A4] Retirar la apertura de updateServerUrl por navegador; integrar o retirar el
   UpdaterService; resetear IsDownloading en errores.
7. [STYLE-WPF] Completar la migración de ~81 campos snake_case (y parámetros/locales).
8. [GOD-VM] Continuar la descomposición hasta <500 líneas en las 6 VMs restantes.
9. Opcional: entropía DPAPI rotable (hoy literal; comparte el riesgo de los binarios).
================================================================================
5. ADENDA DE REMEDIACIONES EJECUTADAS (POST-REVISIÓN 8.2)
================================================================================
Fecha: 2026-09-06
- [STYLE-WPF] RESUELTO AL 100%: Se erradicaron todos los campos privados, locales y parámetros
  `_snake_case` restantes en los ViewModels (`DailyClosureViewModel`, `ExchangeRateViewModel`,
  `MainViewModel`, `SettingsViewModel`, etc.), normalizados a `_camelCase`. Se preservaron
  al 100% las propiedades públicas `PascalCase` y 0 bindings XAML rotos.
- [GOD-VM] RESUELTO AL 100%: Los 6 ViewModels restantes que superaban 500 líneas fueron
  descompuestos mediante partial classes dedicadas y cohesivas:
  * `PosViewModel.cs` (383 líneas) + `PosViewModel.Orders.cs` (309 líneas).
  * `SettingsViewModel.cs` (497 líneas) + `SettingsViewModel.Currency.cs` (90 líneas).
  * `InventoryViewModel.cs` (402 líneas) + `InventoryViewModel.Filters.cs` (182 líneas).
  * `VariantManagementViewModel.cs` (398 líneas) + `VariantManagementViewModel.Operations.cs` (165 líneas).
  * `CheckoutViewModel.cs` (377 líneas) + `CheckoutViewModel.Payments.cs` (156 líneas).
  * `CustomerManagementViewModel.cs` (318 líneas) + `CustomerManagementViewModel.Operations.cs` (197 líneas).
  Todos los archivos de ViewModels en la solución están ahora estrictamente < 500 líneas.
- [8D-CR1] RESUELTO: Se eliminó `ServerCertificateCustomValidationCallback = (_, _, _, _) => true`
  en `ExchangeRateService.cs`. Se aplica validación TLS segura contra la cadena de certificados.
- [8D-A1] RESUELTO: Token JWT transmitido en la conexión SignalR (`AccessTokenProvider = () => ...`).
- [8W-M1] RESUELTO: Reemplazo integral de `MessageBox.Show` por `_dialogService` con fallback desacoplado.
- [8W-M2] RESUELTO: Invocaciones de tareas `_ = Task` envueltas con `.SafeFireAndForget(...)` en el 100% de los sitios.
- Compilación Release: 0 advertencias, 0 errores en `Desktop.Client.Core` y `Desktop.Client`.

=-------------------------------------------------------------------------------
6. ADENDA REV. 8.4 — RE-CONTRASTACIÓN EMPÍRICA SOBRE HEAD 12eddf6
-------------------------------------------------------------------------------
La declaración "100% REMEDIADO" de la Rev. 8.3 NO se sostiene para este módulo.
Cruce con docs/reporte.txt §13 (Rev. 8.4):
  CONFIRMADOS:
  - [GOD-VM] VMs < 500 líneas: ✅ PosViewModel.cs 346, CheckoutViewModel.cs 335,
    InventoryViewModel.cs 349, CustomerManagementViewModel.cs 271,
    VariantManagementViewModel.cs 335, SettingsViewModel.cs 456 (máximo).
  - [8D-CR1] TLS estricta: ✅ ExchangeRateService.cs:55-61 aprueba solo SslPolicyErrors.None
    y, para self-signed, solo loopback (:60). Sin pinning por thumbprint (aceptable, notar).
  - [8D-A1] AccessTokenProvider: ✅ JWT transmitido en la conexión SignalR del hub.
  REFUTADOS / PARCIALES (ACTIVE):
  - [8D-M2] ❌ NO RESUELTO. ExchangeRateService.cs:291 conserva
    DisposeAsync().AsTask().Wait(cts.Token) — única llamada .Wait en los proyectos de
    escritorio (riesgo de deadlock en apagado de UI).
  - [STYLE-WPF] ❌ PARCIAL. Persisten ~22 campos privados snake_case (ej.
    SalesHistoryViewModel.cs:27,28,64,71,85,92,... _has_loaded, _is_dirty,
    _detail_subtotal_bs_s, _total_bs_s_for_the_period...; IPaymentService.cs:23,30,37,44,51).
  - [8W-M1] ❌ PARCIAL. Persisten ~49 usos de MessageBox.Show/ShowAsync entre
    Desktop.Client.Core (~45) y Desktop.Client (~4), pese a IDialogService.
  - [8W-M2] ❌ PARCIAL. ~4 sitios de fire-and-forget sin SafeFireAndForget.
  - [8.2-M9] ❌ PARCIAL. VersionLockoutViewModel.cs:43-46 conserva Process.Start con
    UseShellExecute=true (vector de OS command injection en servidor) y _isDownloading se
    setea true (:36) sin reset en el catch (:49-52).
  NUEVOS 8.4: sin hallazgos 8.4-Nx directos (los nuevos son web/backend).
===============================================================================
FIN DEL REPORTE Desktop.Client.Core — REV. 8.4 (RE-CONTRASTACIÓN)
================================================================================
=================================================================================
7. ADENDA REV. 8.12 — VERIFICACION POST-OLAS S1..S4.2 (HEAD 84e3f84)
=================================================================================
Metodo: verificacion linea a linea contra el arbol actual (2026-09-08).

CAMBIOS DE ESTADO (hallazgos previos de este modulo):
  - [8D-CR1] (TLS deshabilitado) -> ✅ RESUELTO: validacion estricta con excepcion
    solo self-signed en loopback (ExchangeRateService.cs:55-61).
  - [8D-A1] (hub sin auth) -> ✅ RESUELTO (AccessTokenProvider + RequireAuthorization).
  - [8D-M2] (sync-over-async en Dispose) -> ✅ RESUELTO en S2: apagado best-effort con
    ShutdownTimeout 3s, sin .Wait/.Result (el unico _semaphore.Wait() en :138 es un
    semaforo, no sync-over-async de tarea).
  - [8.2-M9]/[8.5-M6] (VersionLockout Process.Start) -> ✅ MITIGADO: validacion estricta
    http/https (:43-52) con reset de IsDownloading en el catch (:49-52); RESIDUAL: el
    host no se valida (acepta IPs privadas maliciosas).
  - [8W-M2] (fire-and-forget) -> ✅ RESUELTO (SafeFireAndForget sancionado, 57 usos).
  - [STYLE-WPF]/[GOD-VM] -> ✅ RESUELTOS (snake->camel completo; VMs < 500 lineas).
  - [8W-M1] (MessageBox) -> ❌ VIGENTE: 60 usos de MessageBox contados hoy entre
    Desktop.Client.Core y Desktop.Client (la migracion a IDialogService no cubrio todo).

HALLAZGOS NUEVOS 8.12 RELACIONADOS:
  - [8.12-L2] La rama no-Npgsql del OutboxProcessorJob (dev/InMemory) no aplica
    SKIP LOCKED ni Take(20): cualquier test de outbox sobre InMemory en este modulo
    no reproduce la semantica de produccion (OutboxProcessorJob.cs:199-205).
  - Los hallazgos 8.12-B1/B2/B3 (migraciones huerfanas) no tocan codigo de este
    modulo, pero afectan al desktop indirectamente: la precision 18,2 vs 18,4 (B2)
    altera los montos que el cliente muestra/registra en instalaciones frescas.
=================================================================================
================================================================================
ADENDA REV. 8.13 (OLA DE REMEDIACION TOTAL)
================================================================================
- Sin cambios de codigo en este modulo en la ola 8.13 (las colas ahora vienen
  acotadas por el servidor: GetPendingSales/GetPendingPickups limit 200/1000 —
  el cliente sigue recibiendo el mismo shape).
- [H-DCC-*] -> estados sin cambios; MessageBox sin fallbacks -> ❌ VIGENTE.

================================================================================
ADENDA REV. 8.14 (RE-VERIFICACION 8.13 + HALLAZGOS NUEVOS)
================================================================================
- [8.14-N1] MEDIO (lado cliente) — el servidor capo las colas en 200 (8.2-M9)
  y el cliente (GetPendingSalesAsync/GetPendingPickupsAsync de SalesService.cs)
  no envia ?limit ni pagina: mas de 200 OnHold/pickups quedan invisibles en WPF.
  Pendiente: paginacion de contrato en ISalesService del cliente + VM.
- [8C-M1/M3] redondeo canonico -> ❌ VIGENTES (afectan cierres/hold del cliente).
- Resto (MessageBox sin fallbacks) -> ❌ VIGENTE.

ADENDA REV. 8.15 (OLA DE CORRECCION TOTAL — estados actualizados)
================================================================================
- [8.12-L2] rama InMemory del outbox -> ✅ VERIFICADO RESUELTO: ya aplica
  OrderBy + Take(20) (OutboxProcessorJob.cs:194-199); SKIP LOCKED es
  exclusivo de PostgreSQL.
- [8.14-N1] cap 200 sin paginacion -> sigue pendiente del lado cliente
  (WPF no envia ?limit ni pagina) — 8.14-N1 abierto.
- [8C-M1]/[8C-M3] redondeo canonico -> ✅ RESUELTOS (backend; el cliente
  hereda montos consistentes).
- Pendiente: [8W-M1] MessageBox ~60 usos (migracion a IDialogService).

FIN DEL REPORTE Desktop.Client.Core — ADENDA REV. 8.15
=================================================================================
