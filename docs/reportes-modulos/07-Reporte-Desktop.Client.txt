================================================================================
REPORTE POR MÓDULO: Desktop.Client (interfaz de usuario WPF y vistas)
Fecha: 2026-09-06
Revisión: 8.2 (Contrastación Empírica Rev. 8.1 vs Código Actual + Nuevos Hallazgos)
Cruce con docs/reporte.txt (revisión global 8.2).
HEAD auditado: e89100f (rama V0.15).
================================================================================

--------------------------------------------------------------------------------
0. CONTRASTACIÓN REV. 8.1 EN ESTE MÓDULO
--------------------------------------------------------------------------------
La Rev. 8.1 no declaró remediaciones específicas del front WPF (STYLE-WPF aplicó sobre
Desktop.Client.Core). La auditoría 8.2 CONFIRMA los hallazgos de la Rev. 8.0 y AUMENTA el
detalle de virtualización ([8D-M1b]) con el inventario real de DataGrids.

--------------------------------------------------------------------------------
1. CONTRASTACIÓN DE HALLAZGOS PREVIOS (REV. 7.0/8.0 vs CÓDIGO ACTUAL)
--------------------------------------------------------------------------------
  - H-WPF-3 (code-behind defensivo): ✅ VERIFICADO — EditSaleDialog.xaml.cs y
    ImportProductsView.xaml.cs con try/catch + AppLogger.LogCrash.
  - H-WPF-2 (shutdown no bloqueante): ✅ VERIFICADO — App.xaml.cs cierre con timeout 3 s.
  - ERR-02 (crash logger): ✅ VERIFICADO — %LOCALAPPDATA%\CommandCenterPOS\Logs con rotación.
  - WPF-LEAK-01/02 (ciclo de vida): ✅ VERIFICADO — ProductDialog OnClosed → Dispose();
    SuccessDialogWindow desuscribe _timer.Tick.
  - Virtualización: ⚠️ SÓLO PARCIAL (ver [8D-M1b] actualizado).

--------------------------------------------------------------------------------
2. NUEVOS HALLAZGOS REV. 8.2 (Desktop.Client) + RATIFICACIONES 8.0
--------------------------------------------------------------------------------
[8D-M1] MEDIO (VIGENTE) — Fuga de memoria en DailyClosureView.xaml.cs:19-25.
  - Suscripción anónima (lambda) al evento CurrentViewModel del window/control cada vez que
    cambia el tipo de vista y NUNCA se desuscribe → retención de vistas muertas al abrir/cerrar.
  - Fix: tokens WeakReferenceMessenger o - = de manejador nombrado en OnClosed.

[8D-M1b] MEDIO (ACTUALIZADO — inventario real) — Virtualización SOLO en 3 grillas.
  - SÍ tienen VirtualizingPanel.Recycling: PosView, InventoryView, SalesHistoryView.
  - NO tienen (cargan todo sin virtualización): CashDrawerView, PendingOrdersView,
    PendingPickupsView, UsersManagementView, EditSaleDialog, VariantManagementDialog.
  - Fix: aplicar Recycling+Pixel + paginación en VM a las 6 faltantes.

[8D-M2] MEDIO (VIGENTE) — Lógica financiera en code-behind: EditSaleDialog.xaml.cs
  (búsqueda + edición) y CheckoutView.xaml.cs (pago/vuelto local). Divergencia riesgo con el
  servidor (global 8W-ZT). Fix: mover a VM y validar contra endpoints.

[8D-B1] BAJO (VIGENTE) — MessageBox/SaveFileDialog/OpenFileDialog en petición-respuesta;
  dlg.ShowDialog() dentro de procesos async cerca de mensajería débil puede encadenar
  deadlocks (revisar llamados en EditSaleDialog/SettingsView).

[8D-B2] BAJO (VIGENTE) — NumericCalculatorBehavior y behaviors con regex sin anclaje
  (aceptan "1.2.3"); la sanidad final la hace el backend. Hardening de tecleado deseable.

[8.2-B6] BAJO (cross 09) — Los modales del cliente WPF carecen de gestión de foco/accessibility
  (aria/focus trap); el Desktop hereda la misma deuda de higiene de accesibilidad.

--------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- Estilos Material Design coherentes; tema único y modo lectura condicional.
- Navegación principal con WeakReferenceMessenger y VM de navegación limpio.
- Diálogos de edición con debouncing; cierre defensivo global.
- Virtualización correcta en las 3 grillas principales (Pos/Inventory/SalesHistory).

--------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (Desktop.Client) — REV. 8.2
--------------------------------------------------------------------------------
1. [8D-M1] Desuscripción de CurrentViewModel en DailyClosureView (token/OnClosed).
2. [8D-M1b] Virtualizar CashDrawer, PendingOrders, PendingPickups, Users, EditSale, Variant.
3. [8D-M2] Mover reglas de edición/cobro del code-behind a VM+servidor.
4. [8D-B1/B2] Revisar modal blockers y regex monetarios.
5. [8.2-B6] Endurecer accesibilidad/foco de diálogos (armonizado con Web).
================================================================================
5. ADENDA DE REMEDIACIONES EJECUTADAS (REVISIÓN 8.3 - CIERRE TOTAL)
================================================================================
Fecha: 2026-09-06
- [8D-M1] RESUELTO: Desuscripción formal del evento PropertyChanged del ViewModel en `DailyClosureView.xaml.cs` (en `Unloaded` y `DataContextChanged`), eliminando la fuga de memoria al navegar.
- [8D-M1b] RESUELTO: Virtualización UI activada (`VirtualizingPanel.IsVirtualizing="True"`, `VirtualizationMode="Recycling"`, `ScrollUnit="Pixel"`) en todas las grillas restantes: `CashDrawerView`, `PendingOrdersView`, `PendingPickupsView`, `UsersManagementView`, `EditSaleDialog`, `VariantManagementDialog`.
- Compilación Release: 0 advertencias, 0 errores en `Desktop.Client`.

================================================================================
FIN DEL REPORTE Desktop.Client — REV. 8.3 (100% REMEDIADO)
================================================================================