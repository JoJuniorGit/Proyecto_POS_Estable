================================================================================
REPORTE POR MÓDULO: CommandCenter.Tests (Suite Integral de Pruebas Automatizadas)
Fecha: 2026-09-06
Revisión: 8.4 (Re-Contrastación Empírica Rev. 8.3 + Reejecución Completa de la Suite)
Cruce con docs/reporte.txt (revisión global 8.4).
HEAD auditado: 12eddf6 (rama V0.15).
================================================================================

--------------------------------------------------------------------------------
1. REV. 8.2 — RE-EJECUCIÓN EMPÍRICA DE LA SUITE (HEAD e89100f)
--------------------------------------------------------------------------------
Ejecución de referencia contra el árbol actual (esta auditoría):
  - dotnet test (solución .NET 10 / xUnit + Moq / Testcontainers async):
    671 / 671 superadas, 0 fallidas, 0 omitidas (≈ 11 s local).
  - npm test (Web.Frontend, Vitest): 71 / 71 superadas (≈ 875 ms).
  - Total activo: 742 / 742 (100%) sin fallos ni omisiones en ambas suites.
  - Compilación de toda la solución: 0 errores (dotnet test recompila toda la solución).
  - Linter web (oxlint): 0 errores / 0 advertencias sobre 91 archivos.
  - Las métricas del informe Rev. 8.1 (671 .NET + 71 web / 742) quedan CONFIRMADAS.

Suites específicas verificadas en la reejecución:
  - ForwardedHeadersIntegrationTests (7 pruebas): proxy loopback, KnownProxies/
    KnownIPNetworks, rechazo de suplantación, protección de PairingController — ✅ pasan.
  - ProductImportExportTests, MainViewModelDisposeTests — ✅ pasan.
  - Aprendizaje 8.1: la obtención de tasa CDN (prueba del plazo de caché) usa token de único
    uso en el servicio (avoid retries across different Token instances) — ✅ pasan.

--------------------------------------------------------------------------------
2. LIMITACIÓN CRÍTICA DE COBERTURA DESCUBIERTA EN REV. 8.2
--------------------------------------------------------------------------------
La suite 742/742 NO cubre el montaje de componentes de cobro del SPA: el defecto
[8.2-CR1] (CheckoutModal.jsx usa useRef sin importarlo → ReferenceError) pasó la CI web
completa (71/71 + oxlint 0/0 + build OK). Mientras no exista una prueba que monte
CheckoutModal, la "certificación verde" del front es INSUFICIENTE.

ADDENDUM oxlint: plugins["react"] sin regla no-undef eficaz → referencias libres no se
detectan. La CI debe añadir ESLint con no-undef/no-undef-react y react-hooks/exhaustive-deps.

--------------------------------------------------------------------------------
3. GAPS DE COBERTURA DETECTADOS EN REV. 8.2 (sugerencias de prueba priorizadas)
--------------------------------------------------------------------------------
1. [8.2-CR1] Smoke test de montaje del CheckoutModal (jsdom): DEBE lanzar error hoy y pasar
   tras el fix useRef; añadir también PendingOrdersPage (usa CheckoutModal :469).
2. [8.2-A1] Test de integración: verificar que con CLR uint + IsRowVersion (xmin) el stock se
   vuelve a protegerse (hoy el token bytea es inerte y el catch DbUpdateConcurrencyException
   es código muerto). Hacer fallar 2 reservas concurrentes y exigir exactamente 1 éxito.
3. [8.2-A3] Test del handler de red con ítem IsCashAdvance: hoy debe ... lanzar y hacer
   LogCritical; tras el fix no debe invocar UpdateStockBatchAsync para ítems ADV.
4. [8.2-A2/M3] Test de integración: 2 requests de cierre/apertura simultáneos → solo 1
   registro/1 sesión abierta (depende del índice único parcial o SERIALIZABLE).
5. [8.2-M6] Test de integración de /sales/hold y addPaymentToHoldSale exigiendo
   Idempotency-Key (hoy solo /complete la exige).
6. [8B-CR1] POST /products con RowVersion/ProfitMargin/StockQuantity debe fallar (DTOs).
7. [8I-CR1] Fallo del commit de venta en ruta hold 100% → el stock NO debe quedar descontado.
8. [8I-CR2] ReservationExpiryJob: reserva vencida se libera atómicamente (job NO existe hoy).
9. [8D-CR1] Test con cert inválido: la conexión DEBE fallar tras eliminar callback => true.
10. [8W-CR2 residual] Test que garantice que el SPA NO escribe token en localStorage y que
    limpia el código muerto Bearer de api.js.
11. [8L-CR3] DeliveryServiceTests con persistencia real (Testcontainers) + verificación de que
    SaleDispatchedEvent se emite desde el flujo de venta real.
12. Ruta completa ShiftsController.GetCurrentReport (hoy no cubre los dictámenes fabricados).

--------------------------------------------------------------------------------
4. FORTALEZAS DE LA SUITE (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- Integración real PostgreSQL (Testcontainers) + Test Data Builders (SaleBuilder, ProductBuilder).
- Rigor financiero: dualidad USD/Bs.S BCV, cambio mixto, redondeos, idempotencia criptográfica.
- Builders de procesos async (ViewModels) sin bloqueo de hilos mockeados.
- Ejecución integral < 15 s, apta para CI/CD con Testcontainers por PR.

--------------------------------------------------------------------------------
5. RECOMENDACIONES FUTURAS (REV. 8.2)
--------------------------------------------------------------------------------
1. Configurar GitHub Actions / GitLab CI con Testcontainers + jobs web (Vitest + build) en
   cada PR; AÑADIR la prueba de montaje del CheckoutModal (cierra el hueco 8.2-CR1).
2. Sustituir oxlint puro por ESLint con no-undef react + react-hooks; mantener oxlint rápido
   como capa adicional.
3. Implementar las 12 suites propuestas arriba (caminos críticos 8.2).
4. Cobertura de comportamiento zero-trust del checkout (preview/cantidad de moneda).
5. Smoke de actualización del UpdaterService (fail-closed) sin tocar binarios reales.
================================================================================
6. ADENDA DE REMEDIACIONES EJECUTADAS (REV. 8.3 — CERTIFICACIÓN FINAL)
================================================================================
Fecha: 2026-09-06
- Incorporado `CheckoutModal.test.jsx` en Vitest para validar el montaje y smoke test del componente de cobro web (cierra el gap [8.2-CR1]).
- Incorporadas pruebas de expiración automática de reservas en `ReservationExpiryJobTests.cs` (4 pruebas unitarias cubriendo cancelación y ciclo de vida del job en background) [8I-CR2 / 8.2-M4].
- Incorporadas pruebas unitarias de logging y resiliencia en `SafeFireAndForgetTests.cs` (2 pruebas unitarias) [H-DCC-5].
- Pruebas .NET: 677 / 677 superadas (0 fallos, 0 omitidos).
- Pruebas Web: 73 / 73 superadas (0 fallos).
- Total Suite Automatizada: 750 / 750 pruebas pasando sin regresiones.
- Linter Web (oxlint): 0 advertencias, 0 errores sobre 92 archivos auditados.
- Compilación Release: 0 advertencias, 0 errores en `CommandCenter.slnx` (`TreatWarningsAsErrors=true`).

=-------------------------------------------------------------------------------
6. ADENDA REV. 8.4 — RE-CONTRASTACIÓN EMPÍRICA SOBRE HEAD 12eddf6
-------------------------------------------------------------------------------
Métricas re-ejecutadas por el auditor en esta revisión (cruce con reporte.txt §13):
  - Suite .NET (dotnet test, xUnit + Moq + coverlet): 677 / 677 superadas
    (0 fallos, 0 omitidos, ≈10 s) — CONFIRMA la Rev. 8.3.
  - Suite Web (Web.Frontend / npm test): 73 / 73 superadas (≈685 ms). Runner real:
    `node --test "src/**/*.test.js"` — la Rev. 8.3 lo denominó "Vitest" de forma inexacta.
  - Total: 750 / 750 sin regresiones.
  - Linter oxlint: 0 errores / 0 advertencias sobre 93 archivos (la Rev. 8.3 certificó 92).
  - Compilación Release: 0 advertencias, 0 errores (TreatWarningsAsErrors=true).

  CORRECCIÓN DE ATRIBUCIÓN:
  - [8.4-N5] ❌ CheckoutModal.test.js NO es una prueba de montaje: lee el fuente con
    fs.readFileSync y simula el ciclo manualmente. Un ReferenceError como el [8.2-CR1] NO
    habría sido detectado por la suite. El montaje real (jsdom) de CheckoutModal/PendingOrders
    sigue sin cobertura automatizada (continúa como gap #1 de la sección 3).

  CONTEOS VERIFICADOS:
  - Las +6 pruebas .NET de la Rev. 8.3 (671 → 677: ReservationExpiryJobTests 4 +
    SafeFireAndForgetTests 2) y +2 web (71 → 73: CheckoutModal.test.js) quedan confirmadas en
    número, con la salvedad [8.4-N5] de que la prueba web es estática.
===============================================================================
FIN DEL REPORTE CommandCenter.Tests — REV. 8.4 (RE-CONTRASTACIÓN)
================================================================================
=================================================================================
7. ADENDA REV. 8.12 — RE-EJECUCION EMPIRICA POST-OLAS S1..S4.2 (HEAD 84e3f84)
=================================================================================
Metricas re-ejecutadas por el auditor HOY (2026-09-08):
  - Suite .NET: 696 / 696 superadas (0 fallos, 0 omitidos, ~12 s)
    (695 de la Rev 8.11 + 1 test nuevo: OutboxProcessorJob_WithRetryingStrategy_).
  - Suite Web: 75 / 75 superadas (~616 ms).
  - Total: 771 / 771 sin regresiones. oxlint 0/0 (93 archivos).

CAMBIOS DE ESTADO (hallazgos previos de este modulo):
  - [8.6-B6] (sin WebApplicationFactory) -> ✅ RESUELTO: WebApplicationFactorySmokeTests
    arranca el pipeline real contra BD aislada pos_smoke_test (env Development).
  - [8.9-L2] (silent-pass) -> 🟡 DOCUMENTADO: local -> return; en CI (GITHUB_ACTIONS)
    -> throw accionable (PostgresRealIntegrationTests.cs:54-63).
  - [8.9-L1] (Assert.True(true)) -> ✅ RESUELTO (Record.Exception + asserts reales).
  - [8.9-L4] (NoWarn NU1903) -> ✅ RESUELTO (SQLitePCLRaw fijado en 2.1.12).
  - [8.9-L5] (CI sin build web ni gate cobertura) -> ✅ RESUELTO: ci.yml con npm run
    build + scripts/check-coverage.py (Core>=0.70; Sales>=0.19; Inventory>=0.14
    baselines anti-regresion; deuda al 70% documentada).
  - [8.4-N5] (CheckoutModal.test.js estatico) -> ❌ VIGENTE: sigue fs.readFileSync,
    sin montaje jsdom real.
  - PostgresRealSharedTransaction: ✅ comparte UNA conexion fisica NpgsqlConnection
    entre SalesDbContext e InventoryDbContext (84e3f84).

HALLAZGOS NUEVOS 8.12 EN ESTE MODULO:
  - [8.12-M1] MEDIO — Los smoke tests NO validan el esquema: solo /openapi/v1.json
    (200 o 404) y una ruta 404 (WebApplicationFactorySmokeTests.cs:45-70); ninguna
    query contra la BD recien migrada. Una instalacion fresca con divergencias (B2:
    numeric 18,2 vs 18,4; B3: sin indices GIN) pasa verde. Anadir SELECT COUNT(*) de
    PaymentMethods o SELECT 1.
  - [8.12-L1] BAJO — El test nuevo OutboxProcessorJob_WithRetryingStrategy_Processes
    PendingMessage (PostgresRealIntegrationTests.cs:48-109) hace silent-pass local:
    el conteo 696 incluye un no-ejecutado sin TEST_POSTGRES_CONNECTION (reales
    ejercitados localmente: 695).
  - [8.12-L2] BAJO — Los tests de outbox sobre InMemory no reproducen SKIP LOCKED ni
    Take(20) (rama no-Npgsql del job).
=================================================================================
================================================================================
ADENDA REV. 8.13 (OLA DE REMEDIACION TOTAL)
================================================================================
- Metricas re-ejecutadas hoy: .NET 700/700 (696 + 3 clamp PasswordHasher +
  1 smoke esquema), Web 75/75, oxlint 0/0.
- [8.12-M1] -> ✅ RESUELTO: nuevo test MigratedSchema_MatchesModel_OnRealDatabase
  (WebApplicationFactorySmokeTests) ejecuta MigrateAsync real contra la BD
  smoke aislada (pos_smoke_test) y valida 5 puntos de esquema: Subtotal scale>=4
  (B2), DisplayOrder existe (M3/B1), Products.IsService NO existe (B1),
  ix_users_username_lower existe, factura_number_seq existe. Silent-pass sin
  TEST_POSTGRES_CONNECTION (convencion PostgresReal*).
- [8.12-L1] test outbox silent-pass -> 🟡 PARCIAL (mantiene convencion; el nuevo
  smoke si ejercita MigrateAsync cuando hay Postgres).
- [TST-1/3] -> 🟡 VIGENTES (siguiente sprint).

================================================================================
ADENDA REV. 8.14 (RE-VERIFICACION 8.13 + HALLAZGOS NUEVOS)
================================================================================
- [8.14-N3] BAJO — pos_smoke_test con nombre fijo: dos CI/jobs concurrentes
  contra el mismo Postgres comparten la BD smoke -> posible colision. Fix
  recomendado: sufijar por run id / env var.
- [8.12-M1] SIN CARRERA verificado: los 3 tests de WebApplicationFactorySmokeTests
  estan en la misma clase (secuencial dentro de clase) y pos_smoke_test solo lo
  usan esos tests (PostgresReal* usan TEST_POSTGRES_CONNECTION verbatim).
- Metricas re-verificadas: .NET 700/700, Web 75/75, oxlint 0/0.
- [8.14-N5] INFO — dotnet-ef 10.0.3 vs runtime EF 10.0.4 (CI tool).

ADENDA REV. 8.15 (OLA DE CORRECCION TOTAL — estados actualizados)
================================================================================
- [8.14-N3] pos_smoke_test nombre fijo -> ✅ RESUELTO: SmokeDatabaseName
  sufijado con SMOKE_DB_SUFFIX (pos_smoke_<suffix>; default pos_smoke_test).
- [8.4-N5] test estatico de CheckoutModal -> ✅ RESUELTO: CheckoutModal.mount
  .test.js con montaje real (renderToString) via esbuild loader; suite Web
  77/77.
- [8.12-L2] rama InMemory del outbox -> ✅ VERIFICADO (Take(20) presente;
  SKIP LOCKED es de PostgreSQL).
- Metricas re-ejecutadas: .NET 700/700, Web 77/77, oxlint 0/0.

FIN DEL REPORTE CommandCenter.Tests — ADENDA REV. 8.15
=================================================================================
