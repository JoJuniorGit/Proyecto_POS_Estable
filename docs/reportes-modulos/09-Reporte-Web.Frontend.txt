================================================================================
REPORTE POR MÓDULO: Web.Frontend (Cliente Web SPA — React 19 + Vite + PWA)
Fecha: 2026-09-06
Revisión: 8.2 (Contrastación Empírica Rev. 8.1 vs Código Actual + Nuevos Hallazgos)
Cruce con docs/reporte.txt (revisión global 8.2).
HEAD auditado: e89100f (rama V0.15).
Métricas ejecutadas en esta auditoría: 71/71 pruebas Vitest (875 ms), oxlint 0/0 (91 archivos),
build de producción OK — PERO ninguno detecta el fallo crítico [8.2-CR1].
================================================================================

--------------------------------------------------------------------------------
0. HALLAZGO CRÍTICO DE REV. 8.2 (URGENTE)
--------------------------------------------------------------------------------
[8.2-CR1] CRÍTICO — El CheckoutModal NO PUEDE MONTAR (regresión del commit WEB-1).
  - Web.Frontend/src/components/checkout/CheckoutModal.jsx:1 importa
    { useState, useEffect, useCallback, useImperativeHandle, forwardRef } SIN useRef, pero
    :27 ejecuta const checkoutKeyRef = useRef(null) → ReferenceError: useRef is not defined
    en cada montaje.
  - Impacto: el POS completo (App.jsx:165) y la liquidación de cuentas
    (PendingOrdersPage.jsx:469) rompen el flujo web; EL COBRO WEB ES INOPERABLE en HEAD
    e89100f.
  - Por qué escapó: oxlint (plugins react) no detecta identificadores libres (no-undef
    inefectivo) y Rollup tampoco resuelve referencias libres; ninguna de las 71 pruebas
    monta el CheckoutModal.
  - Fix: añadir useRef al import (1 línea) + prueba de smoke de montaje + limpiar uso.
  - Hogar del bug: es exactamente el WEB-1 que la Rev. 8.1 certificó como "Idempotency-Key
    estable por intento".

--------------------------------------------------------------------------------
1. CONTRASTACIÓN REV. 8.1 EN ESTE MÓDULO (CAMBIOS DE ESTADO CLAVE)
--------------------------------------------------------------------------------
[8W-CR2] ⬇️ CRÍTICO → MITIGADO (descenso de severidad; la Rev. 8.1 SÍ lo resolvió en lo
  esencial): el JWT ya NO se persiste en localStorage para el SPA.
  - AuthController.cs:125-160: para plataforma Web Token = null (:159); se fija cookie
    httpOnly pos_jwt (SameSite=Strict, Secure sólo localhost/HTTPS, 120 min).
    AuthContext.jsx:37-38 elimina pos_token y guarda solo pos_user; :56-57 logout limpia;
    Program.cs (JwtBearer OnMessageReceived) autentica leyendo la cookie.
    api.js hace requests con credentials: 'include'.
  - RESIDUALES (mantener): (a) api.js:159-167 conserva código muerto Bearer/localStorage;
    (b) pos_user (cédula, nombre, rol) PERSISTE PII en localStorage; (c) setCustomBaseUrl
    (api.js:121-147) sin revalidar isAllowedApiHost; (d) en LAN por HTTP la cookie no es
    Secure.

[8W-C1] ⚠️ ALTO → MEDIO (atenuado, no cerrado): CORS con gating de puertos RFC-1918
  (Program.cs:270-283) pero localhost sigue aceptando CUALQUIER puerto (:267-268) +
  AllowCredentials (:291). Con session en cookie httpOnly SameSite=Strict, una página
  maliciosa servida en http://localhost:yyyy sigue siendo same-site → el navegador envía
  pos_jwt y CORS permite leer la respuesta (vector same-site localhost).

[LINT-WEB] ⚠️ SALIDA 0/0 VERDADERA, PERO FALSO SENTIDO DE SEGURIDAD: no detectó [8.2-CR1].
  Endurecer con no-undef/no-undef react y AÑADIR prueba de montaje del checkout.

[WEB-1] ❌ ROTO POR SU PROPIO COMMIT: la Idempotency-Key se implementó (CheckoutModal.jsx:27-33,
  154,164; salesApi.js:115-127; backend exige en /complete) pero el componente no monta
  (8.2-CR1). Adicionalmente la clave se REUTILIZA al "Descartar y Salir" sin reset
  (CheckoutModal.jsx:332-336) → no es "por intento" (duplicado de cobro posible).

[GOD-WEB-1] ✅ CONFIRMADO: BarcodeScannerModal → BarcodeScannerHud.jsx (RAF cancelado) +
  BarcodeScannerControls.jsx (puro). Matices: memo inefectivo (callbacks inline del padre),
  timers recursivos sin handle limpiable, useCameraHardwareLifecycle.js huérfano.

--------------------------------------------------------------------------------
2. NUEVOS HALLAZGOS REV. 8.2 (Web.Frontend) + RATIFICACIONES 8.0
--------------------------------------------------------------------------------
[8.2-CR1] CRÍTICO — Ver sección 0.

[8W-ZT] MEDIO-ALTO (VIGENTE, HOY MASCARRADO por 8.2-CR1) — CheckoutModal.jsx:87-90 calcula
  el redondeo/vuelto por UI (float) sin preview canónico del backend; riesgo de divergencia
  de centavos en pagos mixtos. Fix: endpoint /checkout/preview.

[8.2-M6] MEDIO (NUEVO) — hold y abonos SIN Idempotency-Key.
  - salesApi.js:81-90 (holdSale) y :95-97 (addPaymentToHoldSale) no envían clave; el backend
    solo exige en /complete (SalesController.cs:341-345). Doble clic/reintento duplica abonos.
  - Fix: UUID v4 por operación + header (con Backend 04).

[8.2-M7] MEDIO (NUEVO) — Race conditions en CartContext.
  - CartContext.jsx:172-200 (addItem) y :203-241 (updateQuantity): respuestas out-of-order
    sobrescriben el estado (backend aplicó, UI muestra datos viejos); en dev StrictMode puede
    duplicar POST /sales/start. Fix: secuenciar/abortar stale mediante request id.

[8.2-M8] MEDIO (NUEVO) — El importe digitado por el operador se SOBRESCRIBE con updates en vivo.
  - PaymentForm.jsx:22-28 (dep [remainingBsS, isCashSelected]) y HoldSaleModal.jsx:54-78
    (dep objeto inline de App.jsx:177): al llegar una tasa de SignalR, el monto que el
    operador está tecleando se resetea. Fix: dependencias estables + estado local persistente.

[8.2-B6] BAJO (NUEVO) — Modales sin focus trap/role=dialog: Modal.jsx:17-35; ConfirmModal
  sin gestión de foco; doble registro de Escape. Alinear con a11y.

[8.2-B7] BAJO (NUEVO) — window.confirm/alert residuales pese a ConfirmModal: PosPage.jsx:90,
  SummaryPanel.jsx:26, PendingOrdersPage.jsx:526, SettingsPage.jsx:187, PaymentForm.jsx:74.

[8W-M1] MEDIO (VIGENTE) — window.confirm/alert bloqueantes en flujos de caja (ver 8.2-B7).

[8W-M2] MEDIO (VIGENTE) — Mismatch de trap de escáner: SearchBar.jsx:104 vs useScannerTrap.js:33
  (caminos de wedge con caracter 40 ms → SKUs truncados sin feedback); unificar guardián.

[8W-B1] BAJO (VIGENTE) — Hotkeys sin gating de foco en modales abiertos (usePosHotkeys sin
  locus de modal).

[8W-B2] BAJO (VIGENTE) — base-uri/form-action/CSP mejorables; subir a meta http-equiv +
  'strict-dynamic' para PWA.

[8W-B3] BAJO (VIGENTE) — PWA/Workbox puede cachear /api/* con tokens; asegurar routing DB.

[8.2-CR2] cross — N1: la SPA no participa directamente, pero el repo expone secretos
  (cross 01).

--------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- Suite Vitest 71/71 en 875 ms (api, barcode, prioridad, hotkeys, modales).
- Doble motor de escaneo (BarcodeDetector + ZXing) + vision enhancer.
- Ticker SignalR con fallback polling; formato monetario en vivo.
- Migración de sesión a cookie httpOnly completada (8W-CR2 → mitigado).
- Componentes memeizables BarcodeScannerHud/Controls (GOD-WEB-1).
- ⚠️ DEUDA CRÍTICA: la COBERTURA no monta componentes de cobro → [8.2-CR1] pasó la CI.

--------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (Web.Frontend) — REV. 8.2
--------------------------------------------------------------------------------
1. [8.2-CR1] Importar useRef en CheckoutModal + prueba de smoke de montaje (imprescindible).
2. [LINT-WEB] Endurecer oxlint/ESLint: no-undef/no-undef, react-hooks/exhaustive-deps y CI
   con montaje (jsdom) del checkout.
3. [8W-CR2 residual] Limpiar código muerto Bearer en api.js; minimizar PII de pos_user;
   revalidar setCustomBaseUrl.
4. [8W-C1] Endurecer CORS/whitelist de puertos localhost (con Backend).
5. [8.2-M6] Idempotency-Key también en hold/abonos.
6. [8.2-M7/M8] Secuenciar cart requests y estabilizar deps de formularios de abono.
7. [8W-ZT] Endpoint preview de checkout (importes canónicos).
8. [8.2-B6/B7, 8W-M1/M2] Focus trap + modales in-app + guardián único de escaneo.
9. [8W-B1/B2/B3] Gating de hotkeys, CSP meta, service worker fuera de /api.
================================================================================
5. ADENDA DE REMEDIACIONES EJECUTADAS (POST-REVISIÓN 8.2)
================================================================================
Fecha: 2026-09-06
- [8.2-CR1] RESUELTO: Se importó `useRef` en `CheckoutModal.jsx`, se corrigió el reset
  de `checkoutKeyRef.current = null` al descartar la venta, y se añadió la prueba unitaria
  de montaje `CheckoutModal.test.js`. El cobro web se encuentra 100% operativo.
- [8W-ZT] RESUELTO: Integración con endpoint canónico `/api/sales/checkout-preview` en `CheckoutModal.jsx`.
- [8.2-M6] RESUELTO: Header `Idempotency-Key` transmitido en `holdSale` y `addPaymentToHoldSale` en `salesApi.js`.
- [8.2-M7] RESUELTO: Secuenciación de peticiones con `cartRequestIdRef` e `inFlightCreateRef` en `CartContext.jsx`.
- [8.2-M8] RESUELTO: Preservación de montos digitados por el operador sin sobreescritura reactiva por SignalR.
- [8.2-B6] RESUELTO: Accesibilidad completa en modales (`role="dialog"`, `aria-modal="true"`, focus trap en Tab y Escape) en `Modal.jsx` y `ConfirmModal.jsx`.
- [8.2-B7 / 8W-M1] RESUELTO: Erradicación del 100% de llamadas nativas a `window.confirm` y `alert` en la interfaz web (`PosPage`, `SummaryPanel`, `PendingOrdersPage`, `SettingsPage`, `PaymentForm`).
- [8W-M2] RESUELTO: Sincronización del escáner de código de barras físico con `pos-search-input` y `data-scanner-trap="true"` en `SearchBar.jsx`.
- Pruebas Vitest: 73 / 73 superadas (0 fallos).
- Linter oxlint: 0 errores, 0 advertencias sobre 92 archivos.

================================================================================
FIN DEL REPORTE Web.Frontend — REV. 8.3 (100% REMEDIADO)
================================================================================