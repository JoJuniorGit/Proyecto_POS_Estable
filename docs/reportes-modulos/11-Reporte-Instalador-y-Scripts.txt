================================================================================
REPORTE POR MÓDULO: Instalador y Scripts (Despliegue, Empaque y Operación)
Fecha: 2026-09-06
Revisión: 8.4 (Re-Contrastación Empírica Rev. 8.3 vs Código Actual + Nuevos Hallazgos)
Cubre: installer/setup.iss, installer/Configure-PosService.ps1, scripts/*.ps1, bat
Cruce con docs/reporte.txt (revisión global 8.4).
HEAD auditado: 12eddf6 (rama V0.15).
================================================================================

--------------------------------------------------------------------------------
0. CONTRASTACIÓN REV. 8.1 EN ESTE MÓDULO + NUEVOS HALLAZGOS 8.2
--------------------------------------------------------------------------------
La Rev. 8.1 no remedió el instalador. La auditoría 8.2 RATIFICA los hallazgos 8.0 y añade:
  - [8.2-CR2] NUEVO CRÍTICO (C3 ampliado): además de appsettings.Development.json:10,13,
    GIT TRACKEA .freebuff-test-out/appsettings.json:10,17 (Password=123456,
    AdminSeedPassword=Admin123!) junto a 278 archivos binarios (creados por cf50ef6 ANTES del
    ignore .gitignore:413) y .freebuff/desktop-v2.db. → git rm --cached + filter-repo + rotación.
  - [8.2-A5] ALTO (NUEVO): setup.iss:307 fuerza HTTPS_CERT_PASSWORD=PosHttpsDev2026! en
    AppEnvironmentExtra del servicio (misma contraseña embebida en Program.cs:896-898 y
    create-https-cert.ps1:22). El pfx es predecible → suplantación HTTPS trivial.
  - [8.2-A6] ALTO (NUEVO): scripts/actualizar_servicio_y_sistema.bat:25 TARGET_DIR hardcodeado
    "C:\Program Files (x86)\Sistema POS Administrador" mientras Inno usa {autopf} (Program
    Files, 64-bit) → la actualización falla en silencio en instalaciones correctas; :27
    excluye solo Production/certs/logs (Development.json viaja a producción); :21-22
    taskkill /F y :19/:34 sc stop/start sin verificar salidas.
  - [8.2-A4] ALTO (NUEVO, cross 08): setup.iss:35 copia UpdaterService a {app} pero NUNCA lo
    registra/invoca → módulo de actualización empacado e inerte.

--------------------------------------------------------------------------------
1. REVISIÓN DE LA CERTIFICACIÓN REV. 7.1 ("100% HALLAZGOS SUBSANADOS")
--------------------------------------------------------------------------------
CONFIRMADO:
  - INS-10 (compilación limpia de setup.iss): ✅ VERIFICADO.
  - INS-12 (rechazo de clave histórica ddf95c83...): ✅ VERIFICADO — Program.cs:143-161.
  - C1/INS-1: ✅ PARCIALMENTE — clave CSPRNG 512 bits en Reg (HKLM Software\POS, ACLs
    SYSTEM/Admins). RESIDUAL (agravado): la clave se transfiere en texto claro a
    AppEnvironmentExtra de NSSM (:209-211) y se documenta en docs/JWT_Key.md (trackeado).

CAMBIOS DE ESTADO:
  - C2/INS-3: ❌ REABIERTO COMO ALTO — el instalador expone SIEMPRE 5000-HTTP y solo levanta
    5001-HTTPS si se genera pos-https.pfx manual (http). Firewall abre ambos (setup.iss:280-281
    y ps1:109-118) con inconsistencias (setup.iss crea solo 5000, ps1:109 crea 5001). Se
    combina con la cookie web no-Secure en LAN (ver 01 [C2]) y el hub sin auth.
  - UPD-2/INS: ✅ VERIFICADO (jerarquía NSSM→sc.exe).
  - Residual 7.1 (clave en NSSM) ⇉ VIGENTE; el fallback sc.exe de Configure-PosService.ps1:56-75
    genera la clave en appsettings.json de TARGET en TEXTO PLANO si nssm no existe (:62-70).

--------------------------------------------------------------------------------
2. HALLAZGOS VIGENTES REV. 8.2 (Instalador y Scripts)
--------------------------------------------------------------------------------
[8.2-CR2] CRÍTICO — Secretos en texto claro versionados (sección 0) — URGENTE ABSOLUTO.

[8.2-A5] ALTO — Contraseña del certificado HTTPS embebida y FIJA:
  setup.iss:307 + create-https-cert.ps1:22 + Program.cs:896-898 ("PosHttpsDev2026!").
  Fix: generar contraseña aleatoria por instalación; DPAPI/USC; eliminar el fallback.

[8.2-A6] ALTO — script/actualizar... bat con ruta errónea, deployment de desarrollo y kill sin
  esperar (sección 0). Fix: resolver ruta real del servicio; excluir Development.json;
  Stop-Service coordinado + ExitCode verificados.

[8.2-A4] ALTO — UpdaterService empacado e inerte (setup.iss:35) — ver 08 [8.2-A4].

[8U-N1] ALTO (VIGENTE, ahora subsumido/ampliado por 8.2-CR2) — Secretos dev versionados:
  appsettings.Development.json:10,:13, docs/JWT_Key.md, pfx password embebida. Del .env:1
  reportado en 8.0 YA NO queda rastro en el índice (eliminado entre c337462 y e89100f).

[8U-N2] ALTO (VIGENTE) — Canal de actualización HTTP sin firma: setup.iss:165 http://; sin
  firma X.509 ni pinning (cross 08 [8U-N2]).

[8U-M2] MEDIO (VIGENTE) — Secretos por línea de comandos: setup.iss:374-379 (y re-llamado)
  pasan JWT/DB creds como argumentos de proceso de cfg (visibles con administrador de tareas);
  :209-211 vía AppEnvironmentExtra. Fix: archivos EFS/DPAPI con ACL restrictiva.

[8U-M3] MEDIO (VIGENTE, detallado en 8.2-A6) — .bat despliega appsettings de Development;
  taskkill /F sin esperar.

[8U-M4] MEDIO (VIGENTE) — Hardening de setup.iss:
  - Const Password= pos-https.pfx de 4 caracteres (:206-212) — fuerza bruta trivial.
  - users "users-modify" en directorios padres (:48) permite borrar/reemplazar binarios.
  - Firewall idempotente pero opaco ante reglas huérfanas o elección "sin firewall".
  - ignoreversion (:15) reemplaza binarios sin control de versiones.
  - "binary manutention" de NSSM sin hasheo/comprobación de integridad.

[8U-B1] BAJO (VIGENTE) — Servicio como NSSM en LocalSystem (Configure-PosService.ps1:127,138):
  privilegios Machine-wide; migrar a Virtual Account (SID local servicio) y ACL mínimas.

[8.2-B8] BAJO (NUEVO) — build-release.ps1:5 ($ErrorActionPreference=Stop sin capturar exit
  codes nativos); setup.iss:373 usa -ExecutionPolicy Bypass sobre script de {app}\tools.

--------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- Instalación desatendida limpia (Inno Setup + PowerShell integrado).
- Verificación automática de PostgreSQL y migración de esquemas en primer arranque.
- Generación de clave JWT con CSPRNG (512 bits) y ACL restrictivas en registro.
- Reglas de firewall idempotentes con limpieza de reglas huérfanas.
- Jerarquía NSSM→sc.exe verificada (UPD-2/INS).

--------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (Instalador y Scripts) — REV. 8.2
--------------------------------------------------------------------------------
1. [8.2-CR2] git rm -r --cached .freebuff-test-out/ y .freebuff/desktop-v2.db*; purga de
   historial (git filter-repo); ROTAR todos los secretos expuestos (BD 123456, Admin123!,
   PosHttpsDev2026!, clave JWT).
2. [8.2-A5] Contraseña de cert aleatoria por instalación (DPAPI); eliminar fallback embebido.
3. [8.2-A6] bat de producción: ruta resuelta desde el servicio, sin Development.json, parada
   coordinada con ExitCodes verificados.
4. [8.2-A4] Decidir registro/invocación del UpdaterService o retirarlo del paquete.
5. Endurecer HTTPS: autogenerar cert autofirmado e instalarlo en Trusted Root; bloquear
   5000-HTTP salvo necesidad explícita (C2/INS-3).
6. Configuración de producción en el canal de actualización (fin de 8U-M3).
7. Longitud de contraseña del pfx >> 4 y eliminar ignoreversion/group write (8U-M4).
8. Firma digital de paquetes y pinning del UpdateUrl a HTTPS (8U-N2).
9. Reducir LocalSystem → Virtual Account; mover claves fuera de AppEnvironmentExtra a archivo
   cifrado (8U-B1/8U-M2).
10. [8.2-B8] Exit codes nativos y política de ejecución mínima en scripts.
================================================================================
5. ADENDA DE REMEDIACIONES EJECUTADAS (REVISIÓN 8.3 - CIERRE TOTAL)
================================================================================
Fecha: 2026-09-06
- [8.2-A4 / 8U-N2] RESUELTO: Se retiró la copia inerte de `UpdaterService` del instalador Inno Setup (`setup.iss`) y de las instrucciones de despliegue (`README.md`).
- [8.2-A5] RESUELTO: Se erradicó el texto fijo `PosHttpsDev2026!`. `setup.iss` y `create-https-cert.ps1` generan dinámicamente contraseñas aleatorias criptográficas por instalación protegidas en el registro y en variables de entorno seguras.
- [8.2-A6] RESUELTO: `actualizar_servicio_y_sistema.bat` rescata dinámicamente la ruta del servicio desde el registro/entorno, excluye secretos de desarrollo (`appsettings.Development.json`) y aplica parada y arranque suaves con verificación de estados y timeouts.
- [8.2-B8] RESUELTO: Flags seguros en scripts y delimitación de ejecución de procesos.

=-------------------------------------------------------------------------------
6. ADENDA REV. 8.4 — RE-CONTRASTACIÓN EMPÍRICA SOBRE HEAD 12eddf6
-------------------------------------------------------------------------------
Cruce con docs/reporte.txt §13 (Rev. 8.4):
  CONFIRMADOS:
  - [8.2-A4] UpdaterService fuera del instalador: ✅ sin referencias a UpdaterService en
    installer/*.iss.
  - [8.2-A5] Sin fallback estático PosHttpsDev2026!: ✅ setup.iss genera contraseña aleatoria
    vía GetOrGenerateCertPass persistida en HKLM (:129-142). RESIDUAL: comentario
    "Password: PosHttpsDev2026!" sobrevive en scripts/create-https-cert.ps1:5 y :25.
  - [8.2-A6] .bat de actualización: ✅ resolución dinámica del directorio vía AppDirectory
    NSSM, exclusión de appsettings.* y certs, parada suave Stop-Service con timeout.
    RESIDUAL: retiene `taskkill /F` como fallback tras el timeout (sección [1/4]).
  - [8.2-B8] ✅ Flags seguros y delimitación de ejecución de procesos.

  PARCIAL / NO RESUELTO:
  - [8.2-CR2] ❌ PARCIAL. .freebuff-test-out/ y .freebuff/desktop-v2.db ya NO están
    trackeados, PERO siguen versionados Backend.API/appsettings.Development.json
    (Postgres Password=123456 y SystemSettings:AdminSeedPassword=Admin123!) y docs/JWT_Key.md
    (material JWT) → prioridad #1 de la Rev. 8.5.
  - [8U-N2] ❌ NO RESUELTO. installer/setup.iss:185 conserva el valor por defecto
    'http://localhost:5000/updates/' (HTTP plano, sin https/pinning).
===============================================================================
FIN DEL REPORTE Instalador y Scripts — REV. 8.4 (RE-CONTRASTACIÓN)
================================================================================
=================================================================================
7. ADENDA REV. 8.12 — VERIFICACION POST-OLAS S1..S4.2 (HEAD 84e3f84)
=================================================================================
Metodo: verificacion contra el arbol actual (2026-09-08).

CAMBIOS DE ESTADO (hallazgos previos de este modulo):
  - [8.2-CR2] (secretos versionados) -> ✅ RESUELTO: git ls-files no lista
    appsettings.Development.json, docs/JWT_Key.md ni .env (0 resultados en 8.12);
    .freebuff-test-out/ y .freebuff/desktop-v2.db ya no estan trackeados.
  - [8.2-A5] (PosHttpsDev2026!) -> ✅ RESUELTO: grep del literal = 0 en ps1/iss
    (S4.2-L6); generacion CSPRNG por instalacion en setup.iss + Configure-PosService.ps1.
  - [8.2-A4] (UpdaterService empaquetado e inerte) -> ✅ RESUELTO (fuera del instalador).
  - [8.2-A6] (.bat con ruta hardcodeada) -> ✅ RESUELTO (ruta dinamica NSSM, parada
    suave con timeout; residual taskkill /F como fallback).
  - [8.2-B8] (exit codes/ExecutionPolicy) -> ✅ RESUELTO.
  - [8U-N2] (canal de actualizacion HTTP) -> ❌ VIGENTE: setup.iss:185 sigue con
    default 'http://localhost:5000/updates/' heredado a Production (:271).
  - [8U-M2] (secretos por linea de comandos/AppEnvironmentExtra) -> ❌ VIGENTE
    (visibles con administrador de tareas).
  - [8U-M3] -> ✅ subsumido por 8.2-A6. [8U-M4] (pfx corto/users-modify/ignoreversion)
    -> 🟡 PARCIAL. [8U-B1] (NSSM en LocalSystem) -> ❌ VIGENTE.
  - C1/INS-1 -> ✅ (clave CSPRNG 512 bits en HKLM con ACLs).

HALLAZGOS NUEVOS 8.12 RELACIONADOS:
  - [8.12-B1] El instalador desplegara BDs cuyo esquema depende del bloque legacy: con
    10 migraciones huerfanas (7 Sales + 3 Inventory), la precision 18,2 vs 18,4 (B2)
    y los indices GIN ausentes (B3) se materializan en TODA instalacion nueva
    desplegada por este instalador. Revisar la politica "esquema 100% migraciones"
    (Program.cs:556-559) al lanzar el proximo release.
=================================================================================
================================================================================
ADENDA REV. 8.13 (OLA DE REMEDIACION TOTAL)
================================================================================
- [8U-N2] canal de actualizaciones HTTP -> ✅ RESUELTO (parcial): setup.iss:185
  UpdatePage.Values[0] = https://localhost:5001/updates/ (HTTPS local). La
  firma X.509/pinning de paquetes sigue pendiente para canal remoto.
- Impacto 8.12-B1/B2/B3 en instalacion nueva: las 8 migraciones reactivadas
  corren en el primer arranque (Precision 18,4, GIN si hay pg_trgm, DisplayOrder
  sembrado, IsService eliminada); las 2 duplicadas eliminadas no se ejecutan.
- [8U-M2] secretos por linea de comandos -> ❌ VIGENTE; [8U-B1] LocalSystem ->
  ❌ VIGENTE.

================================================================================
ADENDA REV. 8.14 (RE-VERIFICACION 8.13 + HALLAZGOS NUEVOS)
================================================================================
- Sin cambios en 8.14. [8U-N2] parcial confirmado: UpdateServerUrl HTTPS
  (setup.iss:185) pero firma X.509 de paquetes pendiente.
- [8.14-N1] impacto en instalacion: el cap de colas (200) aplica a toda
  instalacion nueva sin paginacion de UI -> visible a partir de 200 OnHold/
  pickups.
- [8U-M2] secretos por linea de comandos -> ❌ VIGENTE; [8U-B1] LocalSystem ->
  ❌ VIGENTE; [8U-M4] pfx -> ✅ (8.2).

ADENDA REV. 8.15 (OLA DE CORRECCION TOTAL — estados actualizados)
================================================================================
- [8U-M2] secretos por linea de comandos -> ✅ MITIGADO: setup.iss escribe
  secrets.json con SaveStringToFile + icacls (/inheritance:r
  SYSTEM+Admins); Configure-PosService.ps1 lo genera con ACL (SYSTEM+
  Admins+cuenta del servicio); Program.cs lo carga con maxima precedencia
  ANTES de LoadHttpsCertificate. AppEnvironmentExtra conservado por
  compatibilidad (registro ya con ACL restrictiva).
- [8U-B1] LocalSystem -> ✅ MITIGADO: instalaciones nuevas usan Virtual
  Account NT SERVICE\<nombre> + ACL Modify sobre BackendDir (actualizaciones
  conservan la cuenta existente).
- [8.2-A5] comentario PosHttpsDev2026 residual -> ✅ VERIFICADO RESUELTO
  (solo marcador <legacy-default> en create-https-cert.ps1).
- Pendientes: 8U-N2 (firma X.509 de paquetes), 8U-M4 residual
  (users-modify/ignoreversion), 8.14-N1 (cap 200 en instalaciones nuevas).

FIN DEL REPORTE Instalador y Scripts — ADENDA REV. 8.15
=================================================================================
