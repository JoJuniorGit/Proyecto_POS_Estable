================================================================================
REPORTE POR MÓDULO: UpdaterService (Servicio Windows de actualización de binarios)
Fecha: 2026-09-06
Revisión: 8.2 (Contrastación Empírica Rev. 8.1 vs Código Actual + Nuevos Hallazgos)
Cruce con docs/reporte.txt (revisión global 8.2).
HEAD auditado: e89100f (rama V0.15).
================================================================================

--------------------------------------------------------------------------------
0. ESTADO EN REV. 8.2 (HALLAZGOS 8.0 RATIFICADOS + NUEVO HALLAZGO CRÍTICO FUNCIONAL)
--------------------------------------------------------------------------------
La Rev. 8.1 no remedió este módulo. La auditoría 8.2 RATIFICA 8U-N2/8U-N5 y AÑADE el
hallazgo ORFANDAD (el servicio jamás se ejecuta en producción):

[8.2-A4] ALTO (NUEVO) — UpdaterService es un HUÉRFANO: se EMPAQUETA pero NUNCA se invoca.
  - installer/setup.iss:35 copia los binarios a {app}\UpdaterService, pero NO registra el
    servicio en NSSM/sc.exe ni crea tarea programada ni lo llama (ningún componente invoca
    sus binarios en el flujo normal). El "auto-update" real del cajero lo hace el Desktop
    abriendo updateServerUrl en el navegador (VersionLockoutViewModel.cs:43-47, cross 06
    [8.2-A4]).
  - Impacto: ~20+ MB embebidos en el instalador inútiles, el único "canal" de actualización
    efectiva es la copia manual/HTTP, y toda la supuesta "verificación de firma/hash" del
    servicio no protege NADA en la práctica.
  - Fix: o se integra de verdad (registro + invocación + firma), o se retira del empaquetado
    y se documenta.

--------------------------------------------------------------------------------
1. CONTRASTACIÓN DE HALLAZGOS PREVIOS (REV. 7.1 vs CÓDIGO ACTUAL)
--------------------------------------------------------------------------------
REVISIÓN 1 — [UPD-1] ⚠️ FAIL-OPEN CONFIRMADO (VIGENTE).
  - UpdaterService/Program.cs:33-56: verificación de hash condicional (if providedSha !=
    computed) que NO es bloqueante y al final apenas Devuelve; NUNCA detiene servicios si la
    verificación falla; el sidecar se lanza condicionalmente (:37) con flag PIN/URL no
    autenticado → continúa con paquete http:// sin firmar (cross 11 [8U-N2]).

REVISIÓN 2 — [UPD-2] ✅ VERIFICADO para resolución/path ("0 advertencias"), PERO la
  orquestación de producción (SetupUpdaterPackage con sidecar sin firma) sigue abierta.

REVISIÓN 3 — [SEC-01] ✅ VERIFICADO (validación canónica de rutas, bloqueo de symlinks/
  reparse points, preserva appsettings.Production.json). Atenuante: extracción puede caerse
  silenciosamente (ver [8U-M1]).

--------------------------------------------------------------------------------
2. HALLAZGOS VIGENTES REV. 8.2 (UpdaterService)
--------------------------------------------------------------------------------
[8.2-A4] ALTO — Orfandad del servicio (nuevo, sección 0).

[8U-N5] ALTO — Fail-open de actualización (VIGENTE).
  - Program.cs:33-56, :78-81 ("completed successfully"), :137-157 (traga exit codes;
    :147-148 stdout/stderr redirigidos sin leer → riesgo de deadlock). Decisión FAIL-CLOSED
    ausente; sin verificación de firma X.509.

[8U-N2] ALTO — Canal HTTP SIN TLS/firma (VIGENTE).
  - setup.iss:165 http://; el cliente puede ser relanzado con updateServerUrl no canónica
    (cross 01 [8B-B7], 06 [8.2-A4]).

[8U-M1] MEDIO (VIGENTE) — Errores tragados y reinicio incondicionado:
  - catch nulos/omitidos; sin registro en Windows Event Log; sin feedback/auditoría al
    backend del resultado de la actualización.

[8U-B1] BAJO (VIGENTE) — No usa Windows Event Log (EventLog) ni ETL para los
  reemplazos/veredictos.

--------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- Secuencia SPLIT (programa VE del servicio) → extracción → reinicio en 4 niveles.
- Validación canónica de ZIP y preservación inmutable de appsettings.Production.json.
- Intención correcta de no detener el servicio si el paquete está dañado; el defecto es que
  SÍ reinicia cuando falla la verificación (fail-open).
- ⚠️ FORTALEZA DELETEADA: un servicio de actualización que NUNCA se ejecuta en producción no
  aporta verificación alguna al flujo real (ver [8.2-A4]).

--------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (UpdaterService) — REV. 8.2
--------------------------------------------------------------------------------
1. [8.2-A4] DECIDIR: registrar e invocar el servicio en producción + firma X.509 del paquete,
   o retirarlo del instalador. (Bloquea el resto del plan.)
2. Fail-closed: no tocar binarios sin firma/hash válido + registro de auditoría (Event Log).
3. Exigir https:// y cert de origen en UpdateUrl (eliminar HTTP).
4. Autenticar/autorizar la invocación del sidecar (token de máquina/domain join).
5. Registrar eventos en Event Log (inicio/verificación/éxito/fallo).
6. Añadir rollback del paquete anterior si la actualización falla a mitad.
7. Cruzar con 11-Reporte-Instalador (cobertura setup.iss:35,307 y NSSM).
================================================================================
FIN DEL REPORTE UpdaterService — REV. 8.2
================================================================================