================================================================================
REPORTE POR MÓDULO: Sales.Module (ventas, pagos, caja, cierres diarios, idempotencia)
Fecha: 2026-09-05
Revisión: 8.0 (Contrastación Empírica Rev. 7.0 vs Código Actual + Nuevos Hallazgos)
Cruce con docs/reporte.txt (revisión global 8.0).
================================================================================

--------------------------------------------------------------------------------
1. CONTRASTACIÓN DE HALLAZGOS PREVIOS (REV. 7.0 vs CÓDIGO ACTUAL)
--------------------------------------------------------------------------------
HALLAZGOS VERIFICADOS COMO CORRECTOS EN EL ÁRBOL ACTUAL:
  - H-SAL-1 / A2 (Numeración de facturas): ✅ VERIFICADO — nextval('factura_number_seq') en
    SalesService.cs:56 + setval en Program.cs:508-509.
  - H-SAL-2 / A1 (Atomicidad venta-stock): ✅ en la ruta principal — SalesService.cs:369-377,
    EnrollInTransactionAsync en :376; commit :651-655; rollback :672-689 en catches con
    await using txn.DisposeAsync. ⚠️ GAP en ruta HoldOrders (ver [8I-CR1]).
  - H-SAL-3 / CODE-01 (God Class): ✅ VERIFICADO — SalesService.cs (807 l.) + partials
    .HoldOrders/.Pricing/.CashAdvance/.History.
  - H-SAL-4 (Precios personalizados): ✅ VERIFICADO (gating roles Admin/Manager).
  - H-SAL-13 / W1 (Idempotencia): ✅ VERIFICADO — header obligatorio SalesController.cs:336-341;
    IdempotencyService.cs:64-74 SHA-256/FixedTimeEquals; índice único.
  - W3 / MT-001 (FK PaymentMethod): ⚠️ ESTADO DIFERENTE AL REPORTADO.
    * El retiro de MT-001 está confirmado (docs/medidas-temporales.txt:16;
      PaymentMethodService.cs:204 verifica hasCashTransactions).
    * PERO CashTransaction.cs:63: PaymentMethodId es int? NULLABLE (NO requerida), y la
      migración 20260905180000 crea la columna nullable con FK Restrict. El reporte 7.0
      lo daba como columna obligatoria (citaba CashTransaction.cs:29).
    * SaleItem NO tiene PaymentMethodId; la heurística ct.IsPhysicalCash se mantiene.
  - W4 (Auditoría purga idempotencia): ✅ VERIFICADO (IdempotencyCleanupJob.cs:107-108).
  - H-SAL-... / B5 (Stock negativo): ✅ VERIFICADO — SalesService.cs:603-606 allowNegativeStock:false.
  - ARQ-01 (Arqueo con inactivos): ✅ VERIFICADO — DailyClosureService.cs:62
    Where(p => p.IsActive || salesTotals.ContainsKey(p.Id)).
  - hERR (Cierres ProgramData): ✅ VERIFICADO — DailyClosureService.cs:217-267 con ruta
    CommonApplicationData/CommandCenterPOS/Closures.
  - PERF-02 (AsSplitQuery): ✅ VERIFICADO — Program.cs:100-101 (SplitQuery por defecto en
    SalesDbContext) + SalesService.cs:122, History.cs, Pricing.cs, HoldOrders.cs:492-499.
  - PERF-06 (AsNoTracking): ✅ en CashDrawerService.cs:227 (GetHistoryAsync).
  - Inmutabilidad histórica: ✅ VERIFICADO — Pricing.cs:17-18 bloquea Completed en
    UpdateExchangeRateAsync; RecalculateOnlyOnHold restringido a OnHold.
  - ERR-04 (DailyClosure/Shifts): ✅ en ambos — ya sin ex.Message. NOTA: ExchangeRateController
    (módulo Backend.API) SIGUE filtrando Detail = ex.Message (ver 01 [8B-M2]).

--------------------------------------------------------------------------------
2. NUEVOS HALLAZGOS REV. 8.0 (Sales.Module)
--------------------------------------------------------------------------------
[8I-CR1] ALTO — Ruta "pago 100%" de HoldSale NO atómica con inventario.
  - SalesService.HoldOrders.cs:168-218: txn solo en SalesDbContext (:171); deducción con
    UpdateStockBatchAsync (:215) SIN EnrollInTransactionAsync → Inventory abre/confirma su
    propia transacción. Si falla el commit de la venta, el stock queda descontado sin venta.
  - Fix: EnrollInTransactionAsync antes de UpdateStockBatchAsync (patrón :373-377) o método
    único "completar venta + stock + outbox".

[8S-M1] MEDIO — GetActiveSessionAsync carga TODA la sesión de caja en cada venta.
  - CashDrawerService.cs:30-32: Include(Transactions).ThenInclude(Sale) SIN AsSplitQuery ni
    AsNoTracking, carga infinitas transacciones del turno en cada CompleteSaleAsync con códigos
    de venta secuenciales.
  - Fix: proyección mínima (Id, Status, tasas) no rastreable; historial por paginación.

[8S-M2] MEDIO — Deriva de centavo en flujos hold/abonos.
  - SalesService.cs:156 (RecalculateTotalAsync): SubtotalBsS = item.Subtotal * AppliedRate SIN
    redondear; persistido a decimal(18,4) (SalesDbContext.cs:114) mientras TotalBsS es decimal(18,2).
  - HoldOrders.cs:125: AmountBsS = payment.AmountBsS tampoco se redondea. Divergencia fila-vs-total
    en abonos iniciales mixtos.
  - Fix: redondear SubtotalBsS/AmountBsS a 2 decimales AwayFromZero en ambos puntos.

[8S-M3] MEDIO — Sin guard contra doble deducción en flujos hold/crédito.
  - No existe IsDelivered/DeliverOnCredit en el código (solo en la skill y docs); los ítems
    "sin resolver" de docs/OPtimizacion.txt (líneas 706 y 883) siguen abiertos. Los ítems de
    ventas en espera pueden deducirse dos veces si se confirma y después se completa.
  - Fix: implementar IsDelivered en el ítem y guard previo a la deducción (SalesService.cs:603).

[8S-M4] BAJO — NotFound confuso: SalesController.GetSaleById o flujos de hold pueden llegar a
  estados inválidos sin validar pre-Condiciones (por ejemplo, operar sobre una venta ya Completed).

[8S-M5] BAJO — Compensación del outbox frágil: el catch de SalesController.cs:408 filtra por
  nombre IX_IdempotentRequests que coincide por substring con
  IX_IdempotentRequests_Key_RequestPath (funciona pero es frágil frente a renombrados).

[8S-M6] MEDIO — Concurrency tokens xmin ausentes en Sale, CashTransaction y CashDrawerSession
  (cross-ref 02 [8C-M2]): cierres de caja y arqueos concurrentes sin protección de versión de fila.

--------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS)
--------------------------------------------------------------------------------
- Doble capa de persistencia: venta y outbox en la misma transacción ADO.NET (ruta principal).
- AsSplitQuery estándar para evitar explosión cartesiana.
- Soporte multimoneda con PricingCalculator (AwayFromZero, RoundExchangeRateCeiling).
- Auditoría forense y respaldos PDF/texto para arqueos y cierres.
- Idempotencia criptográfica con comparación constante.

--------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (Sales.Module)
--------------------------------------------------------------------------------
1. [8I-CR1] EnrollInTransactionAsync en HoldOrders 100% (crítico de integridad).
2. [8S-M3] Implementar IsDelivered + guard anti doble deducción.
3. [8S-M2] Redondear SubtotalBsS/AmountBsS a 2 decimales.
4. [8S-M1] Optimizar GetActiveSessionAsync con proyección mínima AsNoTracking/AsSplitQuery.
5. [W3] Decidir si PaymentMethodId debe ser NOT NULL (endurecer FK) o documentar por qué es nullable.
6. [8S-M6] Añadir tokens xmin a Sale/CashTransaction/CashDrawerSession.
================================================================================
FIN DEL REPORTE Sales.Module — REV. 8.0
================================================================================