================================================================================
REPORTE POR MÓDULO: Sales.Module (ventas, pagos, caja, cierres diarios, idempotencia)
Fecha: 2026-09-06
Revisión: 8.4 (Re-Contrastación Empírica Rev. 8.3 vs Código Actual + Nuevos Hallazgos)
Cruce con docs/reporte.txt (revisión global 8.4).
HEAD auditado: 12eddf6 (rama V0.15).
Métricas ejecutadas en esta auditoría: 677/677 .NET + 73/73 web (750/750), 0 errores.
================================================================================

--------------------------------------------------------------------------------
0. CONTRASTACIÓN REV. 8.1 EN ESTE MÓDULO
--------------------------------------------------------------------------------
  - DST-1 (deducción única): ✅ CONFIRMADO — SaleMadeEvent.cs:7-11 añade InvoiceNumber;
    SalesService.cs:662 y HoldOrders.cs:264 lo publican; InventorySaleMadeEventHandler.cs:40-55
    verifica Reason == "Sale #{SaleId}" O "Sale #{InvoiceNumber}". ⚠️ DEBILIDAD: el handler
    no filtra IsCashAdvance (ver [8.2-A3] en 03) → hard breach de la red de seguridad con
    ítems ADV. Además, el snapshot del evento (SalesService.cs:661) incluye TODOS los ítems
    mientras la deducción principal los excluye (:590-593).
  - 8S-M1 (GetActiveSessionAsync): ⚠️ ATENUADO — la carga completa sigue (CashDrawerService.cs:30-32)
    pero el SplitQuery default (Program.cs:103) reduce el cartesiano. Seguir con proyección mínima.
  - [8I-CR1]/[8S-M2]/[8S-M3]/[8S-M5]/[8S-M6]: VIGENTES (ratificados abajo).

--------------------------------------------------------------------------------
1. CONTRASTACIÓN DE HALLAZGOS PREVIOS (REV. 7.0 vs CÓDIGO ACTUAL)
--------------------------------------------------------------------------------
  - H-SAL-1 / A2 (Numeración de facturas): ✅ VERIFICADO — nextval en SalesService.cs:56 +
    setval en Program.cs:508-509.
  - H-SAL-2 / A1 (Atomicidad venta-stock): ✅ ruta principal — SalesService.cs:369-377,
    EnrollInTransactionAsync :376; commit :651-655; rollback :672-689. ⚠️ GAP HoldOrders (8I-CR1).
  - H-SAL-3 / CODE-01 (God Class): ✅ VERIFICADO — SalesService.cs (807 l.) + partials.
  - H-SAL-4 (Precios personalizados): ✅ VERIFICADO (gating Admin/Manager).
  - H-SAL-13 / W1 (Idempotencia): ✅ VERIFICADO — header obligatorio en /complete
    (SalesController.cs:341-345); IdempotencyService.cs:64-74 SHA-256/FixedTimeEquals; índice único.
  - W3 / MT-001 (FK PaymentMethod): ⚠️ PERSISTE — CashTransaction.cs:63 PaymentMethodId es
    int? NULLABLE (migración 20260905180000 con FK Restrict); la heurística ct.IsPhysicalCash
    se mantiene. Decisión pendiente de NOT NULL.
  - W4 (Auditoría purga idempotencia): ✅ VERIFICADO (IdempotencyCleanupJob.cs:107-108).
  - W5 / B5 (Stock negativo): ✅ VERIFICADO — SalesService.cs:603-606 allowNegativeStock:false.
  - ARQ-01 (Arqueo con inactivos): ✅ VERIFICADO — DailyClosureService.cs:62.
  - hERR (Cierres ProgramData): ✅ VERIFICADO — DailyClosureService.cs:217-267.
  - PERF-02 (AsSplitQuery): ✅ VERIFICADO — Program.cs:100-101 (default en SalesDbContext).
  - PERF-06 (AsNoTracking): ✅ en CashDrawerService.cs:227.
  - Inmutabilidad histórica: ✅ VERIFICADO — Pricing.cs:17-18.
  - ERR-04 (DailyClosure/Shifts): ✅ en ambos (sin ex.Message); ExchangeRateController sigue
    filtrando (cross 01 [8B-M2]).

--------------------------------------------------------------------------------
2. NUEVOS HALLAZGOS REV. 8.2 (Sales.Module) + RATIFICACIONES 8.0
--------------------------------------------------------------------------------
[8.2-A2] ALTO (NUEVO) — Carreras de doble cierre/apertura. Sin exclusividad, doble conteo.
  - DailyClosureController.cs:123-154 y ShiftsController.cs:145-168 computan totales ANTES
    de la transacción (check-then-act); SalesDbContext.cs:207 solo índice simple sobre
    ClosureDate → 2 cierres simultáneos cuentan los mismos pagos 2 veces (doble conteo).
  - CashDrawerService.cs:135-147 RolloverSessionAfterClosureAsync puede dejar 2 sesiones
    abiertas; sin índice único parcial Status='Open' (ver [8.2-M3]).
  - Fix: SERIALIZABLE en el cierre + índice único parcial (ClosureDate) y (UserId)WHERE
    Status=Open + tokens de fila en las entidades (8C-M2).

[8.2-M2] MEDIO (NUEVO) — Fallback de tasa 1.0 en cierres sin registro BCV del día.
  - ShiftsController.cs:48-66 y DailyClosureController.cs:45-63: tasa 1.0 distorsiona todos
    los importes Bs.S del arqueo sin marca visible. Fix: bloquear el cierre o tasa explícita.

[8.2-M3] MEDIO (NUEVO) — Carrera de doble sesión de caja abierta.
  - CashDrawerService.cs:38-55 (GetOrCreateActiveSessionAsync) y :69-83 (OpenSessionAsync):
    check-then-act; sin índice único parcial Status=Open; GetActiveSessionAsync().FirstOrDefaultAsync()
    elige arbitraria si hay 2. Fix: índice único parcial + ExecuteUpdate condicional.

[8.2-M6] MEDIO (NUEVO, cross 09) — Abonos/hold SIN Idempotency-Key.
  - salesApi.js:95-97 (addPaymentToHoldSale) y :81-90 (holdSale) no envían clave; SalesController
    solo la exige en /complete. Doble clic duplica abonos. Fix: exigir clave en abonos/hold.

[8I-CR1] ALTO (VIGENTE) — Ruta "pago 100%" de HoldOrders no atómica.
  - HoldOrders.cs:168-218: txn solo Sales (:171); UpdateStockBatchAsync (:215) SIN
    EnrollInTransactionAsync → stock descontado si falla el commit de venta (:240-244).

[8S-M3] MEDIO (VIGENTE) — Sin guard contra doble deducción en hold/crédito.
  - NO existe IsDelivered/DeliverOnCredit (0 matches); la skill y docs los suponen. Existe
    ConfirmPickupAsync (SalesService.History) que NO publica SaleMadeEvent → si además se
    completa la venta, la deducción ocurre 2 veces. Fix: implementar IsDelivered + guard :603.

[8S-M2] MEDIO (VIGENTE) — Deriva de centavo en flujos hold/abonos.
  - SalesService.cs:156 SubtotalBsS sin redondear; HoldOrders.cs:125 AmountBsS sin redondear;
    persistido decimal(18,4) (:114) vs TotalBsS decimal(18,2).

[8S-M1] MEDIO (ATENUADO 8.2) — GetActiveSessionAsync (CashDrawerService.cs:30-32) carga toda
  la sesión (Include+ThenInclude) en cada venta; SplitQuery default mitiga. Aun así, proyección
  mínima AsNoTracking recomendada.

[8S-M5] BAJO (VIGENTE) — Compensación del outbox frágil: SalesController.cs:408 filtra por
  nombre IX_IdempotentRequests (substring coincide con IX_IdempotentRequests_Key_RequestPath).

[8S-M6] MEDIO (VIGENTE → ver 8.2-A1) — Sin tokens de concurrencia en Sale/CashTransaction/
  CashDrawerSession (el único "token" existente, Product, es bytea inerte). Claves para las
  carreras [8.2-A2/M3].

[8.2-B1] BAJO (NUEVO) — CreateCustomerAsync check-then-act sin índice único CedulaOrRif
  (HoldOrders.cs:600-602) → clientes duplicados concurrentes.

[8.2-B2] BAJO (NUEVO) — ProcessCashAdvanceAsync (CashDrawerService.cs:336-360) no dispone
  dbTransaction en la ruta de éxito (retención de conexión).

[8W-M4] MEDIO (VIGENTE, cross 06/09) — Validación de cobro/vuelto por UI; global 8W-ZT.

--------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- Doble capa de persistencia: venta + outbox en la misma transacción (ruta principal).
- AsSplitQuery estándar (SplitQuery default en SalesDbContext).
- Multimoneda con PricingCalculator (AwayFromZero, RoundExchangeRateCeiling).
- Auditoría forense y respaldos PDF/texto de arqueos y cierres.
- Idempotencia criptográfica con comparación constante; contrato DST-1 con InvoiceNumber
  confirmado (pendiente el fix 8.2-A3 del handler).

--------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (Sales.Module) — REV. 8.2
--------------------------------------------------------------------------------
1. [8I-CR1] EnrollInTransactionAsync en HoldOrders 100% (crítico de integridad).
2. [8.2-A2] Exclusividad de cierre: SERIALIZABLE + índice único parcial; tokens de fila
   en Sale/CashTransaction/CashDrawerSession (subordina 8S-M6).
3. [8.2-M3] Índice único parcial Status='Open' + apertura atómica.
4. [8S-M3] Implementar IsDelivered/DeliverOnCredit + guard anti doble deducción; publicar
   SaleMadeEvent en ConfirmPickupAsync o prohibir completar venta ya entregada.
5. [8S-M2] Redondear SubtotalBsS/AmountBsS a 2 decimales (AwayFromZero).
6. [8.2-M6] Exigir Idempotency-Key en abonos/hold (con Web).
7. [8S-M1] Proyección mínima AsNoTracking del GetActiveSessionAsync.
8. [8.2-M2] Bloquear cierre sin tasa BCV (o tasa explícita). [W3] decidir PaymentMethodId NOT NULL.
9. [8.2-B1/B2, 8S-M5] Índice único CedulaOrRif, dispose de transacción en ADV, robustez IX.
================================================================================
5. ADENDA DE REMEDIACIONES EJECUTADAS (POST-REVISIÓN 8.2)
================================================================================
Fecha: 2026-09-06
- [8I-CR1] RESUELTO: En `SalesService.HoldOrders.cs:214` se añadió `await _inventoryService.EnrollInTransactionAsync(dbTransaction)`
  antes de llamar a `UpdateStockBatchAsync`, garantizando transacción compartida y atomicidad completa.
- [8.2-A3] RESUELTO: En `SalesService.cs:661` y `SalesService.HoldOrders.cs:264` se excluyen
  los productos IsCashAdvance del snapshot emitido a `SaleMadeEvent.Items`.
- [8.2-A2 / 8.2-M3] RESUELTO: Índices únicos parciales en `SalesDbContext` para `CashDrawerSessions` (`WHERE Status = 1`, sesión única abierta) y `DailyClosures` (`ClosureDate`).
- [8.2-B1] RESUELTO: Índice único sobre `Customers.CedulaOrRif` con captura defensiva de `PostgresException 23505`.
- [8.2-B2] RESUELTO: Disposición garantizada mediante bloque `using var dbTransaction` en `ProcessCashAdvanceAsync`.
- [8S-M2 / 8C-M1 / 8C-M3] RESUELTO: Aplicación de `PricingCalculator.RoundToDigital` a `SubtotalBsS` y montos de abonos en `HoldOrders`.
- [8W-ZT] RESUELTO: Endpoint canónico `/api/sales/checkout-preview` que calcula con precisión fiscal montos, ajustes y vueltos multimoneda.
- Compilación Release: 0 advertencias, 0 errores en `Sales.Module`.

=-------------------------------------------------------------------------------
6. ADENDA REV. 8.4 — RE-CONTRASTACIÓN EMPÍRICA SOBRE HEAD 12eddf6
-------------------------------------------------------------------------------
Cruce con docs/reporte.txt §13 (Rev. 8.4):
  CONFIRMADOS:
  - [8I-CR1] HoldOrders atómico: ✅ EnrollInTransactionAsync antes de UpdateStockBatchAsync
    (SalesService.HoldOrders.cs:214), transacción compartida con Inventory.
  - [8.2-A3] Exclusión IsCashAdvance del snapshot: ✅ (SalesService.cs:661, HoldOrders.cs:264).
  - [8.2-A2 / 8.2-M3] Sesión de caja única y cierre único: ✅ índices únicos parciales
    (SalesDbContext.cs:164-168) + migración 20260906100000; reapertura race-safe.
  - [8.2-B1] Unicidad de clientes: ✅ índice único CedulaOrRif + captura 23505.
  - [8S-M2 / 8C-M1 / 8C-M3] RoundToDigital aplicado a SubtotalBsS y montos de abonos: ✅.
  PARCIAL:
  - [8.2-M6] ❌ PARCIAL. El servidor valida Idempotency-Key SOLO en POST /complete
    (SalesController.cs:410-430); /hold (:108) y /payments (:157) reciben el header del
    cliente (salesApi.js:102) pero NO lo validan → un retry puede duplicar abonos.
  - [8W-ZT] ❌ PARCIAL / ATRIBUCIÓN FALSA. El endpoint canónico EXISTE (SalesController.cs:327)
    pero CheckoutModal.jsx NO lo consume; redondeo y vuelto siguen calculándose por UI
    (CheckoutModal.jsx:90; PaymentForm.jsx float).
===============================================================================
FIN DEL REPORTE Sales.Module — REV. 8.4 (RE-CONTRASTACIÓN)
================================================================================
=================================================================================
7. ADENDA REV. 8.12 — VERIFICACION POST-OLAS S1..S4.2 (HEAD 84e3f84)
=================================================================================
Metodo: verificacion linea a linea contra el arbol actual (2026-09-08).

CAMBIOS DE ESTADO (hallazgos previos de este modulo):
  - [8.2-A2]/[8.2-M3] (carreras cierre/sesion) -> ✅ RESUELTO (indices unicos parciales
    SalesDbContext.cs:164-168 + migracion 20260906100000; cierre atomico fase7).
  - [8.2-M6] (idempotencia hold/abonos) -> ✅ RESUELTO: ResolveIdempotencyAsync en
    /hold (SalesController.cs:192) y /payments (:265), ademas de /complete.
  - [8S-M2] (deriva de centavo) -> ✅ RESUELTO (RoundToDigital).
  - [8S-M5] (filtro IX substring) -> ✅ MEJORADO (SqlState==23505 incluido).
  - [8S-M6] (sin tokens de concurrencia) -> ✅ RESUELTO (xmin en Sales: migracion
    20260906170744_AddXminConcurrencyTokensToSales).
  - [8.2-B1]/[8.2-B2] -> ✅ RESUELTOS (indice unico CedulaOrRif; using en ADV).
  - [8W-M4]/[8W-ZT] -> ✅ RESUELTO: CheckoutModal.jsx:29-30,154,157 consume el preview
    canonico del backend (verificado en 8.12).
  - [8S-M3] (IsDelivered/DeliverOnCredit) -> ❌ VIGENTE: 0 matches en Sales.Module.
  - [W3] (PaymentMethodId nullable) -> 🟡 VIGENTE: sigue int? sin decision de NOT NULL.
  - [8S-M1] (proyeccion GetActiveSession) -> 🟡 PARCIAL (8.5-M1, 2 queries).

HALLAZGOS NUEVOS 8.12 EN ESTE MODULO (los mas criticos del sistema):
  - [8.12-B2] ALTO — 20260805120000_ExpandSubtotalPrecisionTo4Decimals es HUERFANA y NO
    compensada: el snapshot declara decimal(18,4) (SalesDbContextModelSnapshot.cs:564-
    568,622-626), el negocio calcula Math.Round(...,4) (Pricing.cs:170,174), pero la BD
    fresca queda numeric(18,2) (InitialSales.cs:23,43 + SalesDecimalPrecision.cs:22-26;
    grep numeric(18,4) en Program.cs = 0). PostgreSQL redondea a 2 decimales en
    persistencia -> perdida silenciosa de precision en subtotales/precios Bs.S.
  - [8.12-B3] ALTO — 20260907030000_AddPgTrgmGinIndexesRev89 es HUERFANA: los indices
    GIN de la busqueda del historico (SalesService.History.cs:106-120 usa ILike) NUNCA
    se crean. M11 de 8.11 declarado resuelto es INERTE.
  - [8.12-B1] ALTO — Las 7 huerfanas de Sales listadas en 8.11 son correctas, pero la
    compensacion NO es completa: 20260805120000 (B2) y 20260907030000 (B3) no estan
    compensadas por el bloque legacy. Si estan compensadas: IsPhysicalCash (:580),
    Username index/backfill (:662-698), SaleItemQuantity (:601-605) y AmountLocal
    (por 20260322025621, con atributo).
  - [8.12-M3] MEDIO — Los UpdateData DisplayOrder=1/2 (Efectivo USD/Bs.S) de la huerfana
    20260809232500 se pierden en instalaciones frescas: la autocuracion 8.11-F crea la
    columna con DEFAULT 0 pero no siembra 1/2; el seed 20260303194805 tampoco define
    DisplayOrder -> todos quedan en 0 y el orden depende del Id.
=================================================================================
================================================================================
ADENDA REV. 8.13 (OLA DE REMEDIACION TOTAL)
================================================================================
- [8.12-B2] precision Subtotal/UnitPrice 18,4 -> ✅ RESUELTO: la migracion
  20260805120000 esta ACTIVA (era huerfana); BD frescas ya coinciden con el
  modelo/snapshot; BD legacy se auto-corrigen (ALTER idempotente).
- [8.12-B3] indices GIN -> ✅ RESUELTO: 20260907030000 activa (guard pg_trgm).
- [8.12-M3] DisplayOrder -> ✅ RESUELTO (con correccion): el seed real es
  Cash/Card (SalesDbContext.cs:250-253); la migracion AddDisplayOrderToPaymentMethod
  siembra 1/2 por NOMBRE solo si DisplayOrder=0.
- [8.12-B1] resto de Sales -> ✅: AddDisplayOrderToPaymentMethod,
  AddIsPhysicalCashToCashTransactions (boolean, corrige el INT original),
  AlterSaleItemQuantityToNumeric, AddCaseInsensitiveUsernameIndexAndBackfill
  activadas con guards; 20260320000000_AddCashTransactionAmountLocal ELIMINADA
  (duplicada por 20260322025621).
- [8.2-M9] colas -> ✅ RESUELTO (cap 200/1000 con Take antes de materializar).
- Smoke 8.12-M1 valida en BD real: Subtotal scale>=4, DisplayOrder existe,
  ix_users_username_lower existe, factura_number_seq existe.
- [8S-M3]/[W3] -> ❌ VIGENTES; [8S-M5] -> ✅ verificado en 8.12.

================================================================================
ADENDA REV. 8.14 (RE-VERIFICACION 8.13 + HALLAZGOS NUEVOS)
================================================================================
- VIGENCIA 8.13 confirmada: 6 migraciones reactivadas con atributos y guards;
  AddCashTransactionAmountLocal eliminada; DisplayOrder sembrado por nombre;
  ?limit= en colas (Take antes de materializar, HoldOrders.cs:511/History.cs:42).
- [8.12-M3] INOCUO verificado: PaymentMethodService asigna DisplayOrder via
  nextval('paymentmethod_displayorder_seq') explicito (:240-269) con fallback
  Max+1 -> el DEFAULT 0 de la migracion reactivada nunca se usa en inserciones.
- [8.14-N1] MEDIO — cap 200 sin paginacion de UI: >200 OnHold/pickups quedan
  invisibles para los clientes (no envian ?limit ni pagina).
- [8.14-N2] BAJO — seed de DisplayOrder por nombre falla si Cash/Card fueron
  renombrados en BD legacy (quedan 0 y ordenan primero).
- [8.14-N4] BAJO — sin .Designer.cs en las 6 migraciones reactivadas.
- ABIERTOS confirmados: [8S-M3]/[W3] heuristica MT-001, [8S-M2] redondeo
  canonical en hold/abono (8C-M3), W4 (purga 24h de IdempotentRequests).

ADENDA REV. 8.15 (OLA DE CORRECCION TOTAL — estados actualizados)
================================================================================
- [8.2-B5] snake_case residual -> ✅ RESUELTO (variables locales renombradas
  a camelCase en SalesService*.cs).
- [W4] TTL de idempotencia -> ✅ RESUELTO (IdempotencyService con ttl
  configurable; Idempotency:TtlHours en appsettings, default 24 h).
- [8.2-B4]/[8B-B5] cierres fabricados -> ✅ RESUELTO (404 explicito en
  GetCurrentReport/GetReportById).
- [8S-M3]/[W3] doble deduccion -> 🟡 MITIGADO: idempotencia por venta en el
  handler + guard de venta ya completada; PaymentMethodId validado
  pre-persistencia (8.6-B3); nullable conservado para apertura/cierre
  (decision documentada). IsDelivered/DeliverOnCredit siguen sin existir.
- [8C-M1] conversion de pagos a 4 decimales -> ✅ RESUELTO
  (SalesController.cs:496).
- [8C-M3] RoundToDigital en hold/abono -> ✅ VERIFICADO (HoldOrders.cs:130).
- [8.14-N1] paginacion de colas -> ✅ RESUELTO (backend): offset +
  X-Total-Count en pending/pending-pickups; CountPendingSalesAsync/
  CountPendingPickupsAsync en el servicio.
- [8.14-N2] seed DisplayOrder -> ✅ RESUELTO: fallback ROW_NUMBER por Id
  para metodos renombrados (migracion AddDisplayOrderToPaymentMethod).
- Pendiente: 8.14-N4 (Designers).

FIN DEL REPORTE Sales.Module — ADENDA REV. 8.15
=================================================================================
