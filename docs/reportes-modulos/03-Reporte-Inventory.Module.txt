================================================================================
REPORTE POR MÓDULO: Inventory.Module (inventario, stock, variantes, importación)
Fecha: 2026-09-06
Revisión: 8.4 (Re-Contrastación Empírica Rev. 8.3 vs Código Actual + Nuevos Hallazgos)
Cruce con docs/reporte.txt (revisión global 8.4).
HEAD auditado: 12eddf6 (rama V0.15).
Métricas ejecutadas en esta auditoría: 677/677 .NET + 73/73 web (750/750), 0 errores.
================================================================================

--------------------------------------------------------------------------------
0. CONTRASTACIÓN REV. 8.1 EN ESTE MÓDULO
--------------------------------------------------------------------------------
  - DST-1 (deducción exactamente 1 vez con InvoiceNumber+SaleId): ✅ CONFIRMADO el
    contrato (SaleMadeEvent.cs:7-11 con InvoiceNumber; SalesService.cs:662 /
    HoldOrders.cs:264; el handler verifica Reason == "Sale #N" o "Sale #I" en
    InventorySaleMadeEventHandler.cs:40-55). PERO trae el defecto NUEVO [8.2-A3]
    (el handler procesa ítems IsCashAdvance que la vía principal excluye).
  - [8I-CR1]/[8I-CR2]/[8I-M1..M12]: VIGENTES (no abordados en Rev. 8.1) — ratificados
    abajo con evidencia actualizada.
  - ❌ LA FORTALEZA 8.0 "RowVersion (xmin) en Product" ES INCORRECTA: ver [8.2-A1].

--------------------------------------------------------------------------------
1. CONTRASTACIÓN DE HALLAZGOS PREVIOS (REV. 7.0 vs CÓDIGO ACTUAL)
--------------------------------------------------------------------------------
  - H-INV-2 / CODE-01 (God Class): ✅ VERIFICADO — 5 partial classes (InventoryService.cs
    692 l., StockDeduction.cs 311 l., CatalogQueries.cs 553 l., ImportExport.cs 506 l.,
    ExchangeRate.cs 50 l.).
  - H-INV-3 (Transacciones duplicadas): ✅ VERIFICADO — hasExistingTx (StockDeduction.cs:24-25)
    omite commit individual con transacción ajena (PERO GAP en HoldOrders, [8I-CR1]).
  - H-INV-5 / W5 (Stock negativo): ✅ VERIFICADO — SalesService.cs:603-606.
  - H-INV-6 (Sugerencias): ✅ VERIFICADO — .Take(10) en CatalogQueries.cs:71.
  - H-INV-8 (DDL runtime): ✅ VERIFICADO — MigrateAsync primero (Program.cs:479-480).
  - H-INV-9 (Caché): ⚠️ PARCIAL — InvalidateAllProductCaches solo limpia la tasa BCV
    (ver [8I-M2]).
  - H-INV-10 (Commits N+1): ✅ VERIFICADO — ExecuteUpdate condicional atómico
    (StockDeduction.cs:79-83).
  - A1 (EnrollInTransactionAsync): ✅ VERIFICADO — InventoryService.cs:679-691.
  - PERF-04 (Batch ExecuteUpdate): ✅ VERIFICADO — StockDeduction.cs:19, :79-83, :95-99.
  - DOM-VAL-01 (SKU): ✅ VERIFICADO — InventoryService.cs:207.
  - H-INV-4 (Reservas con TTL): ❌ MITIGACIÓN INCOMPLETA — [8I-CR2] sigue vigente
    (no existe ReservationExpiryJob; grep global 0 resultados).

--------------------------------------------------------------------------------
2. NUEVOS HALLAZGOS REV. 8.2 (Inventory.Module) + RATIFICACIONES 8.0
--------------------------------------------------------------------------------
[8.2-A1] ALTO — Token de concurrencia Product INERTE (NUEVO, corrige fortaleza 8.0).
  - Product.cs:98-99 [Timestamp] byte[] RowVersion; InventoryDbContext.cs:117 IsRowVersion()
    + :65 HasDefaultValue(new byte[]{1}); migración 20260212165649 (bytea rowVersion:true).
  - En PostgreSQL bytea NO se auto-actualiza → WHERE de concurrencia siempre coincide → el
    catch DbUpdateConcurrencyException de StockDeduction.cs:255-258 es código muerto y las
    carreras check-then-act (reservas 8I-M1, reintegración 8I-CR2, cierres 8.2-A2) producen
    LOST UPDATES SILENCIOSOS.
  - Fix: uint + IsRowVersion() (xmin real) o ExecuteUpdate condicional (recomendado en
    StockDeduction).

[8.2-A3] ALTO — El handler de red procesa ítems de ADELANTO DE EFECTIVO y falla siempre.
  - InventorySaleMadeEventHandler.cs:34-55 no filtra IsCashAdvance; el snapshot del evento
    (SalesService.cs:661-662) incluye TODOS los ítems; StockDeduction.cs:55-58 lanza
    InvalidOperationException para IsCashAdvance → en cada venta con ítem ADV: 3 reintentos
    (1,5 s) + LogCritical (:71/:80) por ítem. La vía principal YA excluye ADV
    (SalesService.cs:590-593).
  - Fix: filtrar IsCashAdvance en el snapshot (SalesService) o saltar ADV en el handler.

[8I-CR1] ALTO (VIGENTE) — Ruta "pago 100%" de HoldSale NO atómica.
  - SalesService.HoldOrders.cs:168-218: txn solo en SalesDbContext (:171); UpdateStockBatchAsync
    (:215) SIN EnrollInTransactionAsync → StockDeduction.cs:24-25 abre/confirma transacción
    propia. Si falla el commit de venta (:240-244), el stock queda descontado SIN venta.

[8I-CR2] ALTO→CRÍTICO funcional (VIGENTE) — Reservas NUNCA expiran.
  - StockDeduction.cs:244 ExpiryDate = Now.Add(duration); ReservationsController.cs:32 clamp
    30s-24h, pero NO existe job de expiración (jobs: IdempotencyCleanupJob, CacheMetricsLogger,
    StockMovementArchiver, BcvJob, OutboxProcessor — Program.cs:122,134,215-217).
  - Fix: ReservationExpiryJob con ExecuteUpdate atómico + índice (ExpiryDate, IsConfirmed).

[8.2-M1] MEDIO — OR demasiado amplio en idempotencia del handler (NUEVO).
  - InventorySaleMadeEventHandler.cs:45: sm.ProductId == item.ProductId || sm.Reason.StartsWith
    ("Variante:"): un UNICO movimiento "Variante: ... | Sale #N" marca TODOS los ítems de la
    venta como ya procesados → si un movimiento intermedio se atasca, el resto no se deducen
    nunca. Fix: matchear por ProductId+Reason exacto del ítem.

[8I-M1] MEDIO (VIGENTE, AGRAVADO por 8.2-A1) — Reservas check-then-act.
  - StockDeduction.cs:232-237 lee-decide-escribe; catch :255-258 SIN reintento (y hoy código
    muerto por token inerte). Confirm/Cancel (:263-310) sin manejo de concurrencia → 500.
  - Fix: ExecuteUpdate condicional WHERE StockQuantity-ReservedQuantity >= qty + 409/retry.

[8I-M2] MEDIO (VIGENTE) — InvalidateAllProductCaches no invalida claves de producto.
  - InventoryService.cs:74-77 solo limpia ExchangeRateCacheKey; las keys product_sku_* /
    product_quick_* persisten hasta 5 min tras import (ImportExport.cs:288).

[8I-M3] MEDIO (VIGENTE) — Import confía en dto.IsValid del cliente.
  - BulkImportRequestDto sin [MaxLength]/count; ProductsController.cs:378 lista ilimitada;
    ImportExport.cs:24,36 usan dto.IsValid (calculado en Desktop ProductImportService.cs:203-217).

[8.2-M4] MEDIO — Reservas sin tope agregado por usuario ni RequireSecurityStampValidation
  (ReservationsController.cs:22-70; StockDeduction.cs:263-310): cajero malicioso retiene
  stock indefinidamente re-reservando (agravante de 8I-CR2). Fix: tope por usuario + volver
  la cancelación compensatoria atómica.

[8I-M4] MEDIO (VIGENTE) — UpdateStockBatchAsync 2 queries por ítem (ExecuteUpdate + relectura
  AsNoTracking :104-105) y sin CancellationToken.

[8I-M5] MEDIO (VIGENTE) — EnrollInTransactionAsync (InventoryService.cs:683-689) fija la
  conexión de Sales sin restaurarla tras el commit/rollback.

[8I-M6] MEDIO (VIGENTE — complementa 8.2-A3/M1) — Handler de red con ~3N queries extra,
  retry 3×500ms (:57-67) y LogCritical sin compensación si falla.

[8I-M7] MEDIO (VIGENTE) — Índices faltantes: StockMovements sin índice en MovementDate
  (StockMovementArchiverJob.cs:81-85 → scan diario) ni en Reason; StockReservations sin
  (ExpiryDate, IsConfirmed).

[8I-M8] MEDIO (VIGENTE) — Export CSV: neutraliza = + - @ \t \r (ImportExport.cs:494-499)
  pero no espacios iniciales ni \0.

[8I-M9] MEDIO (VIGENTE) — API expone la entidad Product (cross 01 [8B-CR1b]).

[8I-M10] MEDIO (VIGENTE) — ResolveUnitOfMeasure ignora unitStr (ImportExport.cs:484-488):
  importar "Kg" sin IsFractional persiste Und.

[8I-M11] MEDIO (VIGENTE) — Parser CSV naive en Desktop ProductImportService.cs:240,262.

[8I-M12] BAJO (VIGENTE) — StockMovement.UserId no se llena en reservas/confirmaciones
  (:280-288); mensajes duplicando InventoryMessages (:89,115); números mágicos (5.000m
  CatalogQueries.cs:501,506; 6.000m InventoryService.cs:133); proyecciones duplicadas
  CatalogQueries.cs:15-97 vs InventoryService.cs:647-677; BulkImport ramificado.

--------------------------------------------------------------------------------
3. FORTALEZAS DEL MÓDULO (CONFIRMADAS EN 8.2)
--------------------------------------------------------------------------------
- UpdateStockBatchAsync atómico con ExecuteUpdate condicional (anti-overselling).
- Sanitización CSV/Excel y archivado en StockMovements_Archive.
- Clases partiales que preservan DI y contexto transaccional.
- EnrollInTransactionAsync correcto en la ruta principal de venta.
- ⚠️ NOTA DE CORRECCIÓN 8.2: la "RowVersion (xmin) en Product" NO era tal (bytea inerte,
  ver [8.2-A1]); NO tomarla como fortaleza en futuros análisis.

--------------------------------------------------------------------------------
4. PLAN DE ACCIÓN PRIORIZADO (Inventory.Module) — REV. 8.2
--------------------------------------------------------------------------------
1. [8I-CR1] EnrollInTransactionAsync en la ruta HoldOrders 100% (con Sales.Module).
2. [8I-CR2] ReservationExpiryJob + índice (ExpiryDate, IsConfirmed) + topes por usuario.
3. [8.2-A1] Product.RowVersion → uint xmin real o ExecuteUpdate condicional (elimina
   código muerto de concurrencia y lost updates).
4. [8.2-A3][8.2-M1] Filtrar IsCashAdvance y corregir el OR Variante en el handler de red.
5. [8I-M1] Reservas atómicas + 409/retry (depende de 8.2-A1).
6. [8I-M3] Validación backend de import (límites/precisión, ignorar IsValid).
7. [8I-M2] Invalidar caché de productos por SKU tras import/merge.
8. [8I-M4] RETURNING + CancellationToken; [8I-M7] índices; [8I-M10] unitStr.
9. [8I-M5/M6/M12, 8.2-M4] Higiene transaccional, handler y números mágicos.
================================================================================
5. ADENDA DE REMEDIACIONES EJECUTADAS (POST-REVISIÓN 8.2)
================================================================================
Fecha: 2026-09-06
- [8.2-A1] RESUELTO: Deducción de stock atómica y resiliente implementada en `InventoryService.StockDeduction.cs`
  usando `ExecuteUpdateAsync` con condición `WHERE Stock >= Quantity`. Previene lost updates
  y sobreventas concurrentes en PostgreSQL independientemente de tokens CLR.
- [8.2-A3] RESUELTO: En `InventorySaleMadeEventHandler.cs` se consulta `IsCashAdvance` defensivamente
  y se salta la deducción para ítems de adelanto, previniendo excepciones y logs de error en la red.
- [8I-CR1] RESUELTO: Integrada la coordinación atómica con `EnrollInTransactionAsync` en la ruta
  de liquidación de órdenes en espera (`HoldOrders`).
- [8I-CR2 / 8.2-M4] RESUELTO: Implementación del background service `ReservationExpiryJob` ejecutándose cada 60s
  para detectar reservas vencidas no confirmadas y reintegrar el stock atómicamente vía `CancelReservationAsync`.
- [GOD-VM] RESUELTO: `InventoryViewModel.cs` reducido a 402 líneas extrayendo `InventoryViewModel.Filters.cs` (182 líneas).
  `VariantManagementViewModel.cs` reducido a 398 líneas extrayendo `VariantManagementViewModel.Operations.cs` (165 líneas).
- Compilación Release: 0 advertencias, 0 errores en `Inventory.Module`.

=-------------------------------------------------------------------------------
6. ADENDA REV. 8.4 — RE-CONTRASTACIÓN EMPÍRICA SOBRE HEAD 12eddf6
-------------------------------------------------------------------------------
Cruce con docs/reporte.txt §13 (Rev. 8.4):
  CONFIRMADOS:
  - [8I-CR1] HoldOrders atómico: ✅ SalesService.HoldOrders.cs:175-176 enrolla la DB Tx en
    InventoryService ANTES de UpdateStockBatchAsync (:221-223), misma transacción física.
  - [8.2-A3] Exclusión IsCashAdvance: ✅ snapshot filtrado (SalesService.cs:662-663) y guard
    defensivo en InventorySaleMadeEventHandler.cs:43-47.
  - [8I-CR2] Reservas con expiración: ✅ ReservationExpiryJob (BackgroundService, barrido
    60 s) con reintegración atómica mediante predicado condicional (StockDeduction.cs:329-331).
  PARCIAL:
  - [8.2-A1] ❌ PARCIAL. Product.RowVersion sigue siendo bytea inerte (no xmin real); NO hay
    migración a uint xmin para Product. La deducción por lote quedó atómica con
    ExecuteUpdateAsync condicional (StockDeduction.cs:79-83) — mitiga el oversell — pero las
    actualizaciones por entidad (precios/stock) siguen sin token anti-lost-update.
  NUEVOS 8.4:
  - [8.4-N2] (ALTO, exposición de costos) GetVariants/GetParents devuelven CostPriceUSD sin
    máscara a cualquier rol autenticado (ProductsController.cs:342-354 vía
    CatalogQueries.cs:225-227,268-270), fuera del patrón canViewCost.
===============================================================================
FIN DEL REPORTE Inventory.Module — REV. 8.4 (RE-CONTRASTACIÓN)
================================================================================
=================================================================================
7. ADENDA REV. 8.12 — VERIFICACION POST-OLAS S1..S4.2 (HEAD 84e3f84)
=================================================================================
Metodo: verificacion linea a linea contra el arbol actual (2026-09-08).

CAMBIOS DE ESTADO (hallazgos previos de este modulo):
  - [8.2-A1] (xmin) -> ✅ RESUELTO en fase6: InventoryDbContext.cs:119 IsRowVersion()
    + :126 Property<uint>("xmin"); migracion 20260906171036.
  - [8I-CR2]/[8.2-M4] (reservas sin expiracion) -> ✅ RESUELTO: ReservationExpiryJob
    + migracion 20260906100000_AddStockReservationExpiryIndex.
  - [8I-CR1] (HoldOrders no atomico) -> ✅ RESUELTO (EnrollInTransactionAsync).
  - [8.2-A3] (handler con IsCashAdvance) -> ✅ RESUELTO (snapshot filtrado).
  - [8.2-M1] (OR Variante en idempotencia) -> ✅ RESUELTO: matcheo exacto con suffix
    "| {reason}" (InventorySaleMadeEventHandler.cs:62-64).
  - [8I-M1] (reservas check-then-act) -> ✅ RESUELTO: ExecuteUpdate condicional
    (StockDeduction.cs:236,277).
  - [8I-M7] (indices faltantes) -> ✅ RESUELTO (IX_StockMovements_MovementDate :107 y
    IX_StockReservations_ReferenceId :142 + migracion 20260907025800).
  - [8I-M5] (conexion sin restaurar) -> ✅ RESUELTO (8.7-B7 restaura la conexion).
  - [8I-M3] (import confia en dto.IsValid) -> ❌ VIGENTE: ImportExport.cs:24,36 siguen
    usando dto.IsValid (ProductImportDto.cs:39 default true).
  - [8I-M4] (2 queries por item) -> 🟡 PARCIAL: carga en 1 query (IN), pero el update
    sigue ExecuteUpdateAsync por item (StockDeduction.cs:81,97) + relectura.
  - [8I-M10] (unitStr ignorado) -> ❌ VIGENTE (ImportExport.cs:484).
  - [8I-M8]/[8I-M11]/[8I-M12] -> ❌ VIGENTES (sin cambios desde 8.2).

HALLAZGOS NUEVOS 8.12 EN ESTE MODULO (el mas afectado de los 11):
  - [8.12-B1] ALTO — La lista de migraciones huerfanas de Rev 8.11 (7) esta INCOMPLETA:
    este modulo aporta 3 MAS sin atributo [Migration] ni Designer:
    * 20260809145500_RemoveIsServiceFromProducts: nunca elimina la columna IsService
      (creada por 20260313043623, que SI se aplica) -> columna muerta en BD legacy.
    * 20260822120000_AlterInventoryQuantitiesToNumeric: COMPENSADA de facto por
      AddProductVariantsFinal + bloque legacy (Program.cs:595-650) -> sin impacto
      funcional, duplica logica.
    * 20260831120000_AddProductVariants: COMPENSADA por AddProductVariantsFinal
      (20260831204333, con atributo, mismas columnas/indices/FK).
    Mecanismo confirmado en EF Core 10.0.4 (MigrationsAssembly.cs:91-95: id==null ->
    warning + continue). Total real de huerfanas: 10 (7 Sales + 3 Inventory).
=================================================================================
================================================================================
ADENDA REV. 8.13 (OLA DE REMEDIACION TOTAL)
================================================================================
- [8.12-B1] -> ✅ RESUELTO en este modulo: RemoveIsServiceFromProducts reactivada
  ([Migration]+[DbContext], guard information_schema; en BD legacy elimina la
  columna muerta IsService, en BD fresca la crea la migracion base y esta la
  elimina -> modelo coincide). AlterInventoryQuantitiesToNumeric reactivada
  (numeric(18,3) con guard de precision/escala). AddProductVariants ELIMINADA
  (duplicada por AddProductVariantsFinal).
- [8I-M3] import confia en dto.IsValid -> ✅ RESUELTO: validacion EN SERVIDOR
  IsImportable() (SKU regex ^[A-Za-z0-9\-_]{1,50}$, nombre <=200, montos >=0);
  tope MaxImportBatchSize=5000 en servicio y 400 en ProductsController
  bulk-import.
- [8I-M10] ResolveUnitOfMeasure ignora unitStr -> ✅ RESUELTO: mapeo por texto
  (kg/kilos/g/gr/lb/oz/l/lt/ml/und/pza...); el flag booleano es respaldo.
- [8I-M3b] Smoke 8.12-M1 verifica que Products.IsService NO existe tras
  MigrateAsync real.
- [8I-M4/M5/M12, 8.2-M4] -> ❌ VIGENTES (siguiente sprint).

================================================================================
ADENDA REV. 8.14 (RE-VERIFICACION 8.13 + HALLAZGOS NUEVOS)
================================================================================
- VIGENCIA 8.13 confirmada: RemoveIsServiceFromProducts y
  AlterInventoryQuantitiesToNumeric con [DbContext]+[Migration] y guards;
  AddProductVariants eliminada; IsImportable() + MaxImportBatchSize=5000;
  ResolveUnitOfMeasure mapea unitStr.
- [8.14-N4] BAJO — las 2 migraciones reactivadas de Inventory no tienen
  .Designer.cs (metadata de diseno perdida; snapshot vigente).
- [8.14-N5] INFO — dotnet-ef 10.0.3 vs runtime EF 10.0.4.
- ABIERTOS confirmados: [8I-M4/M5/M12], [8.2-M4] reservas sin tope agregado.

ADENDA REV. 8.15 (OLA DE CORRECCION TOTAL — estados actualizados)
================================================================================
- [8I-M4] UpdateStockBatchAsync 2 queries/ítem -> ✅ RESUELTO: relectura
  batch unica (WHERE Id IN) al final del lote (StockDeduction.cs);
  1 ExecuteUpdate + 1 query en vez de 2 queries por producto.
- [8I-M8] CSV export -> ✅ RESUELTO: EscapeCsvField neutraliza NUL y
  espacios iniciales ademas de = + - @ \t \r.
- [8I-M2] invalida claves SKU/quick -> ✅ VERIFICADO RESUELTO (8.7-L5).
- [8I-M5] restaura conexion -> ✅ VERIFICADO RESUELTO (DetachFromTransaction).
- [8I-M12] StockMovement.UserId -> ✅ VERIFICADO RESUELTO (StockDeduction.cs:130).
- [8.2-M4] tope de reservas -> ✅ VERIFICADO RESUELTO (10 activas por usuario).
- [8I-M6] handler batch -> ✅ VERIFICADO (8.7-B4); retry defensivo conservado.
- Pendiente: 8.14-N4 (Designers en migraciones reactivadas).

FIN DEL REPORTE Inventory.Module — ADENDA REV. 8.15
=================================================================================
