<UserControl x:Class="Desktop.Client.Controls.OmniboxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Border x:Name="SearchBorder" BorderThickness="2" CornerRadius="5" Background="White">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="Gray"/> <!-- Idle -->
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsScanning}" Value="True">
                            <Setter Property="BorderBrush" Value="Green"/> <!-- Scanning -->
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSearching}" Value="True">
                             <Setter Property="BorderBrush" Value="Blue"/> <!-- Searching -->
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            
            <Grid>
                <TextBox x:Name="InputBox"
                         VerticalContentAlignment="Center"
                         Padding="10,0"
                         FontSize="16"
                         BorderThickness="0"
                         Background="Transparent"
                         GotFocus="InputBox_GotFocus"
                         TextChanged="InputBox_TextChanged"
                         PreviewKeyDown="InputBox_PreviewKeyDown"/>
                
                <TextBlock Text="{Binding Placeholder, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           IsHitTestVisible="False"
                           VerticalAlignment="Center"
                           Margin="12,0"
                           Foreground="Gray"
                           FontSize="16">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=InputBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
