<Window x:Class="Desktop.Client.Views.VersionLockoutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Actualización Requerida" Height="420" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="12" BorderBrush="#DC2626" BorderThickness="2" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4"/>
        </Border.Effect>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,15">
                <materialDesign:PackIcon Kind="ShieldAlert" Foreground="#DC2626" Width="64" Height="64" HorizontalAlignment="Center"/>
                <TextBlock Text="Actualización Obligatoria" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Foreground="#DC2626" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- BODY -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Margin="0,0,0,20"/>

                <Border Background="#FEE2E2" CornerRadius="8" Padding="14" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="Versión Instalada" FontSize="11" Opacity="0.7"/>
                            <TextBlock Text="{Binding CurrentVersion}" FontSize="18" FontWeight="Bold" Foreground="#991B1B"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock Text="Versión Mínima" FontSize="11" Opacity="0.7"/>
                            <TextBlock Text="{Binding MinimumClientVersion}" FontSize="18" FontWeight="Bold" Foreground="#065F46"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- FOOTER -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Button Content="Descargar Parche / Actualizar" Command="{Binding StartUpdateCommand}" Style="{DynamicResource MaterialDesignRaisedButton}" Background="#DC2626" BorderBrush="#DC2626" Height="42" Padding="20,0" Margin="0,0,15,0"/>
                <Button Content="Cerrar Aplicación" Command="{Binding ExitAppCommand}" Style="{DynamicResource MaterialDesignOutlinedButton}" Height="42" Padding="20,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
