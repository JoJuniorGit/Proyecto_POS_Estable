<Window x:Class="Desktop.Client.Views.VariantSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Seleccionar Variante / Sabor" 
        Height="460" Width="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        KeyDown="Window_KeyDown">

    <Border CornerRadius="14" 
            Background="{DynamicResource MaterialDesignPaper}" 
            BorderBrush="{DynamicResource PrimaryHueMidBrush}" 
            BorderThickness="1.5"
            Effect="{DynamicResource MaterialDesignShadowDepth4}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="12,12,0,0" Padding="18,12">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="FormatListBulletedType" Width="24" Height="24" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding ParentProduct.Name}" FontSize="16" FontWeight="Bold" Foreground="White"/>
                            <TextBlock FontSize="12" Foreground="White" Opacity="0.9">
                                <Run Text="Precio: $"/><Run Text="{Binding BasePriceUSD, StringFormat={}{0:F2}}"/>
                                <Run Text=" ("/><Run Text="{Binding BasePriceBsS, StringFormat={}{0:N2}}"/><Run Text=" Bs.S)"/>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" 
                            Foreground="White" Width="36" Height="36" Padding="0" Click="Cancel_Click">
                        <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content Body -->
            <Grid Grid.Row="1" Margin="20,16,20,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Seleccione el sabor o presentación deseada (Use Flechas ↑ ↓ y Enter):" 
                           FontSize="13" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,0,0,10"/>

                <!-- Loading Indicator -->
                <ProgressBar Grid.Row="1" IsIndeterminate="True" 
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                             HorizontalAlignment="Center" VerticalAlignment="Center" Width="80" Height="8"/>

                <!-- Error Message -->
                <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="Red" FontSize="13" TextWrapping="Wrap"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}"/>

                <!-- List of Variants -->
                <ListBox x:Name="VariantsListBox" Grid.Row="1"
                         ItemsSource="{Binding Variants}"
                         SelectedItem="{Binding CurrentSelectedVariant, Mode=TwoWay}"
                         MouseDoubleClick="ListBox_MouseDoubleClick"
                         HorizontalContentAlignment="Stretch"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,8" Margin="0,2" CornerRadius="8" Background="{DynamicResource MaterialDesignCard}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource MaterialDesignBody}"/>
                                        <TextBlock Text="{Binding SKU, StringFormat='SKU: {0}'}" FontSize="11" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                    </StackPanel>

                                    <Border Grid.Column="1" Margin="10,0" Padding="8,4" CornerRadius="6"
                                            Background="#E8F5E9" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StockQuantity, StringFormat='{}{0:G29} disponibles'}" 
                                                   FontWeight="SemiBold" FontSize="12" Foreground="#2E7D32"/>
                                    </Border>

                                    <Button Grid.Column="2" Style="{StaticResource MaterialDesignRaisedButton}"
                                            Height="32" Padding="10,0" VerticalAlignment="Center"
                                            Command="{Binding DataContext.SelectVariantCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Check" Width="16" Height="16" Margin="0,0,4,0"/>
                                            <TextBlock Text="Elegir"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="2" Padding="20,12" Background="{DynamicResource MaterialDesignCard}" CornerRadius="0,0,12,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"
                            Content="Cancelar (Esc)" Click="Cancel_Click"/>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Content="Seleccionar (Enter)" Click="Confirm_Click"
                            IsEnabled="{Binding HasSelectedVariant}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
