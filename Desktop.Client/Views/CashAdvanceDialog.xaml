<Window x:Class="Desktop.Client.Views.CashAdvanceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Cash Advance Request" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Cash Advance Details" Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,25" FontWeight="Bold"/>

        <!-- Main Form -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Enter the total amount to withdraw from the register:" TextWrapping="Wrap" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <materialDesign:PackIcon Kind="CashMultiple" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="AmountBox" Width="280" materialDesign:HintAssist.Hint="Amount Requested" Style="{DynamicResource MaterialDesignOutlinedTextBox}" TabIndex="1" PreviewTextInput="NumberValidationTextBox" DataObject.Pasting="AmountBox_Pasting"/>
            </StackPanel>

            <TextBlock x:Name="ErrorText" Text="" Foreground="Red" Margin="34,5,0,0" Visibility="Collapsed"/>
        </StackPanel>

        <!-- Footer / Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Style="{DynamicResource MaterialDesignFlatButton}" Margin="0,0,15,0" IsCancel="True" TabIndex="3"/>
            <Button Content="Add to Cart" Click="Save_Click" Style="{DynamicResource MaterialDesignRaisedButton}" IsDefault="True" TabIndex="2"/>
        </StackPanel>
    </Grid>
</Window>
