<UserControl x:Class="Desktop.Client.Views.CashAmountDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:b="clr-namespace:Desktop.Client.Behaviors"
             Width="320" Padding="20">
    <StackPanel>
        <TextBlock x:Name="TitleText" Text="Enter Amount" FontSize="18" FontWeight="Medium" Margin="0,0,0,16"/>
        <TextBox x:Name="AmountBox" b:IntegerTextBoxBehavior.IsIntegerOnly="True" materialDesign:HintAssist.Hint="0" Margin="0,0,0,24" FontSize="18" HorizontalContentAlignment="Center"/>
        
        <Button x:Name="BtnKeepAmount" Content="Keep expected cash in fund" Style="{DynamicResource MaterialDesignFlatButton}" 
                Visibility="Collapsed" Click="BtnKeepAmount_Click" Margin="0,0,0,16" Foreground="{DynamicResource PrimaryHueMidBrush}" />

        <TextBlock x:Name="ErrorText" Foreground="Red" Visibility="Collapsed" Margin="0,0,0,16"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL" IsCancel="True" Style="{DynamicResource MaterialDesignFlatButton}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Margin="0,0,8,0">
                <Button.CommandParameter>
                    <system:Boolean>False</system:Boolean>
                </Button.CommandParameter>
            </Button>
            <Button Content="CONFIRM" Style="{DynamicResource MaterialDesignFlatButton}" Click="Confirm_Click" />
        </StackPanel>
    </StackPanel>
</UserControl>
