<UserControl x:Class="Desktop.Client.Views.ImportProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:Desktop.Client.ViewModels;assembly=Desktop.Client.Core"
             xmlns:controls="clr-namespace:Desktop.Client.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="Transparent"
             FontFamily="{DynamicResource MaterialDesignFont}"
             d:DataContext="{d:DesignInstance Type=vm:ImportProductsViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="20" AllowDrop="True" Drop="Grid_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <Button Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
                    CommandParameter="{x:Static Dock.Right}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="Abrir Menú de Navegación"
                    Margin="0,0,16,0" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Cog" Width="24" Height="24" />
            </Button>
            <TextBlock Text="IMPORTACIÓN Y EXPORTACIÓN DE PRODUCTOS" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="Exporte el catálogo actual o descargue la plantilla para cargar/actualizar masivamente productos." Style="{DynamicResource MaterialDesignSubtitle2TextBlock}" Foreground="{DynamicResource MaterialDesignTextBoxBorder}" Margin="0,0,0,15"/>

        <!-- CONTROLS & STATUS -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding ExportCatalogCommand}" Style="{StaticResource MaterialDesignRaisedSecondaryButton}" IsEnabled="{Binding CanMutateCatalog}" Margin="0,0,10,0" ToolTip="Exportar catálogo completo a archivo CSV">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FileExportOutline" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="Exportar Catálogo" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="1" Command="{Binding SelectFileCommand}" Style="{StaticResource MaterialDesignRaisedButton}" IsEnabled="{Binding CanMutateCatalog}" Margin="0,0,10,0" ToolTip="Seleccionar un archivo Excel (.xlsx) o CSV (.csv) para importar masivamente productos">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CloudUploadOutline" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="Cargar / Importar Productos" VerticalAlignment="Center" FontWeight="Bold"/>
                </StackPanel>
            </Button>
            
            <Button Grid.Column="2" Command="{Binding DownloadTemplateCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" IsEnabled="{Binding CanMutateCatalog}" Margin="0,0,15,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FileDownloadOutline" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="Descargar Plantilla" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <CheckBox Grid.Column="3" Content="Sobrescribir coincidencias por SKU (Merge)" IsChecked="{Binding OverwriteMerge, Mode=TwoWay}" IsEnabled="{Binding CanMutateCatalog}" Style="{StaticResource MaterialDesignDarkCheckBox}" ToolTip="Actualiza los productos existentes que coincidan por SKU." Margin="0,0,20,0" VerticalAlignment="Center" />

            <!-- PROGRESS BAR AND STATUS MSG -->
            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="{Binding IsProgressIndeterminate}" Value="{Binding ProgressValue}" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" Width="100" Height="10" Margin="0,0,10,0" />
                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>

        <!-- COLUMN MAPPING -->
        <materialDesign:Card Grid.Row="3" Margin="0,0,0,15" Visibility="{Binding HasMappings, Converter={StaticResource BoolToVis}}">
            <StackPanel Margin="15">
                <TextBlock Text="Asignar Columnas del Archivo a Propiedades del Sistema" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <ItemsControl ItemsSource="{Binding ColumnMappings}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,20,10" Width="180">
                                <TextBlock Text="{Binding ExcelHeader}" FontWeight="Medium" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                                <ComboBox ItemsSource="{Binding AvailableProperties}" SelectedItem="{Binding SelectedProperty}" Style="{DynamicResource MaterialDesignOutlinedComboBox}" Margin="0,5,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Command="{Binding ProcessDataCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Content="Procesar Datos" HorizontalAlignment="Right" Margin="0,10,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- DATAGRID -->
        <controls:PerformantDataGrid Grid.Row="4" ItemsSource="{Binding ImportedProducts}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" HeadersVisibility="All" Background="{DynamicResource MaterialDesignPaper}">
            <controls:PerformantDataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                    <Style.Triggers>
                        <!-- Highlight invalid rows firmly -->
                        <DataTrigger Binding="{Binding IsValid}" Value="False">
                            <Setter Property="Background" Value="#FFCDD2" />
                            <Setter Property="Foreground" Value="#B71C1C"/>
                            <Setter Property="ToolTip" Value="{Binding ErrorMessage}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:PerformantDataGrid.RowStyle>
            
            <controls:PerformantDataGrid.Columns>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="SizeToCells" MinWidth="80"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Costo (USD)" Binding="{Binding CostPriceUSD, StringFormat={}{0:C2}}" Width="100" />
                <DataGridTextColumn Header="Margen Detal" Binding="{Binding ProfitMarginRetail, StringFormat={}{0:N2}%}" Width="90" />
                <DataGridTextColumn Header="Precio Detal (USD)" Binding="{Binding PriceRetailUSD, StringFormat={}{0:C2}}" Width="110"/>
                <DataGridTextColumn Header="Margen Mayor" Binding="{Binding ProfitMarginWholesale, StringFormat={}{0:N2}%}" Width="90" />
                <DataGridTextColumn Header="Precio Mayor (USD)" Binding="{Binding PriceWholesaleUSD, StringFormat={}{0:C2}}" Width="110"/>
                <DataGridCheckBoxColumn Header="Mayorista" Binding="{Binding HasWholesale}" Width="75"/>
                <DataGridCheckBoxColumn Header="Fraccionable" Binding="{Binding IsFractional}" Width="85"/>
                <DataGridTextColumn Header="Unidad" Binding="{Binding UnitOfMeasure}" Width="70"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="70"/>
                
                <DataGridTextColumn Header="Estado / Error" Binding="{Binding ErrorMessage}" Foreground="DarkRed" Width="SizeToCells" MinWidth="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                            <Setter Property="ToolTip" Value="{Binding ErrorMessage}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

            </controls:PerformantDataGrid.Columns>
        </controls:PerformantDataGrid>

        <!-- FOOTER ACTIONS -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Limpiar" Command="{Binding ClearDataCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,15,0" Visibility="{Binding HasFileData, Converter={StaticResource BoolToVis}}" />
            <Button Content="Guardar en Base de Datos" Command="{Binding CommitImportCommand}" Style="{StaticResource MaterialDesignRaisedButton}" IsEnabled="{Binding CanCommit}" ToolTip="Guardar filas válidas en la base de datos PostgreSQL." />
        </StackPanel>
    </Grid>
</UserControl>
