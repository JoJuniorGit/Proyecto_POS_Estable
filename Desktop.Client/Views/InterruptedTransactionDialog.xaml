<Window x:Class="Desktop.Client.Views.InterruptedTransactionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Operación Interrumpida" Height="360" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{DynamicResource MaterialDesignPaper}" CornerRadius="12" BorderBrush="#D97706" BorderThickness="2" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4"/>
        </Border.Effect>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,15">
                <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="#D97706" Width="56" Height="56" HorizontalAlignment="Center"/>
                <TextBlock Text="Operación Interrumpida" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Foreground="#D97706" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- BODY -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" LineHeight="20" Margin="0,0,0,15"/>
            </StackPanel>

            <!-- FOOTER -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Button Content="Entendido / Volver al Mostrador" 
                        Command="{Binding DismissCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{DynamicResource MaterialDesignRaisedButton}" 
                        Background="#D97706" 
                        BorderBrush="#D97706" 
                        Height="42" 
                        Padding="24,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
