<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================
         LIGHT MODE NAV PALETTE
         All values meet WCAG AA contrast (≥ 4.5:1 for normal text)
         Colors inlined directly into brushes for maximum compatibility
         ============================================================ -->

    <!-- Background & Surface -->
    <SolidColorBrush x:Key="NavBgBrush"          Color="#F8F9FA"/>
    <SolidColorBrush x:Key="NavBorderBrush"      Color="#CBD5E1"/>

    <!-- Typography -->
    <SolidColorBrush x:Key="NavTextBrush"        Color="#1A1A2E"/>  <!-- Contrast 14.7:1 on NavBg -->
    <SolidColorBrush x:Key="NavTextMutedBrush"   Color="#475569"/>  <!-- Contrast 5.4:1  on NavBg -->

    <!-- States -->
    <SolidColorBrush x:Key="NavHoverBgBrush"     Color="#E2E8F0"/>
    <SolidColorBrush x:Key="NavActiveBgBrush"    Color="#3B82F6"/>  <!-- Active item -->
    <SolidColorBrush x:Key="NavActiveTextBrush"  Color="#FFFFFF"/>  <!-- Contrast 4.6:1 on ActiveBg -->
    <SolidColorBrush x:Key="NavSectionHeaderBrush" Color="#64748B"/>

    <!-- ============================================================
         NAV ITEM BUTTON STYLE
         Custom ControlTemplate with hover / active / disabled states
         ============================================================ -->
    <Style x:Key="NavItemButtonStyle" TargetType="Button">
        <Setter Property="Background"       Value="Transparent"/>
        <Setter Property="Foreground"       Value="#1A1A2E"/>
        <Setter Property="BorderThickness"  Value="0"/>
        <Setter Property="Padding"          Value="12,10"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Cursor"           Value="Hand"/>
        <Setter Property="FontSize"         Value="13"/>
        <Setter Property="FontWeight"       Value="Medium"/>
        <Setter Property="Margin"           Value="8,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="#E2E8F0"/>
                        </Trigger>
                        <!-- Pressed -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="#3B82F6"/>
                            <Setter Property="Foreground"
                                    Value="#FFFFFF"/>
                        </Trigger>
                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Active variant of the nav item (currently selected view) -->
    <Style x:Key="NavItemActiveButtonStyle" TargetType="Button"
           BasedOn="{StaticResource NavItemButtonStyle}">
        <Setter Property="Background" Value="#3B82F6"/>
        <Setter Property="Foreground" Value="#FFFFFF"/>
    </Style>

    <!-- Section header label inside the sidebar -->
    <Style x:Key="NavSectionHeaderStyle" TargetType="TextBlock">
        <Setter Property="Foreground"   Value="#64748B"/>
        <Setter Property="FontSize"     Value="10"/>
        <Setter Property="FontWeight"   Value="Bold"/>
        <Setter Property="Margin"       Value="16,14,8,4"/>
    </Style>

</ResourceDictionary>
